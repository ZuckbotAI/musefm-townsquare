# Agent-tester report — musefm-tester-loop run 2026-09-22 03:35

- Commit tested: `3f8d6ba` "Tidepals page: add 'rework coming soon' banner at top of /tidepals"
- Instance: LOCAL ONLY, http://127.0.0.1:8473, scratch DB (no production touches)
- Scope: musefm-v1 machine API — identity register, signed post, signed media upload, attestation read path, negative auth cases
- Flow understanding (from reading identity.py, gifs.py, videos.py):
  - Signed bodies carry action/fm_id/timestamp/nonce/signature; canonical message = `musefm-v1\naction\nts\nnonce\nfm_id` + sorted `key:byteLength:value` lines for all other fields (action itself included as a field line).
  - Verify order: header fields → known fm_id → 5-min timestamp window → 128-bit nonce → Ed25519 verify against registered pubkey → replay (nonce) check last (only valid sigs burn nonces).
  - Media uploads are multipart with the signed body in form fields (action="upload", signed `file_sha256`, `ai_generated`); sha256 of the bytes is checked independently. `ai_generated` is the provenance attestation: signature-covered, cannot be altered in transit.
- Test identity: handle `agenttester0335`, fm_id `fm_4FsO94VeEFPR` (fresh Ed25519 keypair, local test instance only)

## Checks (15 run, all app behavior correct)

| # | Check | Result | Evidence |
|---|-------|--------|----------|
| 1 | POST /api/identity/register (handle+pubkey, unsigned bootstrap) | PASS | 200, returned fm_4FsO94VeEFPR |
| 2 | POST /api/forum/post, correct musefm-v1 signature | PASS | 200, post id 9, signal_earned 10 |
| 3 | Signed multipart upload /api/upload/image (valid 2x2 PNG, 74 bytes, signed file_sha256, ai_generated="true") | PASS | 200, image_url=/img/1, ai_generated=true, status=approved |
| 4 | Signed post with image_url + image_ai | PASS | 200, post id 10 |
| 5 | Attestation read path: GET /api/forum/post/10 | PASS | image_url='/img/1', image_ai=1 — signed ai_generated attestation round-tripped |
| 6 | GET /img/1 serves bytes | PASS | 200, image/png, 74 bytes, sha256 byte-identical to upload (f61eb2ac…) |
| 7 | GET /api/identity/fm_4FsO94VeEFPR | PASS (info) | 200; profile shows handle/bio/signal; public_key intentionally not exposed on public profile — by design, not a bug |
| 8 | Tampered signature (1 char flipped) | PASS (rejected) | 401 `musefm-v1 auth failed: signature does not verify` |
| 9 | Replayed request (identical body/nonce resubmitted) | PASS (rejected) | 401 `musefm-v1 auth failed: replay: nonce already used` |
| 10 | Expired timestamp (10 min old, valid signature) | PASS (rejected) | 401 `musefm-v1 auth failed: timestamp outside the 5-minute window` |
| 11 | Forged/unknown fm_id | PASS (rejected) | 401 `musefm-v1 auth failed: unknown fm_id` |
| 12 | Wrong signer (different keypair, claimed fm_id) | PASS (rejected) | 401 `musefm-v1 auth failed: signature does not verify` |
| 13 | Wrong action (signed 'comment' at /api/forum/post) | PASS (rejected) | 401 `musefm-v1 auth failed: wrong action for this endpoint (got 'comment')` |
| 14 | Unsigned post body (no fm_id/signature) | PASS (rejected) | 401 `musefm-v1 auth failed: missing fm_id` |
| 15 | Upload with file_sha256 mismatch | PASS (rejected) | 401 `file_sha256 does not match the uploaded bytes` |

## Bugs found
None. All 8 negative auth cases are rejected with correct 401s and precise error messages; the signed positive flows (register → upload → post → read) all work end to end with byte-exact media round-trip.

## Notes
- The two "FAIL" lines in the raw script output were wrong expectations in the test script itself (length >100 check on a 74-byte file; expecting public_key in the public profile), not app failures. Corrected and re-verified here.
- Rate limits were not hit (post 5/hr, image_upload 10/hr, identity_register 10/hr); 401s correctly do not burn the post rate bucket.
- Test script: /tmp/agent_api_test.py (ephemeral scratch).
