# Tester-loop bug file — Muse FM (run 2026-09-19 ~09:35 CDT)

- **Commit tested:** `1e4fa8e` — "Tidepal expansion: hatch economy grants Signal, animated Town Pond, /pet clarity pass"
- **Target:** local only, `http://127.0.0.1:8473` on scratch DB (`hidden_files/tester-loop/test.db`, deleted at run start — this run fully hermetic). Production untouched except read-only smoke.
- **Testers:** human tester (report: human-tester-2026-09-19-0935.md), agent tester (report: agent-tester-2026-09-19-0935.md), adversarial QA (report: adversarial-2026-09-19-0935.md).
- **Own suites:** test_sidebar.py 97/97, test_musefm.py 75 passed / 2 failed (both known-stale, see below), test_shorts.py 77 passed / 1 failed (known-stale, see below), test_auth.py 62/62, test_identity.py 41/41, test_uploads.py 24/24.
- **Production smoke (read-only):** `/`, `/api/shorts`, `/musefm`, `/shorts` on https://musefm-townsquare.onrender.com → all 200. Live site healthy.

## Verdict: no new bugs. Everything this run found dedupes to known issues or is as-designed. Chat-silent.

## New P1 / P0: NONE

## Findings this run (all deduped or by-design)

1. **SQLite lock 500s (transient, under concurrent writers)** — human tester (`POST /fb_react` ~14:39 UTC), agent tester (`POST /api/forum/vote`), adversarial QA (`/api/forum/vote`, `/api/forum/comment`, `/fb_react`, `/api/upload/audio`; traceback `app.py:445 verify_signed_body → identity.py:196 db.note_nonce → db.py:1624/658`). Matches the post-demo defect queue's "SQLite lock contention causing concurrent-write 500s" and the 06:35 run's escalated wedge P1. **Caveat:** all three tester subagents hammered one SQLite DB from 127.0.0.1, so concurrency was partly a test artifact; the missing retry/backoff in `db._exec` is real regardless. Not new.
2. **`/submit` has no CSRF token** (human tester; repro: plain multipart POST → 302, posts.id 8) — known since 2026-09-18 18:35 ("Missing CSRF on `/submit`, `/fb_react`, `/photos/upload`"). Not new.
3. **Votes/reactions on nonexistent targets silently 302** (human tester) — known (06:35 known issue #6: HTML form paths of `/vote` and `/fb_react` swallow validation errors into silent 302s). Not new.
4. **10k-char comment silently truncated to 2000 with no notice** (human tester) — known (06:35 known issue #5). Adversarial QA separately confirmed no body cap raises a 400 (10k/50k chars → 200). Not new.
5. **Rapid identical double-vote toggles the vote off** (adversarial QA) — NOT A BUG. Toggle semantics verified "correct" in the 06:35 run (vote toggle/idempotency: 5× rapid concurrent votes → exactly one row; human tester re-confirmed idempotency this run). Same-value-second-vote = un-vote is the designed toggle behavior. Closed as by-design.
6. **`/api/forum/react` allows stacking different emojis while `fb_react` enforces one-per-person** (adversarial QA, framed as a question) — NOT A BUG, by design. Verified in code this run: `db.py:346-348` — `reactions` table PK is `(target_type, target_id, reactor, emoji)` with an inline comment marking it the multi-emoji model; `fb_reactions` PK is `(target_type, target_id, reactor)` (one reaction per reactor, `fb_reactions.py:39`). Two distinct reaction models, schema-explicit. Closed as by-design.
7. **Suite failures** — `test_musefm.py` "photos shows agent photo newest-first" + "no musebook in template copy", and `test_shorts.py` "home shorts open the anchored feed": all three re-confirmed this run, all already filed as stale-test P2s (06:35 run). Not new, not regressions.

## Agent tester notes (not bugs)

- 19 machine-API flows verified green with live evidence: identity registration, signed post (id=11, author verified from registry), signed comment (id=4), signed vote, signed react, signed identity_update, all five negative auth cases (bad signature, wrong action, replayed nonce, stale timestamp, unknown fm_id → 401s), and all three platform attestation envelopes (signal credential, passport, signed activity feed) verified against `/api/platform-key` with an independent verifier.
- 5 flows [UNVERIFIED this run] — image/video/audio/GIF uploads, bounty, heartbeat: the shared-127.0.0.1 `/api/identity/register` bucket (10/hr) was exhausted by the three parallel testers; every retry returned 429. Test-range artifact, not an app bug. **Carry to next run** after the bucket resets.

## Verified good this run (no bug)

- XSS: `<script>` + `<img onerror>` in comment bodies and post titles render fully escaped in `<title>`, og/twitter meta, and H1 — no stored XSS (human tester, code + rendered-HTML evidence).
- Human flows: signup/login, post creation, comments, vote idempotency, fb reactions, photo upload magic-byte validation (fake PNG → 400, valid PNG → mod-pending), episode comments, audio range requests (206), shorts/episodes pages (200), proper 404s, all rate limits (post 5/hr, comment 30/hr, vote/react 120/hr → friendly 429s).
- Machine-API auth: unauthenticated hits → 401 on 15 endpoints; replay/tampered-signature/future-timestamp → 401; path traversal → 400/404 everywhere probed; 26MB upload → 413; PNG-as-MP3 → 400 (magic-byte sniffing); sha mismatch → 401; malformed JSON on register → clean 400s (never 500s); `/login?next=` escaped, no open redirect; branded 404s; correct 405s.

## Run caveats / not tested

- `curl -F` with a leading `<` in field values dies (HTTP:000) — curl interprets `<` as file-read syntax; identical payloads via python-requests worked. Testing artifact only (human tester).
- Title-length probes blocked by the shared-IP post rate limit (adversarial QA); /submit image upload not exercised (post bucket exhausted, human tester).
- True mobile viewport not tested (no browser tooling).
- No source edits, no commits, no production writes (read-only smoke only).

## Notes

- Fix policy followed: no app source edited. No new failing test added — the only new claims this run turned out to be by-design on code inspection; the failing-test budget is better spent on the wedged-writer P1 (06:35) which remains nondeterministic.
- Test server on :8473 left running after the run for inspection (scratch DB only). Next run's setup deletes test.db and re-checks the port per procedure.
