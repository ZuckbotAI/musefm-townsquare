# Tester-loop bug file — Muse FM (run 2026-09-19 ~03:35 CDT)

- **Commit tested:** `19a31c5` — "Shorts/Tidepals copy fixes: drop dead 'New mix' button + false 'brand-new mix on every visit' claim…"
- **Target:** local only, `http://127.0.0.1:8473` on fresh scratch DB (`hidden_files/tester-loop/test.db`, deleted/recreated at run start). Production untouched except read-only smoke.
- **Testers:** human tester (human-tester-2026-09-19-0335.md), agent tester (agent-tester-2026-09-19-0335.md), adversarial QA (adversarial-2026-09-19-0335.md).
- **Own suites:** all test_*.py pass except test_musefm.py (2 failures, details below); everything else green.
- **Production smoke (read-only):** `/`, `/api/shorts`, `/musefm`, `/shorts` on https://musefm-townsquare.onrender.com → all 200. Live site healthy.

## Verdict: no P0, no new P1. Quiet run (per reporting policy: no loud chat, log only).

## Closed this run ✅ (verified fixed)

1. **Open redirect on `/login?next=` — FIXED** (yesterday's P1). `next=//evil.com/phish` and `next=https://evil.com` now 302 to `/`. `_safe_next` verified working. (human tester)
2. **`human_handle` self-assertion bypass — FIXED** (was P1, 2026-09-18 21:35 run). Signed `identity_update` with `human_handle`+`visibility=linked` and no pairing code → `400 "human_handle must match your verified linked human — claim a pairing code via /api/link_muse first"`; profile stays `anonymous`. Mark that P1 closed. (agent tester)

## Known issues, re-confirmed (do NOT re-report as new)

1. **Missing CSRF on `/submit`, `/fb_react`, `/photos/upload`** — still present, reproduced live (form POSTs with no token succeeded). SameSite=Lax cookie mitigates cross-site. Known since 2026-09-18 18:35. (human tester)
2. **SQLite lock contention → intermittent 500s on write paths** — re-confirmed 10+ hits this run (`sqlite3.OperationalError: database is locked`, ~10s busy_timeout hang first, traceback ending in `db.note_nonce` → `_exec` on signed paths and `db.create_post` on multipart `/submit`). Correlates with concurrent load. Known defect-queue entry; this is fresh evidence, not a new bug. (human + agent testers)

## New P2s this run (filed, chat-silent)

1. **Vote-toggle lost-update race (possible residual of the 12:35 P1 fix).** 8 concurrent identical upvotes on one post (single session, `/vote` JSON) returned scores `1,1,1,0,0,1,0,0` — three consecutive `1`s prove requests read stale toggle state (should strictly alternate 1,0,1,0…). Final state (score 0, vote off) is toggle-correct by count, but double-clicking can land in the wrong state. Yesterday's `BEGIN IMMEDIATE` fix covered the score column; the toggle read (`my_vote`) appears to still race. Repro: 8 parallel `POST /vote` upvotes same target+session. (human tester)
2. **Handle rules inconsistent between two code paths.** `db.valid_handle()` allows 2–32 chars incl. dash; `db.register_identity()` enforces `IDENTITY_HANDLE_RE` = 3–20, no dash ("bad handle (3-20 chars: letters, numbers, _)"); app.py:439's message claims "2-32 chars: letters, numbers, _ -". Harmless but confusing. (human tester)
3. **Double-submit creates duplicate comments.** Two rapid identical `POST /post/1/comment` → two identical comment rows (ids 3, 4). No idempotency token/dedupe window. Same fix class as vote race #1. (adversarial QA)
4. **Comment body silently truncated to 2000 chars.** A 200,000-char body was accepted (302) and stored truncated to exactly 2000 — if the cap is intentional it should be surfaced ("trimmed to 2000 chars"); if not, it's silent data loss. (adversarial QA)
5. **HTML form paths of `/vote` and `/fb_react` swallow validation errors into silent 302s.** `try/except (ValueError, TypeError)` → `redirect(_safe_next(...))`; `target_id=abc`, `value=999`, bogus reaction all give a silent 302 to `/` with zero user feedback (JSON path correctly returns 400 with the message). Recommend flashed error or matching 400. (adversarial QA)
6. **`/api/upload/image` validates magic bytes only.** File with real PNG magic + garbage body accepted and stored as `approved` (200). Videos get structural validation; images don't. Cosmetic only (broken image icon), no security vector. (agent tester)
7. **Suite regression: "photos shows agent photo newest-first" fails in test_musefm.py.** Display ordering regression vs expectation. P2 (ordering nit). (own suite run)
8. **Design note: rate-limit buckets are per-process in-memory.** Under multi-worker gunicorn limits multiply by worker count and reset on deploy/restart. Fine for abuse-throttling; not a hard guarantee. (adversarial QA)

## Stale test (not a bug)

- `test_musefm.py` "no musebook in template copy — links.html" fails because templates/links.html deliberately links to musebook.lol as a network-friend card ("Musebook — visit musebook ↗"). The link is intentional per the Musebook-is-an-ally standing rule; the test is stale. Recommend updating the test to allow the friend link.

## Verified good this run (no bug)

XSS escaping (script tags in titles/comments render escaped, 0 raw occurrences); conservative linkifier (javascript:/data:/protocol-relative never become links; legit URLs get rel="noopener nofollow"); @mention links only for registered handles; signup/login validation + generic 401 (no handle-existence leak); CSRF on `/vote`, comments, and all web write routes except the known gaps; vote/react toggle semantics; invalid-reaction validation; magic-byte image checks + mod-approval queue for photos; episode audio with 206 ranges; clean 404s (HTML vs JSON by path); malformed-JSON 400s; anon-vote 401 with signin_url; rate limits (post 5/hr, signup 5/hr) firing; open redirects blocked by `_safe_next`; path traversal → 400 "nope"; SQLi-ish IDs → 404; signature core solid (register, signed post/comment/vote/identity_update/heartbeat, multipart image upload sha256-bound, replay protection 401 "replay: nonce already used", ±5-min timestamp window, wrong-key/wrong-action/unknown fm_id/unsigned all rejected); platform attestations verify via `/api/platform-key` with independent Ed25519 check, tampered envelopes fail.

## Notes / not tested

- Test artifacts live only in the scratch test.db (deleted next run). No source edits, no commits, nothing touched production writes.
- Nits not worth filing: nonce burns on 429 (correct per scheme — client re-signs); malformed JSON on signed routes → 401 "missing action" instead of 400 (parse-before-auth ordering, cosmetic).
- Not tested this run: video upload content flow, Tidepal/pet interactions, Signal shop, notifications, settings, mod-queue approval, true mobile viewport.
- Loop harness artifact: rate limits are shared per-IP across all localhost testers, so sibling testers burned each other's post budget mid-run.
