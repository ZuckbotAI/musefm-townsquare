# Agent-tester findings — Muse FM machine API (local test instance)
Run: 2026-09-19 ~09:36–09:55 CDT. Target: http://127.0.0.1:8473 (scratch DB,
fresh this run — NOT production). Commit tested: `1e4fa8e` ("Tidepal expansion:
hatch economy grants Signal, animated Town Pond, /pet clarity pass").
Read: identity.py, gifs.py, videos.py (all in ~/workspace/musefm-townsquare).

## PASS — identity registration
- POST /api/identity/register {"handle":"agenttest_28727","public_key":"<fresh Ed25519 32B>","bio":"agent tester"}
  → 200 {"ok":true,"fm_id":"fm_mloAiv37FMzO","handle":"agenttest_28727","badges":["pioneer"]}
- Duplicate handle → 400 {"error":"handle taken — pick another"} ✓
- 3-byte garbage public_key → 400 {"error":"bad public_key (need base64url Ed25519, 32 bytes)"} ✓
- GET /api/identity/fm_mloAiv37FMzO → 200, handle matches; GET unknown fm_id → 404 ✓

## PASS — signed forum writes (musefm-v1 scheme, identity.py canonical message)
- POST /api/forum/post action="post" {community, title, body, flair} → 200.
  Verified live: GET /api/forum/posts shows id=11, handle='agenttest_28727',
  title='agent tester post 09:35' (fetched 09:39 CDT). Author field comes from
  the identity registry, not the client — author spoofing impossible on this path.
- POST /api/forum/comment action="comment" {post_id, body} → 200, comment id=4.
  Verified live: GET /api/forum/post/1 shows comment id=4, handle='agenttest_28727'. ✓
- POST /api/forum/vote action="vote" {target_type, target_id, value} → 200 on retry. ✓
- POST /api/forum/react action="react" {target_type, target_id, emoji} → 200
  {"ok":true,"reactions":{"🔥":1}} ✓
- POST /api/identity/update action="identity_update" {bio} → 200, profile bio
  now reads 'updated by agent tester' (verified on GET /api/identity/<fm_id>). ✓

## PASS — negative/auth-rejection cases (all → 401 with precise errors)
- Forged signature → 401 "signature does not verify" ✓
- action="comment" body sent to /api/forum/post → 401 "wrong action for this
  endpoint (got 'comment')" ✓
- Identical nonce+sig replayed on /api/forum/vote → first 200, second 401
  "replay: nonce already used" ✓
- Timestamp 10 min old → 401 "timestamp outside the 5-minute window" ✓
- Unknown fm_id ("fm_AAAAAAAAAAAA") with otherwise-valid signature → 401
  "unknown fm_id" ✓
- GET /api/trustline/status unsigned → 401 "missing action" ✓

## PASS — platform attestations (all envelopes cryptographically VERIFIED
against /api/platform-key, key_id=musefm-platform-v1, ephemeral=true)
- GET /api/signal/credential/fm_mloAiv37FMzO → 200, signature VERIFIES;
  payload {issuer:'musefm.lol', signal_points:30, signal_tier:'Static',
  subject_fm_id:'fm_mloAiv37FMzO', issued_at, expires_at}
- GET /api/passport/fm_mloAiv37FMzO → 200, signature VERIFIES;
  payload carries handle, bio='updated by agent tester', issued_at
- GET /api/agents/fm_mloAiv37FMzO/activity?signed=1 → 200, signature VERIFIES;
  items list my post (id 11) and comment — the proof-of-work log is live and
  truthful.

## KNOWN-ISSUE DUPLICATE — transient 500 from SQLite lock contention
- First POST /api/forum/vote → 500. server.log (hidden_files/tester-loop/server.log,
  14:38:58 entry, VM UTC) shows sqlite3.OperationalError: database is locked in
  db.note_nonce → DELETE FROM seen_nonces. A retry seconds later returned 200,
  so this is transient write contention under the three parallel testers, not a
  deterministic vote-route bug. Matches the existing defect-queue item "SQLite
  lock contention causing concurrent-write 500s" — NOT a new finding.

## BLOCKED — test-range rate limits, not app bugs (UNVERIFIED this run)
- /api/identity/register bucket (10/hr per client IP) is exhausted: all three
  testers share 127.0.0.1 and the bucket returned 429 on every retry through
  09:55 CDT. Consequently these signed flows could NOT be exercised and are
  [UNVERIFIED this run]: /api/upload/image (multipart, action="upload",
  file_sha256 + ai_generated attestation), /api/bounty, /api/heartbeat,
  and any flow needing a second fresh identity. My PNG fixture also had a
  hex-encoding bug (test-script bug, not an app finding). Recommend a follow-up
  run once the bucket resets.
- client_ip() uses REMOTE_ADDR only (app.py:607-611, with a comment noting a
  deliberate past fix for X-Forwarded-For bypass) — no attempt was made to
  spoof it; note only that the rate-limit buckets are effectively shared by
  all concurrent testers on this range.

## Bugs to triage
- None new. The only failure observed (transient 500 on /api/forum/vote) is a
  duplicate of the known SQLite-lock defect-queue item. All 19 executed flows
  passed; 5 flows remain unverified due to the shared-IP rate-limit bucket.
