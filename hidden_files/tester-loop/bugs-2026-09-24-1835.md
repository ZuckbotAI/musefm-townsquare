# Tester loop triage — 2026-09-24 18:35 CDT

- Commit at run start: `6661f91` — "Pet backend cutover: canonical pet store + village key unification"
- Local instance: http://127.0.0.1:8473 (port free at boot), scratch DB `hidden_files/tester-loop/test.db` (deleted at run start; hermetic)
- Real local DB: untouched. Production: READ-ONLY smoke — `/`, `/api/shorts`, `/musefm`, `/shorts` all 200. Live site healthy.
- Three subagent reports: human tester, agent API tester, adversarial QA (all evidence-verified by coordinator before filing — see below).
- Proving test added (tests only, no source changes): `test_testerloop_empty_sig_2026_09_24.py` (1 fails = the new empty-signature P2, 2 controls pass).

**Verdict: 0 new P0, 0 new P1, 3 new P2s. P2-only run → staying silent per reporting policy.**

---

## NEW P2s (all verified/corroborated by coordinator, not just worker claims)

### 1. P2 — Double-submit creates two identical posts (no dedupe on /submit)
- [human tester; coordinator-corroborated in the scratch DB — live repro was blocked by the burned human_login 10/hr budget (429s), so verification is DB-direct]
- Repro: logged in, POST /submit twice with identical title/community/body (valid CSRF each time) → 302 to /c/lobby/post/17, then 302 to /c/lobby/post/18.
- DB evidence (scratch DB, read-only query): posts 17 and 18 both titled "dedupe check post", body "same body twice", handle MischiefTester9 — two distinct rows from the double-submit.
- Expected: second rapid duplicate either rejected or idempotently collapsed (accidental double-click is the classic trigger).
- Sibling: 15:35 P2 #3 covers the signed-API comment-burst twin; this is the web-form post twin — new facet, new filing.

### 2. P2 — Empty-string `signature` reports "signature does not verify" instead of "missing signature"
- [agent tester; coordinator-verified live: `POST /api/forum/react` with `"signature": ""` on an otherwise-valid signed body → `401 {"error":"musefm-v1 auth failed: signature does not verify"}`; absent key → "bad signature encoding"; both verified on `6661f91`]
- Expected: 401 `musefm-v1 auth failed: missing signature` — there is no signature present to verify, so "does not verify" is inaccurate; matches the timestamp/nonce "missing X" guard pattern.
- Root cause: `verify_signed_body` (identity.py:199) passes `data.get("signature")` into `b64u_decode`; `""` decodes to `b""` without raising (length cap is on the string, not the bytes), so it falls through to Ed25519 verify and is rejected as a bad signature. Third distinct input in the missing-vs-malformed family (absent → "bad signature encoding" filed 15:35; empty → "does not verify" here; garbage → "bad signature encoding").
- Proving test: `test_testerloop_empty_sig_2026_09_24.py` — `test_empty_signature_reports_missing_not_does_not_verify` FAILS as designed; 2 controls (absent → "bad signature encoding" pinned, valid body → 200) pass.

### 3. P2 — Reaction allowlist rejects skin-tone modifiers (👍🏽 → 400)
- [adversarial QA; coordinator-verified live: signed `POST /api/forum/react` with `emoji:"👍🏽"` → 400 `{"error":"emoji must be one of: 🔥 ❤️ 👍 😂 🎙️ 👏 💡 🚀"}`; base 👍 → 200]
- Expected vs actual: phone keyboards emit skin-toned emoji by default, so a human tapping 👍 from their phone gets a 400. The 400 message is clear (lists the allowed set), so it's UX friction, not breakage.
- Caveat: the allowlist is explicitly enumerated, so this may be intended design (keeps counting/dedup simple). Flagged for Anthony's review rather than asserted as wrong. No proving test added (judgment call, not a clear bug).

---

## FIXED since 15:35 (strike from dedupe list)

- **15:35 P1 `GET /api/forum/posts?q=<60000 chars>` → 500 — FIXED in this tree.** Coordinator-verified live: 60k chars → **400** `{"error":"search query too long (max 2000 characters)"}`; threshold edge 2000 → 200, 2001 → 400. Web-UI twin `/c/lobby?q=<60k>` → 400 too. The sprint added the `SEARCH_Q_MAX` guard (app.py). The 15:35 proving test (`test_testerloop_search_q_500_2026_09_24.py`) now PASSES 2/2 — the fix is confirmed by the loop's own failing test.
- **15:35 P2 #1 `POST /api/memory` 201 → 200 — still fixed** (agent tester verified live: signed memory_write → 200).
- **15:35 P2 #2 upload `file_sha256` mismatch 401 → 400 — still fixed** (agent tester verified live on the gif path).
- **React path now validates BEFORE the rate limit** (agent tester, from diff vs 961f41f): target_type allowlist, emoji allowlist, and target-exists check all run before `check_limit("react", 120)` — Anthony's sprint applied the same fix pattern the 15:35 run filed for gif_url. Suggest the same reorder for the forum-post attachment validators when the tree is reviewed.

## Deduped (already on file, unchanged)

- All previously-filed P2s from the last 48h (gif_url rate-budget burn, missing-signature "bad signature encoding", comment-burst, text/plain-as-JSON, login rate-limit aggressiveness, `Server: Werkzeug` banner, omitted-vote-value default, missing-nonce message, silent limit clamping, channel-vs-community param, react-302, share-dialog copy, session-not-cleared-on-login, malformed-JSON-no-CT 401, vote/react 400-vs-404, signup-without-email stale cascade).
- All by-design-failing proving tests (`test_testerloop_sig_missing_2026_09_24` for the still-open absent-signature P2, plus the 15:35-run list) — still failing as designed.

## Repo suites (coordinator runs)

- **Full pytest** (9 script-style collection blockers + test_musefm.py excluded with --ignore, same as 15:35): **87 passed, 13 failed** — all 13 match the 15:35 baseline: test_row.py × 7 (stale: legacy 2D row.html retired, /row serves village.html 3D canvas), test_testerloop_1835_fixes_2026_09_19 × 3 + test_testerloop_flag_messages_2026_09_19 × 2 (stale: signup-without-email cascade after commit 454ba91 made email mandatory), test_testerloop_sig_missing_2026_09_24 × 1 (by-design failing for the still-open 15:35 P2). **No new suite failures.**
- **test_sidebar.py** (script-style, run via `python3 test_sidebar.py`): **108 passed, 19 failed** — all 19 explained, no new app bugs: 10× "all section links" failures are stale test expectations (Links-hub consolidation `ede27ca` folded /arena and /pro into /links; /pro route still serves a 301 to /playbook — deliberate), signup-400 → login-401 → account-block cascade is the known email-mandatory staleness, "fm shorts highlights FM Shorts" is the deliberate dedup commit `7caa312` ("drop duplicate FM Shorts link").
- **Observation (not filed):** on `/musefm/shorts` (200) NO sidebar link carries the active class — the FM Shorts dedup removed the only link that highlighted there. Minor wayfinding gap; flagged for Anthony's review rather than filed (the drop was deliberate).

## Observations (not bugs)

- **Test server died mid-run twice this run.** Once per the human tester (~23:47 UTC, no traceback, server.log just ends — second consecutive run with a mid-run death under parallel tester bursts) and once from the runtime service restart (~00:00 UTC). Coordinator rebooted both times → 200 each time. server.log shows 3 boot cycles this run. The werkzeug dev server looks unstable under parallel burst traffic — worth a look, not a product bug.
- **human_login 10/hr budget burned by parallel testers** — coordinator's double-submit live repro hit 429s; verification fell back to DB-direct evidence (posts 17/18). Environmental, scratch-only.
- **Session secret is per-boot** (human tester): after a server restart, session cookies invalidate and /vote silently 302s to /login (the only tell is the Location header). Environmental; no phantom vote bug.
- Scratch DB contains seeded demo content from earlier runs — untouched by testers except their own artifacts.
- Tree is dirty (Anthony's ongoing build-sprint work) — uncommitted, as required; nothing committed or branch-switched this run.

## Caveats

- Bug reports live in `hidden_files/tester-loop/` (uncommitted) — the branch is Anthony's to review and change.
- The new `test_testerloop_empty_sig_2026_09_24.py` proving test joins the by-design-failing family until the fix lands.
- Test instance on :8473 killed at run end to keep the port free for the next run.
