# Adversarial QA — 2026-09-22 03:35 loop (local test instance :8473)

Commit tested: (unable to read — server died before `git log` was captured; checkout is
~/workspace/musefm-townsquare, scratch DB hidden_files/tester-loop/test.db)

## HEADLINE: test server died mid-run (operational P0 for the loop)

- The local instance on 127.0.0.1:8473 served normally from 08:50 through 08:52:46 UTC
  (log line `GET /forum -> 301`), then vanished: port closed, no `app.py` process,
  no traceback in server.log, no OOM-kill/segfault in `dmesg`, no journal entries.
- My probes immediately before/after: none issued a crashing request (last of mine
  were `GET /api/forum/post/8`, `GET /forum`, then a 12-request sequential burst at
  `POST /api/identity/register` that never connected — 000, server already dead).
  Another tester persona was active concurrently (`POST /login` 429s,
  `POST /post/None/comment` 404s in the log), so the cause is ambiguous.
- Impact: ~20 probes completed and are reported below; NOT completed due to death:
  identity-register 429+Retry-After check, web `/vote` sessionless check,
  `POST /api/forum/post/<id>` wrong-method check, and any signed-path (musefm-v1)
  duplicate-key probes beyond the agent-key path.
- Recommendation: the parent loop should re-check the instance between personas;
  this run's partial results are still valid (all requests were against the live
  instance and have request/response evidence).

## New findings

None that are new bugs. One new confirmation + one new operational item:

### P2 (dup, confirmed new variant) — absolute API URLs reflect hostile Host
- Already reported in adversarial-2026-09-22-0035.md for `/api/episodes`.
- This run: `curl -H 'Host: evil.com' /api/forum/post/8` → `"url":
  "http://evil.com/c/lobby/post/8"` in the JSON `post` object (built via
  `url_for(..., _external=True)`). Redirects use relative `Location:` (safe),
  `X-Forwarded-Host` is not trusted over Host, but the raw Host is.
- Expected: pin canonical host for generated absolute URLs. Same P2 as before.

## Passes (verified with request/response evidence)

1. **404 handling:** `/nonexistentxyz`, `/c/bogus/post/1`, `/s/abc`, `/s/8`,
   `/m/doesnotexist`, `/api/identity/fm_doesnotexist`, `/passport/doesnotexist`,
   `/musefm/photos/999999`, `/photo-file/999999`, `/api/forum/post/999999` →
   all 404 with a branded HTML 404 page (title "404 — MuseFM"), consistent.
2. **No `/s/<id>` share route exists** (verified via source grep + 404s); the
   "share_url" concept lives only in API JSON fields. Nothing to break there.
3. **ID tampering:** `/c/lobby/post/99999999999999999999999` (over 2^63-1),
   `/c/lobby/post/-1`, `/c/lobby/post/abc`, `/c/lobby/post/8.5` → 404, no 500
   (sqlite_int converter + 404s behave). `%38` (encoded "8") → 200, correct.
4. **Wrong methods:** GET/PUT/DELETE on POST-only `/api/forum/react`,
   `/api/forum/post`, `/vote` → 405. Good.
5. **Unauthenticated writes:** POST (agent-key/signed-required) paths all 401:
   `/api/admin/identity/rotate-key`, `/api/forum/{react,post,comment,vote,fb_react}`,
   `/api/memory`, `/api/memory/wipe`, `/api/memory/1/{edit,delete}`,
   `/api/identity/update`, `/api/collab`, `/api/workroom/create`,
   `/api/swarm/projects`, `/api/rewards/invite-code`, `/api/agent-memory/store`,
   `/api/row/journal`; `/row/avatar` POST → 403 (web-session path).
   Public GETs (`/api/collab`, `/api/swarm/projects`) → 200 as designed.
   Mod/admin/settings paths correctly reject — the 401/403/302 set is the pass.
6. **Malformed JSON:** garbage body on signed route → 400 "Malformed JSON body";
   JSON array → 400 "JSON body must be an object"; top-level and nested
   duplicate keys (with valid handle) → 400 "duplicate key 'x' in JSON body"
   (dedupes at every nesting level — passes); wrong/no Content-Type → 401
   (auth checked before body parse, consistent); empty JSON body → 401.
7. **Oversized body:** 36MB JSON POST → 413 (MAX_CONTENT_LENGTH 34MB respected).
8. **Path traversal:** `/audio/../app.py`, `/audio/%2e%2e/app.py`,
   `/episode-video/..%2fapp.py`, `/audio/..%5capp.py`,
   `/episode-video/../../../../etc/passwd` → 400; `/static/../app.py` → 404.
   No file leakage. Null-byte fnames (`/episode-video/x%00y`) → 404.
9. **Rapid taps (own post #8, handle advprobe_zz, agent-key auth):**
   - 20 sequential emoji reacts (🔥) → all 200, final `{"🔥":1}` — idempotent,
     no double-count.
   - 20 sequential fb_react "like" → all 200, toggles add/remove, final total 0.
   - 20 sequential votes `value:1` → all 200, toggles 1↔0 (same-value toggle by
     design), no score inflation. `value:-1` likewise; `value` ∈ {5,0,1.5,"2"} →
     400 "value must be 1 or -1" / "bad value: must be an integer".
   - Note (not a bug, documented before): `direction` is not the field — the
     endpoint reads `value`; unknown fields are silently ignored (standard).
10. **Reaction-picker edges:** invalid emoji / empty / missing emoji →
    400 "emoji must be one of: …"; invalid target_type → 400; unknown target_id /
    negative id → 400 "unknown target"; float/bool target_id → 400;
    string "8" coerced (200 — accepted by design); fb invalid reaction → 400;
    "LIKE" uppercased → normalized and accepted (200); empty reaction → 400;
    comment target 999999 → 400 "unknown target". No 500s anywhere.
11. **Host header:** evil.com/empty Host accepted with 200 (same known P2 as
    above; redirects relative so no open-redirect).
12. **`/api/shorts?limit=abc|-5|99999999`** → 200 (tolerated, no crash).
    Junk community on post create → 400 "unknown community".

## Server-log scan
- Reviewed `hidden_files/tester-loop/server.log` in full: zero 500s, zero
  tracebacks for the entire run. The process ended silently (see headline).

## What I did not touch
- Production (no requests to musefm-townsquare.onrender.com — out of scope for
  this probe; parent's smoke step covers it).
- No other persona's data; only post #8 (my own) and reactions/votes on it.
- No app source edits; no commits; no branch changes.
