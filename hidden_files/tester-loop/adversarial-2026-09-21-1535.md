Probing complete. Compiling the final report.

---

## ADVERSARIAL QA REPORT — musefm-tester-loop run (2026-09-21 ~15:37 CDT)

**Commit tested:** `89b6522` ("Fix tester-loop P2 (2026-09-21 12:35 run): /login validate-first")
**Instance:** http://127.0.0.1:8473, scratch DB. Production untouched. No tracebacks in `server.log` during the run (grep count: 0).

### Verdict: no P0, no P1. Two P2 nits. Everything else held.

### P2-1 — Validation failures burn the react/vote rate budget (comment/behavior mismatch)
Code comments on `api_react`/`api_vote` claim *"bad target_id/emoji 400 without burning the shared per-IP budget"*, but only non-integer target_id (via `_int_field`) avoids the budget. Requests with a **valid-shaped but invalid emoji** or unknown target still record a hit in `check_limit("react", 120)` before `db.react` raises.
Repro (signed request, any valid identity; register first via `POST /api/identity/register`):
```
for i in $(seq 1 130); do curl -s -o /dev/null -w "%{http_code}\n" -X POST \
  http://127.0.0.1:8473/api/forum/react \
  -H 'Content-Type: application/json' \
  -d '<signed body action=react target_id=13 emoji="nope_not_emoji">'; done
# observed: 120× "400 emoji must be one of: ..." then 10× "429 rate limit hit — slow down, friend"
```
Blast radius is the attacker's own IP (buckets are per-`REMOTE_ADDR`; X-Forwarded-For spoofing correctly ignored), so this is self-DoS only — hence P2. Fix: move the emoji-whitelist + target-existence checks before `check_limit`, or correct the comment.

### P2-2 — og:url / og:image hardcode the production domain
Every page, including on this local instance, emits `og:url https://musefm.lol/...` and `og:image https://musefm.lol/static/img/og-image.png`. Verified on `GET /c/lobby/post/13` (my own post) and the 404 page. Share links generated from any non-prod deployment advertise prod URLs.

### Caveat for triage (not a bug)
Rate buckets are per-IP and all tester loops run from 127.0.0.1: my `POST /api/forum/post` 429'd with `Retry-After: 3271` because sibling loops had burned the shared 5/hr `post` bucket. Test runs sharing a host can false-positive on 429s. I worked around it with a second loopback source IP (127.0.0.2–127.0.0.5) for my own probes only.

### Verified solid (do not file)
- **404s:** clean branded 404 page, no tracebacks — garbage paths, deep paths, bad id types (`/post/abc`), negative ids, nonexistent ids.
- **Traversal:** `/uploads/../../app.py`, `%2e%2e` variants, `/uploads//etc/passwd`, `/photo-file/../../../../etc/passwd`, `/video/../../../../etc/passwd` → all 404.
- **Admin/debug:** `/admin`, `/debug`, `/console` → 404. `POST /api/admin/identity/rotate-key` unauth → 401. `POST /api/challenges/settle` unauth → 401. Agent-memory endpoints → 401.
- **Session gating:** unauth `POST /settings/unlink`, `/pet/feed`, `/workroom/create`, `GET /settings`, `/notifications`, `/agent/profile` → 302 to `/login?next=...` (my first 200s were the HTTP client's redirect-follow artifact — verified with no-follow curl).
- **Signed API auth:** tampered signature → 401 "signature does not verify"; 10-min-old timestamp → 401 "timestamp outside the 5-minute window"; wrong action for endpoint → 401; identical body replayed → 401 "replay: nonce already used". Impersonation attempt (B's key + A's fm_id) → 401.
- **Malformed input:** broken JSON → 400 "Malformed JSON body"; `text/plain` body → 400; JSON array body → 400 "JSON body must be an object"; `GET` on POST-only routes → 405.
- **Reaction picker:** legacy `/api/forum/react` enforces the 8-emoji whitelist (`emoji` field; invalid/unicode/20k-char/empty/non-string/null all → 400); fb_react enforces the 6-name set (case/space normalized, bogus → 400); repeat same-emoji react is idempotent (INSERT OR IGNORE); one-per-identity fb toggle works.
- **Votes:** `value` restricted to ±1 (0, 2, -2, 999999999, strings, floats, bools → 400); same-value repeat toggles off (intended, per code comment); atomic via BEGIN IMMEDIATE; null defaults to +1 (documented `_int_field` default).
- **Flag:** reasons whitelisted (spam/harassment/nsfw/misinfo/other); 5000-char reason → clean 400.
- **Registration:** dup handle → 400 "handle taken"; SQLi handle / 200-char handle → 400; bad/short pubkey → 400.
- **XSS:** `<script>alert("xss")</script><img src=x onerror=alert(2)>` as post title → entity-escaped in HTML body and `<title>`; no raw script/attribute injection.
- **SQLi:** `?community=' OR 1=1--` → `{"posts":[]}`; `?q=' OR '1'='1` → `{"posts":[]}` — parameterized.
- **Open redirects:** `_safe_next` blocks absolute URLs, `//evil.com`, backslash tricks, control chars; `?next=https://evil.com` and `?next=//evil.com/x` on /login and /signup → no redirect issued.
- **CRLF in path** (`/%0d%0aX-Injected:%20yes`) → 404, no header injection.
- **Share links:** `/c/lobby/post/13` renders proper og:title/og:description ("advqa4 probe — MuseFM", "⌂ Lobby · 💬 1 comment · on Muse FM"); malformed share ids (`/watch/abc`, `/post/999999999`, `/musefm/photos/abc`, `/passport/notarealid`, `/api/episodes/no-such-slug[/comments]`) → clean 404s.
- **No data leaks:** post detail, posts list, and public identity responses contain no keys, emails, or IPs (identity exposes signal/spendable/spent — public by design).
- **claim-human** generates a fresh keypair server-side (no takeover vector); `avatar_url` must be http(s).
- **Games:** `/api/games/tide-toss/play`, `/api/games/feed-frenzy/click` unauth → 401.

### Methodology correction (for the triage record)
My first react/vote/flag probes used wrong field names (`reaction` instead of `emoji` on `/api/forum/react`; `direction` instead of `value` on `/api/forum/vote`; action `flag` instead of `flag_post` on `/api/forum/flag`) — those 400/401s were my client errors, not server bugs. All were re-run with correct fields; results above reflect the corrected runs.

All test artifacts (posts 13/14, comments, identities advqa1–advqaB2) live only in the scratch test DB and will be wiped by the next run's hermetic reset.
