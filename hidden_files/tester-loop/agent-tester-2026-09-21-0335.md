# Agent tester report — 2026-09-21 ~03:35 CDT

Target: local scratch instance http://127.0.0.1:8473 (disposable test.db), commit 5a38b43.
Persona: AI agent exercising Muse FM's musefm-v1 signed machine API.
Scope: /api/identity/register, /api/identity/<fm_id>, /api/identity/update, /api/forum/post,
/api/forum/comment, /api/forum/vote, /api/upload/image, /api/upload/gif, /api/upload/video,
/api/platform-key, /api/signal/credential/<fm_id>, /api/passport/<fm_id>.

## Verdict: NO FAILURES. Every signed-flow check passed. No P0, no P1, no P2.

| Check | Result |
|---|---|
| Register identity (unsigned) | 200, fm_id fm_zxNbggQVQZP5 |
| Signed post (action=post) | 200, id 8 |
| Signed comment | 200, id 3 |
| Signed vote (post) | 200, score 1 |
| Replay same signed body twice | 1st 200, 2nd 401 `replay: nonce already used` |
| Tampered payload (title swapped after signing) | 401 `signature does not verify` |
| Expired timestamp (6 min old) | 401 `timestamp outside the 5-minute window` |
| Missing timestamp | 401 `bad timestamp` |
| Wrong-key signature (signed by other key for same fm_id) | 401 `signature does not verify` |
| Unknown fm_id | 401 `unknown fm_id` |
| Wrong action for endpoint (comment body on /api/forum/post) | 401 `wrong action for this endpoint (got 'comment')` |
| Unsigned post | 401 `musefm-v1 auth failed: missing action` |
| Malformed JSON body | 400 `Malformed JSON body` |
| Signed image upload (4x4 PNG, sha256 matches, ai_generated=true) | 200, /img/1, status approved |
| Post with attached image_url + image_ai | 200, id 10 |
| Upload with mismatched file_sha256 | 401 `file_sha256 does not match the uploaded bytes` |
| Non-image bytes as image | 400 `not an image -- PNG, JPEG, or WebP required` |
| Upload with no body at all | 401 `musefm-v1 auth failed: missing action` |
| GIF upload (43-byte real GIF89a) | 200, /gif/1 |
| Video upload (ftyp+moov+5k zeros, valid structure) | 200, /video/1, status approved, duration_secs=30 |
| Truncated video (ftyp + zeros, no moov) | 400 `corrupt or truncated video file -- please re-upload` |
| Overlong title (500 chars) | 400 `title too long (max 200 characters)` |
| Overlong body (200k chars) | 400 `body too long (max 10000 characters)` |
| Duplicate handle | 400 `handle taken — pick another` |
| Case-variant handle (AGENTTESTER2 vs agenttester2) | 400 `handle taken — pick another` |
| Bad public_key (8 bytes) | 400 `bad public_key (need base64url Ed25519, 32 bytes)` |
| Signed identity_update | 200; profile GET reflects new bio + avatar_url |
| /api/platform-key | 200, key musefm-platform-v1 |
| /api/signal/credential/<fm_id> | 200; envelope verifies locally against platform key (json canonical, Ed25519) |
| /api/passport/<fm_id> | 200; envelope verifies locally |
| Tampered platform envelope (fm_id swapped in payload) | local verification FAILS as expected — attestations are tamper-evident |
| Double vote same direction | 200, score 0 (toggle-off — expected behavior, not a bug) |

## Observations (non-issues)
- `/api/upload/image` with an entirely empty body returns 401 "missing action" rather than 400.
  Consistent with the auth-first design of all signed endpoints; the human-readable error
  points at the fix. Not filed as a bug.
- Video upload approved a 5KB ftyp+moov+zeros stub as structurally valid. The structural
  check only requires the moov box, not playable media data. Acceptable for the threat
  model (broken files rejected), but a purely synthetic moov box passes — moderation layer
  concern, not a verification hole.

## Scripts
Test scripts are disposable scratch at /tmp/agent_tester.py and /tmp/agent_tester2.py
(fresh handle per run: agenttester, agenttester2/3). Raw full output captured in run logs.
No source files were modified (tests-only, per fix policy).
