# HUMAN TESTER REPORT — 2026-09-21 06:35 run (commit 62e646b, port 8473)

Full human journey: signup (handle `mischief_marauder`), login, logout, posting,
commenting (top-level + nested via parent_id), episode comments, voting,
FB reactions, photo upload (real PNG + fake-image rejects), episode audio
playback (/audio/*.mp3 200, 2.4MB streamed), /shorts feed, mobile markup
(viewport meta + 19 @media queries in style.css).

## P0 — none. Every XSS vector neutralized:
<script> in titles/comments/episode comments → escaped; <img onerror> and
javascript: links in body → fully escaped (**bold** shows literally too).
Display names/handles strictly validated: <script>-handle, emoji-handle →
400; SQL-injection-style handle → 400. gif_url https-only + Giphy/Tenor
whitelist + .gif suffix. Fake images (text .gif, EXE .png) → 400 "not a
recognized image"; real PNG → 302 moderation-pending queue. CSRF on all form
routes (stale/missing → 403); anon vote/react → 401; GET /logout → 405
POST-only; session killed after POST logout.

## P1 — none.
Signup validation (dup handle, short password, mismatch), nested comment
tree, vote range (value=5 → 400), reaction allow-list matches picker
(like/love/haha/wow/sad/angry), friendly 429s, 404s for bad
communities/posts/profiles/routes.

## P2 — nits
1. Display-name charset rejects apostrophes — "Bob O'Brien" signup → 400.
   Secure but hostile to real human names.
2. No cap on post body length — 10,000-char body stored and rendered in full.
   Title capped at 200; body uncapped — page-weight/abuse vector.
3. Post bodies render as escaped plaintext — **bold** shows literally, no
   formatting at all. If plaintext is by design, fine; if formatting was
   expected, UX is confusing.
4. Repeated identical votes toggle the vote off (reddit-style): 10 parallel
   value=1 votes alternated 1→null→1→null, ending clean at score 0 — state
   consistent, no race corruption. But a user clicking an already-set upvote
   unwittingly removes it.

(Note: one 403 on /episodes/<slug>/comment and /submit was the tester's own
stale CSRF token from a cookie-less fetch, not an app bug.)
