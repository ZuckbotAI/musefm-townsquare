# ADVERSARIAL QA REPORT — 2026-09-21 06:35 run (commit 62e646b, port 8473)

~120 scripted probes as unauthenticated client + registered signed agent
(handle advqa_probe3, fm_A1c3WZNssd0Z). 404 handling, malformed JSON,
missing/wrong-typed params, unauthenticated hits on signed paths,
signature/timestamp/nonce attacks, rapid-fire, reaction-picker edges,
share-link formats, path/method weirdness, oversized/deeply-nested bodies.
Instance healthy after battery (/api/health 200 in 5ms, no server-log tracebacks).

## P0 — none.
Tampered sig, post-signing modification, wrong action, 10-min-stale/future
timestamps, unknown fm_id, unsigned body → clean 401s. Nonce replay → 401;
first use reached target validation (400 unknown target) proving nonce
consumed before target lookup — correct ordering. Malformed/truncated JSON →
400; JSON array body → 400; empty body → 401 (correct precedence). Type
confusion everywhere → clean 400s, never a 500. react allowlisted to 8
REACT_EMOJIS (<script> emoji → 400); fb_react six only, case-sensitive.
Path attacks (/static/../app.py, %2e%2e, %00, unicode, 4000-char, /....) →
all 404; custom 404 escapes reflected paths (verified &lt;script in body).
5000-deep nested JSON → 401 (no RecursionError); 10MB body → 401; 40MB body
→ 413 (34MB MAX_CONTENT_LENGTH enforced). X-Forwarded-For rotation bypass
fixed in code (verified client_ip()). Share links degrade: /shorts?video=abc
and =99999 → 200 normal feed; /musefm/photos/abc and /99999999 → 404;
sqlite_int converter 404s non-integers.
CSP-header absence is a documented deliberate tradeoff (tracked separately) —
not re-reported.

## P1 — none.

## P2 — nits
1. 405s return Werkzeug default HTML, not JSON. Repro: curl -X POST
   /api/episodes -d '{}'. Expected: {"ok": false, "error": ...} consistent
   with all other API errors.
2. Inconsistent path normalization: GET /api/forum/react/ (trailing slash)
   → 404, but GET /api//forum/react (double slash) → 405. Should agree.
3. No Cache-Control on API responses (e.g. /api/ping). Suggest
   Cache-Control: no-store on /api/*.
4. Unknown identity activity returns 200, not 404:
   GET /api/agents/fm_!!!/activity → 200 {"fm_id":"fm_!!!","items":[],...}
   while /api/identity/nope, /api/pond/nope, /api/rewards/zz-nope → 404.
   Standardize to 404.
5. Misleading error wording: unsigned body missing `timestamp` reports
   "bad timestamp"; "missing timestamp" clearer.
6. Duplicate JSON keys silently last-win (registered advqa_probe4 from second
   "handle" key). Standard json behavior — noting only.

## Coverage gaps (not bugs)
- Signed post bucket (5/hr per IP) exhausted by sibling testers sharing
  127.0.0.1: double-react toggle, vote switching, fb_react toggle, stored-XSS
  rendering of comment bodies untested; comment image_url/video_url validation
  (e.g. javascript:) sits after post lookup, unreachable without a writable
  target. Recommend hermetic toggle/idempotency run or per-identity buckets
  for test scenarios.
