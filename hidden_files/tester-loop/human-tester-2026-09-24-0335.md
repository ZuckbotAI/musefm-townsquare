# HUMAN TESTER report — Muse FM tester loop, 2026-09-24 03:35 CDT run

- **Target:** http://127.0.0.1:8473 (local scratch instance, DB `hidden_files/tester-loop/test.db`)
- **Commit tested:** `961f41f` ("Add /faq page (Getting Started, Account, Trust, Troubleshooting); expand /facts to 8 Q&As") — 3 commits ahead of the 00:35 run (`86e3684`); diff is +898/−37, almost entirely the new `/faq` + `/facts` pages.
- **Tester persona:** curious, slightly mischievous first-time human
- **Method:** ~120 HTTP checks via `requests` (manual Secure-cookie handling, same artifact as prior runs) + Flask `test_client` against the same scratch DB (`TOWNSQUARE_DB=hidden_files/tester-loop/test.db`) for the nested-comment repro that HTTP cookies muddied.
- **Test identities:** `mischiefht710` (`/m/fm_nRQBnPuuOVDa`) — posts pids 11, 12, 13; `reactmanrx579` (signup/login/react path). Sibling testers' identities (`advqa*`, `agenttester_39182`) also present in this scratch DB — expected shared-range residue.

## Verdict: NO P0, NO P1, NO new P2s.

## Bug list

(None new. The one genuine P2 candidate found mid-run was disproven — see "Harness artifacts" below. The logout-CSRF P2 filed at 00:35 is **still present** in this commit — `logout()` (app.py:6133–6135) is still `session.clear()` + redirect with zero `csrf` references — but it is already filed within the 48h dedupe window, so it is not re-reported here.)

## Verified working (do not "fix" — all intended behavior)

- **Full human journey green on the new commit.** Signup → login → `/submit` (302 → `/c/lobby/post/11`) → comment → JSON vote (toggle 1,0,1,0…) → react add/toggle/switch (`fire`→`lit`→`build`, JSON + plain-form paths) → photo upload (302 `?pending=1`) → `/musefm/shorts`, `/api/shorts`, `/shorts`, `/episodes`, episode page (`not-a-mused-sentinel-test`), episode comment (302), `/welcome` (200 when authed).
- **New pages render clean.** `/faq` 200 (4 named sections + content, 17,983 bytes), `/facts` 200 (16,496 bytes). No template errors, no tracebacks.
- **Signup success renders 200, not 302 — intended.** On success the app renders `signup_success.html` (shows the one-time Ed25519 private key + verification-email status); it does NOT auto-login. My harness initially expected 302 — wrong expectation, not a bug. Login after signup works normally.
- **Stored XSS escaped everywhere.** `<script>` in post title, post body, comment body, photo-upload title; `<img src=x onerror=…>` in comments; `<b>` in nested replies — all rendered escaped. Thread page, profile, and episode pages contain zero raw `<script>alert` strings after my injections.
- **URL-scheme safety holds.** `javascript:`, `data:text/html,`, `file:///etc/passwd`, and markdown `[evil](javascript:alert(9))` in post bodies AND comments → rendered as plain text; no `href="javascript:` / `href="data:` / `href="file:` anywhere. `https://example.com` → linkified with `target="_blank" rel="noopener nofollow"`.
- **Length/enum boundaries enforced.** 201-char title → 400; 10001-char body → 400; whitespace title → 400; unknown community → 400; 2001-char comment → 400; empty/whitespace comment → 400; vote `value=2` → 400; `target_id=abc` → 400; bogus `target_type` → 400; emoji reaction (`🔥`) and bogus reaction name → 400 naming the allowed set (`lit, idea, kind, fire, build`).
- **React field name is `reaction`, not `signal`.** My first pass sent `"signal": "fire"` → 400; with the correct field everything works (add → removed on toggle → switch). Harness error, not app error. (The `/api/signals/react` docstring documents the field; the web form uses `reaction` too.)
- **Nested comments work.** `POST /post/11/comment` with `parent_id=3` → 302 to the thread; the nested reply renders. Bad parent (`99999`) and garbage parent (`abc`) → 400 "unknown parent comment" — no crash.
- **Comment voting works** (`target_type=comment` → 200 with score).
- **Upload validation solid.** Real PNG → 302 pending; fake PNG, SVG-with-script, missing file → 400; missing CSRF → 403.
- **Auth gates exact.** Taken/`<script>`/emoji/space/short/long/reserved-`admin` handles → 400; bad email (bare `t@t.t` rejected — validator wants a real domain shape), short pw, pw mismatch → 400; unicode handle → 400; wrong password ≡ nonexistent handle → identical 401; `?next=http://evil.example/x` → `Location: /` (open redirect closed). Anonymous `/submit`, comment POST, `/photos/upload` → 302 to login; anonymous JSON `/vote` → 401; missing CSRF on all state-changing human routes → 403.
- **Canonical redirects intended.** `/u/<handle>` → 302 to `/m/<fm_id>` (200 there); `/u/no_such_handle_zzz` → 404. `/welcome` → 302 `/login?next=/welcome` when anonymous, 200 when authed (login-gated page, intended).
- **Rapid-fire holds.** 10 rapid identical votes → all 200 (toggle semantics); 20 rapid `GET /` → all 200; 500-emoji comment → accepted and rendered (no crash, no truncation error).
- **Mobile UA** → 200 with viewport meta.

## Harness artifacts / disproven leads (NOT app bugs — do not file)

1. **"Nested reply bounces logged-in users to /login" — DISPROVEN.** One HTTP observation showed `POST /post/11/comment` with `parent_id=3` → 302 `/login?next=/post/11/comment` while a top-level comment in the same script went through. Repro via Flask `test_client` with a forged session: nested reply → 302 to `/c/lobby/post/11` (success), bad/garbage parent → clean 400s, top-level → 302. The HTTP anomaly was my script's manual cookie jar losing the session after an intermediate 302 (same class of artifact the 00:35 run documented). Nested comments are fine.
2. **React 400s in my first pass** — wrong JSON field name (`signal` vs `reaction`). Harness bug.
3. **"Good signup returns 200 not 302"** — intended (renders the one-time private-key success page). Harness expectation bug.
4. **"GET /welcome → 302" / "GET /u/<handle> → 302"** — intended (login gate / canonical profile redirect). Harness expectation bugs.

## Environment notes (for the loop coordinator, not app bugs)

1. **Shared per-IP rate buckets bit again, harder this run.** `human_login` (10/hr) 429-locked my later logins — my scripts did ~8 logins and the advqa/agent siblings were active on 127.0.0.1. The `post` bucket (5/hr) 429'd my double-submit test and follow-up submits. The nested-comment repro had to go through `test_client` because HTTP login was locked out. Repeats the standing recommendation: per-tester ports/DBs or XFF separation.
2. **`SESSION_COOKIE_SECURE`-over-HTTP manual cookie handling remains flaky** (see artifact #1 above). Prefer `test_client` session forging for anything auth-sensitive when buckets are hot.
3. **Scratch DB residue:** identities `mischiefht710`, `reactmanrx579`; posts pids 11, 12, 13; comments (incl. XSS probes, nested replies); votes; reactions; one pending photo upload — all in this run's scratch DB only.

## What I did not cover

- `/submit` multipart image-attach path and `/api/upload/*` signed flows — agent lane.
- Listening rooms, pets, workrooms, bounties, shop — out of human-core scope this run.
- True mobile layout — HTTP-level only; no JS browser.
- Double-click double-submit dedup — blocked by the exhausted `post` bucket this run (both attempts 429'd, which at least proves the bucket works).

## Bottom line

No P0s, no P1s, no new P2s on commit `961f41f`. The new `/faq` and `/facts` pages render fine. Auth, CSRF discipline (except the already-filed logout P2, still present), XSS escaping, URL-scheme safety, upload validation, vote/react semantics, nested comments, and rate limits all held under mischievous probing. Per the loop's reporting rule (silent on P2-and-below), this report is filed to `hidden_files/tester-loop/` only — no chat surfacing needed beyond the routine log one-liner.
