# Tester loop — bugs — 2026-09-24 03:35 CDT run

- **Commit tested:** `961f41f` ("Add /faq page (Getting Started, Account, Trust, Troubleshooting); expand /facts to 8 Q&As")
- **Target:** LOCAL instance http://127.0.0.1:8473, scratch DB `hidden_files/tester-loop/test.db` (fresh, deleted at run start). Real `data/townsquare.db` untouched. No branch switches, no commits, no source edits.
- **Production smoke (read-only, every run):** `GET /`, `/api/shorts`, `/musefm`, `/shorts` on https://musefm-townsquare.onrender.com — all **200**. Live site up.

## Verdict: NO P0, NO brand-new P1, NO new P2s. Staying silent in chat per reporting rule.

The adversarial tester's two "P1s" are re-finds of bugs filed 2026-09-21 as P2s
(still open, code unchanged). Coordinator verified both against the live instance
and the code, and added a failing proof test (tests only). Nothing in this run
is new — but the two known bugs have now survived ~2.5 days / multiple loops
unfixed, which is the notable signal.

## Re-observed (previously filed, still present — not re-reported as new)

1. **Signed `/api/forum/react` burns the 120/hr react budget on invalid emoji/unknown target.**
   Filed 2026-09-21 15:35 as P2 (`bugs-2026-09-21-1535.md`). Coordinator-verified
   this run, code + empirical: emoji allowlist + target-exists checks live inside
   `db.react()` (db.py:2821-2822), called AFTER `check_limit("react", 120)`
   (app.py:5322) — so the route comment claiming the 09-19 P2 fix validates
   before counting is false for the allowlist case. Proof: new
   `test_testerloop_react_login_budget_burn_2026_09_24.py` — 120 invalid signed
   reacts (all 400) from one IP, then a VALID signed react from the same IP →
   **429** (should be 200). ~120 junk POSTs disable reacting for everyone behind
   the same egress IP for an hour.
   Severity note: the adversarial tester graded this P1 (cross-user DoS behind
   shared NAT); the 09-21 triage graded P2 ("self-DoS only"). Same fact, kept at
   P2 per prior triage — flagging the disagreement, not relitigating it.

2. **POST `/login` burns the 10/hr `human_login` budget before any input validation.**
   Filed 2026-09-21 12:35 as P2 (referenced in `bugs-2026-09-21-1835.md`).
   Coordinator-verified: `rate_limit_message("human_login", 10)` is the FIRST
   statement in `login()` (app.py:6105), before handle/password are read — an
   empty POST burns one of 10 attempts. Proof: same new test file — 11 empty
   `/login` POSTs from one IP → the 11th is **429** (should never 429 on
   invalid input). 10 junk POSTs lock out login for the whole IP for an hour.
   Same pattern was fixed on `/submit` and `/api/identity/register`; `/login`
   was missed. New failing test: `test_testerloop_react_login_budget_burn_2026_09_24.py`
   (tests only, no source changes).

3. **Logout CSRF** (filed 2026-09-24 00:35 as P2): still present — `logout()`
   (app.py:6133-6135) still never calls `_check_csrf()`. Deduped.

4. **`/api/shorts?page=-1` echoes `"page": -1`** while serving page 0's content
   (coordinator-confirmed: `{"page": -1, "next_page": 0, ...}`). Same cosmetic
   echo pattern as filed 00:35 P2-3 (`/api/shorts/cards?page=-1`) — same bug
   family, not re-reported.

5. **`/api/health` internals** (`thread_id`, `video_diag`, `disk_free_mb`) —
   already filed (00:35 leaks note). Deduped.

## Dropped (worker lead disproven — again)

- Adversarial P2-3 claimed unauthenticated POSTs to `/mod/*` return the login
  page with 200. Coordinator re-check: `POST /mod/flags` → **405**,
  `POST /settings` → **405**, `GET /mod/uploads` → **302 → /login?next=…**.
  Same redirect-following client artifact disproven at 00:35. Not a bug.

## Repo suites (110 test_*.py, serial, throwaway DBs/TestClient)

56 pass / 54 fail. Non-testerloop failures IDENTICAL to the previous run's
triaged set — all previously filed: signup-without-email cascade (commit
454ba91 made email mandatory): `test_auth`, `test_sidebar`, `test_human_usability`,
`test_identity`, `test_kind_tags`, `test_pet_onboarding`, `test_pet_presence`,
`test_p1_csrf_human_handle_2026_09_18`, `test_workroom_pilot`, `test_workroom_private`,
`test_openmic`, `test_signals`, `test_notifications`, `test_asks`, `test_ai_images`,
`test_comments_pro`, `test_gifs`, `test_linking`, `test_mod_approval`,
`test_mod_notifications`, `test_sso_provider`, `test_shorts`, `test_video`,
`test_video_comments`, `test_uploads`, `test_upload_audio_mimetype_2026_09_18`;
stale/dead: `test_musefm` + `test_p1_2026_09_18` (fb_reactions removed), 
...[truncated 3086 chars]
