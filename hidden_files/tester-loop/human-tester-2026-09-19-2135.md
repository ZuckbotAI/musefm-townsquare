# Human tester — musefm-tester-loop 2026-09-19 21:35

**Commit:** `6adf330` · **Instance:** local `http://127.0.0.1:8473`, scratch DB
`hidden_files/tester-loop/test.db` · **Persona:** human form flows (curl + session jar)

## Findings (all verified against DB where stated)

- **10,000-char post body accepted, rendered in full** (`/submit`, handle `CuriousMia`).
  Not a bug: `db.py` `MAX_BODY = 10000` — the submission sat exactly at the cap.
- **10,000-char comment truncated to 2000** (form route). Known P2 (silently
  truncated to 2000, reported 2026-09-19 03:35) — re-confirmed, not new.
- **XSS payloads escaped.** `<script>alert(1)</script>` in a comment stored raw in
  DB (id 15) but rendered escaped in HTML. No stored-XSS. Verified good.
- **Open-redirect probes** (`?next=https://evil.com`, `//evil.com`) all fell back
  to internal `/`. Verified good.
- **Unauthenticated write routes** (`/submit`, `/vote`, comment POST) redirect to
  `/login`. Verified good.
- **404s clean** — `/post/99999`, `/video/99999` render the 404 page, no stack
  traces. Verified good.
- **Image upload landed pending moderation** ("Image pending mod review") for a
  non-AI-attested upload. Verified good.
- **Rate-limit message** is the clear shared string ("rate limit hit — slow down,
  friend") on form 429s. Verified good.

## Nothing new filed from this persona.

## Late handoff addendum (full tester report)
- Verified good: signup→login→logout→re-login flow; vote upsert semantics;
  malformed JSON on signed API → 401 auth-before-parse (no crash); garbage
  IDs → clean 404s; zero tracebacks in server log; mobile UA gets same HTML.
- Filed from this handoff: **P2-5** empty server-rendered `#handle-chip`
  (no-JS cosmetic).
- Not filed: invalid-reaction silent 302s (covered by known 03:35 P2 on form
  paths swallowing validation errors); self-flagging (arguably fine);
  handle-validation edge cases untested (signup bucket 429'd — harness
  limitation, not a bug).
