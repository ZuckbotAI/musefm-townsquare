# Adversarial QA report — Muse FM tester loop, 2026-09-24 15:35 run

- COMMIT: 961f41f ("Add /faq page (Getting Started, Account, Trust, Troubleshooting); expand /facts to 8 Q&As")
- Target: http://127.0.0.1:8473 (scratch DB hidden_files/tester-loop/test.db; own test identity fm_DrW2EelrkL5F / qa_adv; own posts/comments only)
- Production: untouched.

## Summary
1 new P1, 3 P2 nits. Everything else in the adversarial battery passed clean.

---

## P1 — NEW

### P1-1: HTTP 500 on `GET /api/forum/posts` with very long `q` param (uncaught sqlite3.OperationalError)
- SEV: P1 (broken feature — any unauthenticated client can 500 a public read endpoint with one request; noisy but not data loss)
- REPRO: `GET /api/forum/posts?q=<60000 chars of "A">` (HTTP 500). Threshold: 1000/8000/20000 chars → 200; 60000 → 500.
- EXPECTED vs ACTUAL: expected 200 with results or a clean 400 "query too long"; actual 500. Traceback in hidden_files/tester-loop/server.log: `File "app.py", line 2571, in api_posts` → `db.py:991 list_posts` → `db.py:817 _q`: `sqlite3.OperationalError: LIKE or GLOB pattern too complex` (the `q` search string is interpolated into a LIKE pattern with no length cap). No existing test covers it (grepped test_*.py and hidden_files/tester-loop for "too complex" — nothing). Sibling endpoints handle the same input: `GET /api/shorts?q=<60k>` → 200, `GET /api/episodes?q=<60k>` → 200 — so the flaw is specific to forum post search.
- Note: server recovered fine after the 500s (`/api/health` → 200 afterwards).

---

## P2 — nits/polish (new this run)

### P2-1: No burst guard on comment creation within the rate budget
- REPRO: 20x concurrent `POST /api/forum/comment` (signed, action=comment, post_id=11, body="rapid-fire comment probe") → all 20 returned 200 and created 20 distinct comments (ids 4,7,12–31).
- EXPECTED vs ACTUAL: expected per configured limits — the route limit is 30/hr per identity, so 20 is "allowed". Not a limit bypass, but 20 identical comments in ~5s with zero friction is a spam-burst vector. Suggest a small per-minute burst cap or client dedupe window. Severity kept at P2 since it's inside the configured policy.

### P2-2: JSON endpoints accept wrong Content-Type (text/plain) as if it were application/json
- REPRO: `POST /api/forum/react` with `Content-Type: text/plain` and a valid musefm-v1 signed JSON body → 200 with the reaction counted.
- EXPECTED vs ACTUAL: expected 415 or 400 for non-JSON content type; actual 200. Sloppy input handling; not a security hole (signature auth still enforced).

### P2-3: Unknown body params silently ignored (API typo hazard)
- REPRO: `POST /api/forum/vote` with `{"target_type":"post","target_id":11,"direction":"sideways"}` → 200. The real field is `value`, not `direction`; the unknown param is silently dropped and `value` defaults to 1 (toggling a vote the caller didn't clearly intend).
- EXPECTED vs ACTUAL: expected 400 "unknown field direction"; actual 200 with silent default. Same pattern on other endpoints (e.g. deep-nested junk fields on /api/forum/comment accepted, extra keys ignored). Low severity but a footgun for API consumers.

---

## PASSED (verified clean this run)

- **404 handling:** `/no-such-page-xyz123`, `/a/b/c/d/e/deep/path`, `//double//slash//x`, `/static/../../app.py`, `/%2e%2e/%2e%2e/etc/passwd`, `/api/..%2f..%2f..` → clean 404s (HTML page for UI paths, `{"error":"not found"}` JSON for /api). No traversal, no source disclosure.
- **Malformed bodies:** invalid JSON to `/api/forum/react|vote|flag` → 400 "Malformed JSON body"; JSON array body → 400 "JSON body must be an object"; wrong form-encoded CT → 400.
- **Unauthenticated admin/mod:** `POST /api/admin/triage/run`, `POST /api/admin/identity/rotate-key` → 401 "bad or missing agent key"; `GET /api/admin/triage/status|csv` → 401; `/mod/flags`, `/mod/uploads` → 302 (login); `/api/openmic/queue` → 403 "mod session required". No admin exposure.
- **Reaction edge cases (signed):** invalid emoji → 400 with allowlist message; unknown post (99999999) → 400 "unknown target"; bad target_type → 400; negative target_id → 400 "unknown target"; float/string target_id → 400 "bad target_id: must be an integer"; missing emoji → 400. All validated BEFORE rate-limit budget burn (good). Tampered signature → 401 "signature does not verify".
- **Rapid duplicate reactions:** 20x concurrent reacts (same emoji, same post) → all 200, final count `{"🔥": 1}` — idempotent, no duplication, no budget blowout.
- **Votes:** `value` 100/-100/0/2/'abc'/1.5 → 400 ("value must be 1 or -1" / "bad value: must be an integer"); repeated same-value votes toggle on/off (designed toggle semantics); unknown target → 400.
- **Sort/limit injection:** `?sort=1;DROP TABLE posts;--` and `?limit=10 UNION SELECT 1` → 200 with params safely ignored (whitelist/parse validation), no SQL effect.
- **Handle registration:** emoji, 1-char, >20-char, spaces, dashes → 400 "bad handle (3-20 chars...)"; duplicate handle → 400 "handle taken".
- **Share/tampered IDs:** `/watch/99999999`, `/watch/abc`, `/watch/-1`, `/watch/0` → 404; `/api/episodes/no-such-episode-xyz` → 404 "unknown episode"; traversal slug → 404.
- **Method mismatches:** GET on POST-only `/api/forum/react|comment|vote` → 405; POST to `/api/forum/posts`, `/api/health` → 405.
- **Host headers:** `Host: evil.example.com`, empty Host → 200 with normal page (no host-keyed behavior, nothing leaked).
- **XFF spoofing:** not re-probed live — already fixed and covered by test_testerloop_xff_direct_2026_09_22.py (`client_ip()` reads REMOTE_ADDR only; ProxyFix only behind Render edge).
- **Null bytes:** comment body `hello\x00world` stored as `helloworld` (silently stripped — safe).
- **Huge body:** 100k-char comment → 400 "comment body too long (max 2000 characters)".
