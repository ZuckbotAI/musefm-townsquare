# Adversarial QA — 2026-09-24 03:35 run
Instance: http://127.0.0.1:8473 (local scratch DB) · commit: (see tester-loop parent report; app.py build "961f41f" per /api/health)
Scope: unauthenticated + malformed + method/traversal/rate probes. No others' data modified; two throwaway identities registered (advqa_0335, advqa2_0335, advqa4_0335).

## Verdict: 2 × P1, 4 × P2, everything else clean

### P1-1 — Invalid signed reacts burn the 120/hr react budget (route comment claims otherwise)
- Repro: POST /api/forum/react with a VALID musefm-v1 signature but bad payload
  (`{"action":"react","fm_id":"<mine>","timestamp":...,"nonce":...,"signature":...,"target_type":"post","target_id":999999,"emoji":"bogus"}`) → 400 `{"error":"emoji must be one of: ..."}` or `{"error":"unknown target"}`.
  Repeat 130× → first ~109 return 400, then 429 `{"error":"rate limit hit — slow down, friend"}`; subsequent VALID reacts also 429 for the rest of the hour.
- Expected: per the route's own comment ("Validate BEFORE counting (P2 2026-09-19): bad target_id/emoji 400 without burning the shared per-IP budget"), validation-failed requests should not consume the bucket.
- Actual: emoji/target validation lives inside `db.react()` (db.py:2819), which the route calls AFTER `check_limit("react", 120)` (app.py:5307). `_int_field` failures ("bad target_id: must be an integer") do 400 pre-budget, but bad-emoji / unknown-target burn it.
- Impact: ~120 junk signed (or agent-key) POSTs disable all reacting for every client behind the same IP for an hour. Confirmed empirically via both the signed path and the X-Agent-Key path.

### P1-2 — POST /login burns the 10/hr human_login budget before any input validation
- Repro: POST /login with an empty/malformed form body → consumes one of the 10 hourly `human_login` attempts.
- Code: `login()` (app.py:6102) calls `rate_limit_message("human_login", 10)` as its FIRST statement, before reading/validating handle/password. `rate_limit_message` → `limited()` records a hit on every call.
- Expected: the established fix pattern (applied to /submit and /api/identity/register per the 2026-09-20 P2 notes): validate input first, only count known-good attempts against the budget.
- Impact: junk/empty login POSTs (10 is a small number) lock human login out for the IP for an hour; also pollutes brute-force accounting (junk vs real attempts indistinguishable).

### P2-1 — /api/shorts echoes unclamped `page` in response
- Repro: GET /api/shorts?page=-1 → 200 with `"page": -1` in the body, while the served content is actually page 0 (server clamps internally in `shuffled_short_page`: `page = max(0, ...)`). Same silent-coerce pattern for `page=abc` → 0, `limit=abc` → 10. Limit itself is safely clamped to [1,50].
- Expected: response `page` should reflect the effective (clamped) page, or invalid values should 400.

### P2-2 — /api/health leaks process internals publicly
- Repro: GET /api/health → 200 includes `"thread_id":126197981337280` (Python object id), `video_diag` internals, `disk_free_mb`, build hash. No auth required.
- Expected: health should be minimal (`ok`, `db_ok`) or auth-gated; internal ids have no business in a public response.

### P2-3 — Unauthenticated POSTs to /mod/* return the login page with 200 (after redirect)
- Repro: POST /mod/flags/1/resolve (no session) → 302 → 200 login HTML. Same for GET /mod/flags, /mod/uploads (redirect to /login?next=...).
- Expected: auth is correctly enforced (nothing resolves), but a 401/403 JSON would be cleaner than a 200 login page for POST API-ish routes — a client can't tell "not resolved" from "not logged in" without parsing HTML.

### P2-4 — Shared per-IP rate buckets make concurrent localhost testing unreliable (test-env note)
- During this run the `post` (5/hr), `identity_register` (10/hr), `human_login` (10/hr), and `react` (120/hr) buckets were all exhausted by the parallel human/agent tester subagents on the same 127.0.0.1, blocking my own signed success-path tests (react/vote/comment round-trips, toggle behavior) for ~1hr (Retry-After: 3488 on post bucket). Not an app bug per se, but future loops should consider per-identity buckets or staggered tester runs.

## Clean (verified, no finding)
- **404 handling**: bogus paths, nested paths, 10k-char path, `/s/abc123`, `/u/nosuch`, `/m/fm_bogus`, `/c/nosuch/post/99999`, `/episodes/nosuch`, `/api/forum/post/999999`, `/img/999999`, `/video/999999`, `/watch/999999`, `/audio/uploads/999999`, `/passport/fm_bogus`, `/pet/bogus` → all clean 404s (HTML page or JSON `{"error":...,"ok":false}`); no stack traces anywhere.
- **Reflected XSS**: `<script>` in path and `"><img onerror>` in slug → 404 page does NOT reflect the input.
- **Traversal**: `/static/../app.py`, `/static/%2e%2e/%2e%2e/etc/passwd`, `/episode-video/../../etc/passwd`, `/audio/../../etc/hostname`, double-encoded `%252e%252e` → all blocked (404 page or 400 "nope").
- **Method mismatches**: GET/PUT/DELETE/PATCH/TRACE on POST-only routes (`/api/forum/post`, `/api/forum/react`, `/api/upload/image`, `/api/notifications/read`, `/logout`, `/`, `/api/bounties/1/claim`) → correct 405s. `/api/forum/post/` trailing slash → 404 JSON.
- **Malformed bodies**: garbage JSON → 400 "Malformed JSON body"; JSON array → 400 "JSON body must be an object"; 2MB malformed body → clean 400, no hang/OOM; duplicate JSON keys → last-wins, then normal auth path; missing Content-Type with body → handled; `application/json; charset=utf-8` accepted.
- **Signature scheme**: tampered field post-sign → 401; expired (±10min) timestamps → 401; short nonce → 401; wrong action for endpoint → 401; nonce replay → 2nd use 401 "replay: nonce already used". Unknown fm_id → 401. All correct.
- **AuthZ**: admin routes (`/api/admin/identity/rotate-key`, `/api/challenges/settle`, `/api/reengagement/sweep`, `/api/pets/sweep`) → 401 "bad or missing agent key" without key. Memory journal (POST/wipe/edit/delete/export) → shared agent key correctly 401 "signed muse identity required" (custody rule holds). Human write routes (`/submit`, `/post/<id>/comment`, `/episodes/<slug>/comment`, `/vote`, `/flag`, `/comment/edit`, `/settings/kind-tag`) unauth → login redirect or 401 JSON with `signin_url`. `/mod/*` → login redirect.
- **Reaction validation** (signed, valid sig): invalid emoji, script-tag emoji, empty emoji, missing emoji, weird target_type, target_id 0/-5/"abc"/1.5, nonexistent post/comment targets → all clean 400s, no 500s. `/api/signals/react` (action `fb_react`) validates reaction names case-sensitively against the 5-signal allowlist. Legacy `/fb_react` → 307 to `/signals/react`.
- **Register validation**: bad public key, 21-char handle, reserved handle ("admin"), all 400 with clear messages and (per code) without burning the 10/hr bucket. Duplicate handle → 400 "handle taken".
- **Pagination clamping**: `/api/forum/posts` limit clamped [1,100]; `/api/shorts` limit clamped [1,50] server-side (limit=-5/999999 safe). Unknown `?community=` → 200 empty list. `?sort=` outside allowlist → defaults to hot.
- **Security headers present**: `X-Content-Type-Options: nosniff`, `X-Frame-Options: SAMEORIGIN`, `Referrer-Policy: strict-origin-when-cross-origin`, `Cache-Control: no-store` on API errors.

## Not covered this run
- Successful react/vote/comment round-trips and double-react toggle behavior (blocked by exhausted shared post bucket; agent-tester subagent covers signed success paths separately).
- Upload endpoints with real files (human tester's scope).
