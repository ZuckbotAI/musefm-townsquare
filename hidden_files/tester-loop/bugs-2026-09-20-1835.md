# Tester-loop findings — run 2026-09-20 18:35 CDT (commit f4d6a22)

Commit tested: `f4d6a22` — "P1 2026-09-20: Secure cookie gated on request scheme (fixes plain-HTTP human login); P2 missing-signature message; P2 key-reuse docs note; repair stale contention test handles"

- Local instance: http://127.0.0.1:8473, scratch DB `hidden_files/tester-loop/test.db` (deleted at run start, hermetic)
- Repo suites: 76/76 standalone test scripts pass (`hidden_files/tester-loop/suite-results.txt`)
- Production smoke (read-only): `GET /`, `/api/shorts`, `/musefm`, `/shorts` on musefm-townsquare.onrender.com → all 200
- Personas: human tester, agent/API tester, adversarial QA — all completed

## Severity summary: NO P0s, NO new P1s. Four new P2s below + one methodology note.

---

## P2 (NEW): `POST /api/identity/register` silently drops `display_name`
- Found by: adversarial QA
- Repro: `POST /api/identity/register` with JSON `{"handle": ..., "public_key": ..., "display_name": "<img src=x onerror=alert(1)>"}` → 200 OK, but `display_name` is not read in that path — nothing stored, nothing validated.
- Expected: either validate/persist the field or return a 400 for the unexpected field.
- Note: no XSS — nothing is stored. The human signup form regex-validates `display_name`; this is the machine-API path.

## P2 (NEW): `/api/health` exposes build commit
- Found by: adversarial QA
- Repro: `GET /api/health` → `{"build":"f4d6a22", ...}`.
- Expected: health endpoints should not leak build internals; arguably intentional for debugging, but flag as trivial info disclosure.
- Note: `/api/stats` exposes only aggregate counts (posts, episodes, members) — looks intentional.

## P2 (NEW, informational): same-value re-vote removes the vote (toggle-off), may surprise
- Found by: human tester
- Repro: `POST /vote` (target_id=8, target_type=post, value=1, logged in, valid CSRF) ×3 → rendered score 0→1→0→1.
- Expected (naive): idempotent upvote, score stays 1.
- Actual: re-voting the same value toggles the vote OFF. Confirmed intentional in db.py (`# toggle off` comment, atomic recompute) — not a double-count bug. Flagged because rapid-click QA should know this is designed behavior; consider a UI hint.
- Dedupe: not previously reported.

## P2 (NEW): 2000-emoji comment accepted and rendered in full
- Found by: human tester
- Repro: `POST /post/8/comment` (logged in, valid CSRF), body = "🔥"×2000 → 302; comment renders in full on `/c/lobby/post/8`.
- Expected: arguably a cap/collapse for pathological input (comment cap is 2000 chars via `clean(body, 2000)` — this hit it exactly).
- Actual: full 2000-emoji wall stored and rendered. Cosmetic only; the `c-collapsible` class exists but the full string is still in the DOM.
- Dedupe: 0935 discussed emoji-flood POSTs being 429-blocked; this is the distinct COMMENT path accepting and rendering the wall.

## Methodology note (repeat of 1535): parallel testers contend on shared per-IP buckets
- The human, agent, and adversarial testers all hit the same IP-keyed buckets (`("post", 127.0.0.1)` 5/hr, identity_register 10/hr) on this port, producing 429s that looked like failures mid-run. Not an app bug — correct behavior — but next run should stagger testers or give each a distinct client IP identity.

## Deduplicated (already in recent bug files, not re-reported)
- Shared human/agent IP-keyed "post" rate bucket (0046/0635/0935/1535); the human tester's variant (12k-char body hitting 429 before validation when another persona burned the bucket) is a consequence of that known design.
- X-Forwarded-For rate-limit bypass (1535, P1 — LIVE, awaiting fix).

## Verified-working this run (no regressions)
- Signed machine flows: registration, signed post/comment, signed multipart image upload (sha256-bound, `/img/<id>` byte-identical), attestation verify (signal credential + passport). All 10 negative cases rejected correctly (replay → 401 "replay: nonce already used", tampered sig/field → 401, stale ts → 401, action mismatch → 401, unsigned → 401, sha mismatch → 401, non-image bytes → 400).
- XSS: `<script>` in titles/bodies/comments/episode comments/signup bio all escaped; autolinker ignores `javascript:` (incl. case variants, newline splits); `<script>` inside http:// URL query strings truncated+escaped. `|safe` absent on bio in all templates.
- Uploads: SVG/HTML-disguised-as-PNG rejected 400; real PNG accepted into mod-approval queue.
- Auth: signup/login, duplicate handle → 400, bad password → 401; logged-out /submit, /photos/upload → 302; logged-out JSON /vote, /fb_react → 401 with signin_url.
- Validation: votes reject non-{1,-1} values; reactions restricted to allowlist; vote/comment on nonexistent targets → 404; cross-post parent_id → 400; `/login?next=//evil.com` → 302 to /.
- Robustness: guessed admin paths → 404; truncated/wrong-CT/array JSON → clean 400/401; SQLi/path-traversal strings → clean 404s; 20-request bursts → 20×200/20×401, no degradation; 15-vote/12-comment bursts → no duplicates, no 500s.
