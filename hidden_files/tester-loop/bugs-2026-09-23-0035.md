# Muse FM tester-loop bug report — 2026-09-23 00:35 CDT run

**Commit tested:** `9ce67b7` ("Merge remote-tracking branch 'origin/master' into batch-orb-sayings")
**Working tree:** uncommitted modifications pre-existing (hidden_files/grok_watch_state.json, growth_watch_state.json, tester-loop/server.log) — noted, not touched.
**Instance:** local hermetic server on port 8473, scratch DB `hidden_files/tester-loop/test.db` (freshly deleted at run start; one mid-run restart reused the same scratch DB after an OS reboot — see below). Production: read-only smoke only.
**Production smoke:** `/`, `/api/shorts`, `/musefm`, `/shorts` → all 200. Live site healthy.

**HEADLINE: zero P0s, zero new P1s. Yesterday's P2s are getting fixed.** The 21:35
`/submit` draft-loss P2 is FIXED and verified (5/5), and the 18:35 upload
rate-limit-before-validate P1 stays green (4/4). The XFF rate-limit P1 fix (conditional
`ProxyFix`, commit `4f596a6`) is now in the code at HEAD — the 09-22 note said "awaiting
fix"; it has landed.

**Fix policy:** no source edits. No new failing-test artifacts added (nothing new to pin).

---

## FIXED since yesterday (verified this run, hermetic)

1. **/submit draft-loss P2 (filed 2026-09-22 21:35) — FIXED.**
   `hidden_files/tester-loop/test_testerloop_submit_draft_loss_2026_09_22.py` now passes
   5/5. Re-verified directly: POST /submit with 201-char title → 400, and the re-rendered
   composer now preserves BOTH the over-long title and the body ("t"*201 and
   "my precious draft body" both present in the 400 HTML). The whitespace-only-title
   400 case also preserves the draft. Test now green as designed — no longer failing.
2. **Upload rate-limit-before-validate P1 (filed 2026-09-22 18:35) — still fixed.**
   `test_testerloop_upload_ratelimit_before_validate_2026_09_22.py` 4/4 green.
3. **XFF rate-limit bypass P1 (filed 2026-09-21 15:35) — fix landed in code.**
   `app.py:116-125`: `ProxyFix(x_for=1)` now runs ONLY when `RENDER_GIT_COMMIT` is set;
   `client_ip()` reads REMOTE_ADDR only. Commit `4f596a6` ("P1 XFF rate-limit bypass fix:
   gate ProxyFix on RENDER_GIT_COMMIT; regression test; repair stale pioneer test").
   The remaining `test_p1_2026_09_18.py` check ("different proxy-supplied IP keeps its own
   bucket" → 429) now fails *by design* — XFF is ignored on direct connections, so all
   TestClient requests share the 127.0.0.1 bucket. Not a bug; the pioneer test's second
   assertion is stale post-fix.

---

## Stale-test wave from today's batch (NOT app bugs — tests need refresh)

Today's `d2e9861` ("orb hero integration + tap dialogue, signals reactions, spring cleanup",
2026-09-23) made deliberate removals that broke 19 test checks across 5 files:

- **`fb_reactions.py` DELETED** (reactions folded into signals). Three test files now die at
  `import fb_reactions` (ModuleNotFoundError): `test_fb_reactions.py`, `test_musefm.py`,
  `test_testerloop_0046_p2s_2026_09_20.py`. The feature they tested still works live —
  `/api/forum/react` signed path and `/signals/react` human path verified by the testers
  this run (reaction allowlist correctly 400s `not-an-emoji`, flood, `<script>`, etc.).
- **`/arena` + `/pro` scrubbed from `templates/base.html`** (family bar links removed;
  sidebar Links active-state dropped `startswith('/arena')`) — completes the 2026-09-21
  Arena retirement (`bc274c2`) and the Exchange-Pro brand retirement. `test_sidebar.py`:
  108 passed, **11 failed** (all `['/arena', '/pro']` section-link assertions).
  `test_links.py`: 17 passed, **10 failed** (all arena/pro card/link assertions).
- **`/fb_react` → legacy 307 alias to `/signals/react`** (code-commented 2026-09-22).
  `test_bugfix_csrf_2026_09_19.py`: 8 passed, **5 failed** (tests POST to the old route and
  expect 200/403/302, get 307). The redirect is intentional; CSRF coverage of the new
  route is green elsewhere (test_p1_csrf_human_handle).

Also stale (pre-existing, unrelated to today's batch):
- `test_testerloop_bio_truncate_2026_09_22.py` — `ImportError: clean_bio` (helper moved out of db.py).
- `test_workroom_bridge.py` — `AttributeError: ensure_workroom_events_schema` (renamed to `ensure_workroom_schema`).
- `test_shorts_seen_rotation.py` (untracked, added 2026-09-22) — 12 passed, **6 failed**.
  Its expectations never matched shipped design: (a) the unit checks assert excluded ids
  sort *after* unseen, but `videos.shuffled_short_page` *drops* excluded ids when the pool
  is comfortably larger (commit `101959b` docstring: "leave out of the pool"); (b) "home
  mints a seed" greps `var miniSeed`, which commit `747dc81` (founding-members revert)
  removed from the homepage; (c) the load3 "unseen-first" checks contradict the deliberate
  pool-safe fallback in the 120s repeat window (commit `4af2bda`). Test needs a rewrite
  against the actual design, not the app.
- `test_testerloop_logincsrf_2026_09_21.py` 0/3 — the known `/signup`+`/login` CSRF-exempt
  P2 (filed 09-21, still open; listed below), not new.

## Environmental incident (not an app bug)

- **The test server died mid-run (~00:44 CDT).** Forensics by the adversarial tester:
  the whole VM rebooted (~05:45 UTC; `uptime` confirmed "up 8 min" later this run).
  No traceback in server.log, no OOM/core, and no request ever 500'd — an OS-level restart,
  not a probe-induced crash. Instance was rebooted per loop procedure (same scratch DB)
  and served 200s for the rest of the run. In-memory rate-limit buckets reset with it;
  testers worked around the shared-IP budget. Same signature as the 09-22 03:35 loop's
  mid-run death — this time the cause is identified.

## Re-verified still-open (already filed, not new)

- Comment-CREATE and comment-EDIT **silent truncation P1** (filed 2026-09-21 22:35; loud
  report already sent). `test_testerloop_comment_edit_truncate_2026_09_21.py` 4 passed /
  8 failed — oversize bodies still 302-accepted instead of 400, on both paths.
- `/signup` + `/login` **CSRF-exempt P2** (filed 09-21 21:35).
- **Trailing-slash policy class P2** (`POST /api/forum/react/` 404 vs `/api/forum/react`
  400; custom 404 swallows Flask's 308).
- **`/api/platform-key` envelope P2** (filed 09-22 06:35; still 1 check down).
- **Episode-comment redirect P2** (filed 09-22 09:35; still 1 check down).
- **rawint-exn P2** (`test_testerloop_rawint_exn_2026_09_22.py` 3/3 down), **flood-wall P2**
  (`test_testerloop_1835_flood_wall_2026_09_20.py` 12/4), **ratelimit-before-validate-workroom P2**
  (`test_testerloop_ratelimit_before_validate_2026_09_20.py` 3/2).
- **Missing-signature says "bad signature encoding" P2** (filed 09-22; agent tester
  re-filed the same nit this run — deduped: `identity.py verify_signed_body`,
  `b64u_decode(None)` maps to the wrong message).
- **Host-header reflection P2** (`Host: evil.example.com` → absolute API URLs echo it;
  login redirects are safe/relative).
- Query-param `agent_key` accepted (noted 09-18; unchanged).
- `test_listening_rooms_2026_09_21.py` needs `AGENT_KEY` in env (harness-only, not a bug).

## Verified solid this run (no findings)

- **Human tester (live):** XSS battery held — `<script>` in titles/comments/bios/episode
  comments all escaped at render (`link_mentions` runs `htmlmod.escape`); attribute-breakout
  title neutralized; `javascript:`/`data:`/`vbscript:`/entity-encoded schemes never linkified;
  magic-byte upload sniffing rejects renamed HTML; vote toggle idempotent (1 row, score −1);
  404s clean everywhere; 5/hr post budget 429s with Retry-After; validation before budget
  (400s don't burn). Two mid-run XSS "P0s" were retracted after render verification
  (raw-in-JSON ≠ XSS).
- **Agent tester (60+ checks):** signed-request crypto all green — tampered sig 401,
  nonce replay 401, ±10min timestamps 401, wrong-action 401, unknown fm_id 401,
  tampered field 401, malformed JSON 400; nonce NOT burned on 429 or tampered-401
  (proven against `seen_nonces`); media provenance holds (tampered `ai_generated` 401,
  sha256 mismatch 401, fake/truncated/tiny video 400, oversize GIF 413, evil hosts 400);
  platform attestations verify and tampered payloads fail; pending uploads hidden from
  `/api/shorts` and 404 on direct fetch.
- **Adversarial QA (live):** path traversal (incl. double-encoded, overlong UTF-8) → 400;
  SQLi strings in search params → clean 200/404, zero error strings; null bytes clean;
  100k-char inputs → 400/401, no crash; CRLF-in-filename neutralized; unauthenticated
  privileged paths all 401/403/302→login; 130 rapid authed votes → 85×200 then 45×429;
  duplicate JSON keys → 400; TRACE → 405.
- **Repo suites:** everything not listed above green, incl. test_human_usability 31/31,
  test_row.py 65/65 (pytest), test_video_comments 43, test_vote_stress 9, test_workroom 43.

## Caveats

- Testers shared one IP; per-IP budgets partially consumed by concurrent traffic.
  Restart mid-run reset in-memory buckets. Human tester's Secure-cookie-over-HTTP quirk
  (manual Cookie jar) is a harness note, not an app bug.
- Scratch DB retains this run's tester objects; the next run deletes it per procedure.
- Commit `9ce67b7` is a merge of today's batch; uncommitted state was limited to
  watch-state JSONs and this run's server.log.

**NOT LOUD:** zero P0s, zero new P1s. Newly-fixed P2 + landed P1 fix noted above; the rest
is the known backlog.
