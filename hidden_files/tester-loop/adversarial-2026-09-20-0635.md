# Adversarial QA notes — 2026-09-20 06:35 run
Target: LOCAL instance only, http://127.0.0.1:8473 (scratch test DB)
Commit tested: d1fde08 ("Maker's Row Cycle 6: puffy pixel clouds, lifted clear of the roofline")
Persona: ADVERSARIAL QA (robustness / unauthenticated / edge-input). No production touched.

## F1 (P2 nit): /api/ping leaks internal diagnostics unauthenticated
Repro: `curl -s http://127.0.0.1:8473/api/ping`
Response included: `{"ok":true,"ts":1789904352,"build":"d1fde08","thread_id":128406522525376,"worker_pid":1857,"cleanup_46":"skip:no_row @1789904167","video_diag":{"count":0,"max_id":null,"v46":null}}`
Expected: health endpoint returns ok/ts/build only. thread_id, worker_pid, and db-cleanup diag fields are internal noise exposed to anyone. Harmless, but tighten before excellence: return `{"ok":true,"ts":...,"build":...}`.
Note: no 429 observed after 50 rapid-fire GETs — reads have no rate limiting (acceptable, not a bug).

## F2 (P2 nit): Server banner leaks exact Werkzeug/Python version
Repro: `curl -s -D - http://127.0.0.1:8473/api/ping | grep -i server`
Got: `Server: Werkzeug/3.1.8 Python/3.12.3`. Version fingerprinting aids attackers; strip/customize in prod config (dev server header — check prod serving stack).

## Verified clean (no bugs)
- 404 handling: /nonexistent-page-xyz, /api/doesnotexist, /post/999999999, /user/nope, /.git/config, /.env, /server.js all → clean custom 404 page (no stack traces, no internals).
- Garbage share links: /m/%20, /watch/999999, /gif/999999, /video/999999, /photo-file/999999, /musefm/photos/999999, /pet/';--, /agent/';--, /c/<img>, /c/nonexistent-slug-xyz → 404, no trace leaks, no SQLi signals.
- Path traversal: /episode-video/../../etc/passwd → 404; /audio/..%2F..%2Fetc%2Fpasswd → 400; /audio/uploads/../../../../etc/passwd → 404. No /etc/passwd leakage.
- Open redirect: /login?next=https://evil.com and /logout?next=https://evil.com produce no Location header; code check confirms _safe_next() (app.py:713) is same-origin-only (rejects absolute URLs, //evil.com, backslashes, control chars). No open redirect.
- Mod areas: GET /mod/flags, /mod/uploads → 302 → /login?next=... unauthenticated. GET on mod POST routes → 405. /api/openmic/queue → 403 "mod session required".
- Malformed JSON / wrong content types on /api/forum/post, /api/forum/vote, /api/forum/react, /api/identity/register, /api/link_muse, /vote: all handled → 400 or 401 JSON errors, no crashes. Empty body: 400/401. text/plain: 400 (302 for /vote, redirect-to-login, benign). Array body: 400 "JSON body must be an object". /api/agent-memory/recall → 405 regardless (GET-only).
- React/vote edge cases (garbage reaction, missing params, 5000-char reaction string, negative post_id, string-typed IDs): all → 401 auth gate first. Auth-before-validation everywhere.
- Privileged POSTs unauthenticated all 401: /api/memory/wipe, /api/memory, /api/swarm/freeze, /api/pets/sweep, /api/reengagement/sweep, /api/pet/fusion/invite, /api/challenges/settle, /api/identity/update, /api/upload/audio, /api/webhooks, /api/rewards/invite-code (GET and POST), DELETE /api/agent-memory/forget.
- Login brute-force rate limit works: 15 rapid bad-password POSTs → 401×9 then 429 (rate_limit_message("human_login", 10); Retry-After header present).
- /signup garbage: empty form → 400; SQLi handle `x';DROP TABLE ident;--` → 400, clean signup page re-render. No injection.
- Method discipline: PUT /api/forum/post → 405, DELETE /api/agent-memory/list → 405, TRACE / → 405. OPTIONS / → 200 (normal).
- Security headers: X-Frame-Options: SAMEORIGIN present on responses.
- 5MB garbage body to /api/forum/post → 400 in 0.06s (rejected before meaningful read; no DoS hang).

## Summary
No P0s, no P1s. App is well-hardened on the adversarial surface: auth-first ordering everywhere, _safe_next closes open redirects, mod paths gated, login rate-limited, signup validation sane, path traversal blocked. Two P2 nits: /api/ping diagnostic leakage and the Werkzeug version banner.
