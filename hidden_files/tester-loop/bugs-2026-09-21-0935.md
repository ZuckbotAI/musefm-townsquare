# Tester-loop triage — 2026-09-21 09:35 CDT run
**Commit:** `20f46e0` ("Fix tester-loop P2s (2026-09-21 06:35 run)"), branch
`workroom-bridge-2026-09-21` (current checkout tested as-is; no branch switch)
**Target:** local instance `http://127.0.0.1:8473`, fresh scratch DB
(`hidden_files/tester-loop/test.db` deleted at run start; real `data/townsquare.db` untouched)
**Production smoke (read-only):** `/`, `/api/shorts`, `/musefm`, `/shorts` → all 200
**Repo suites:** pytest sweep 75 passed / 3 failed in
`test_testerloop_1835_fixes_2026_09_19.py` (`test_scunthorpe`,
`test_edit_echoes_stored_body`, `test_comment_body_cap`) — confirmed pre-existing
via `git stash` (same 3 fail on the committed tree). `test_sidebar.py` run
directly: 107 pass / 9 fail — the 9 are the stale `/musefm/shorts` link
expectations already recorded in the last 48h bug files.

## P0 — crash / data loss / security hole
**None.** All three personas + adversarial battery agree.

## P1 — broken feature
**None.**

## New P2s this run (not in last 48h bug files)
Proven by new tests-only file `test_testerloop_p2_2026_09_21_c.py`
(4 fail / 1 pass; no source changes):

1. **Param validation before the auth gate on `/api/link_muse`** — unsigned
   `POST /api/link_muse` (no code) → 400 `"code required"` instead of 401.
   Repro: `curl -s -X POST -H "Content-Type: application/json" -d '{}'
   http://127.0.0.1:8473/api/link_muse`. Expected: unauthenticated requests
   fail auth first. (adversarial QA; live-proven + failing test)
2. **Same on `/api/episodes/<slug>/clips`** — unsigned POST to a bad slug →
   400 `"unknown episode"` instead of 401. Ordering nit only; no data leak —
   with valid params both endpoints correctly 401 when unsigned.
   (adversarial QA; live-proven + failing test)
3. **`/video/<uid>/comment` burns rate budget before body validation** —
   `video_comment_web` (app.py:6876) calls `_video_comment_limits(uid)`
   (records into the 30/hr `video_comment` + 10/min `vcomment:<uid>` buckets)
   *before* `db.create_video_comment` validates the body, so an invalid body
   (→ 400) still consumes budget. The 2026-09-20 03:35 validate-first fix
   covered `/submit`, `/post/<pid>/comment`, `/episodes/<slug>/comment` but
   not this route. Code-confirmed + failing test.
   (human tester)
4. **JSON POST to `/post/<pid>/comment` 403s with a misleading message** —
   `_check_csrf()` reads only `request.form`, so a JSON POST carrying a valid
   CSRF token gets 403 `"bad form token — reload and try again"`. Sibling
   routes (`/vote`, `/flag`, `/comment/edit`) read `request.get_json`. Low
   impact (humans use forms); fix is either JSON support or a JSON 4xx that
   names the real problem. (human tester; code-confirmed + failing test)

## Known / already-recorded findings (deduped, not re-reported as new)
- Web comment bodies silently truncated to 2000 chars (pre-existing P2; the
  `/post/<pid>/comment` variant is the same class as `/submit`'s fixed one)
- Vote-on-unknown-target: JSON → 400 vs form → 404 inconsistency (KNOWN P2)
- Comment double-submit has no idempotency (filed 2026-09-18 human #5)
- Per-IP shared `post` (5/hr) bucket shared between human form and signed API
  (recorded 09-20 all runs, 09-21-0035)
- `test_scunthorpe` / `test_edit_echoes_stored_body` / `test_comment_body_cap`
  suite failures (pre-existing, stash-confirmed)
- `int()` accepting unicode digits (fullwidth "１") in `/vote` — benign
  normalization, informational

## Verified NOT bugs this run
- **Repeat identical upvote toggles the vote off** (agent + human testers):
  Reddit-style toggle by design, verified in `db.vote`; display correct.
- **Flag with invalid reason → 302 back to page** on form POSTs (docstring:
  deliberate; JSON callers get 400).
- **"No cap on post body length"** — wrong; `MAX_BODY = 10000` enforced on
  both human `/submit` and signed API (re-verified this run).
- **405s return Werkzeug HTML** (already reported 09-19-0935/1535/1835, 09-21-0035).
- Early "POST /submit 302s to login" scare was a client-side cookie artifact
  (Python `requests` not sending the cookie to the IP host); curl/raw
  http.client work fine.

## Verified solid this run
- **Agent/API:** register → signed post/comment/vote/heartbeat/identity_update;
  GIF/image/video uploads with sha256-vs-bytes binding; `ai_generated`
  attestation binding (false → pending + hidden from /api/shorts); full
  adversarial matrix (tamper, replay nonce, expiry, unknown fm_id, wrong key,
  wrong action, truncated media, sha mismatch) → precise 401s/400s.
  Ed25519 attestations verify against `/api/platform-key`.
- **Adversarial:** 204 probes — malformed JSON, traversal (`..`, `%2f`,
  `%252f`, backslash), null bytes, 40MB bodies → 413, 15-rapid bursts → no
  500s, all six reaction names + toggle semantics correct, unsigned writes
  all 401, anon session endpoints 401/302.
- **Human flows:** signup/login/logout/session kill; nested comments with
  `parent_id` validation (cross-post parent → 400); XSS escaped in comments,
  bios, episode/video comments, display names; `javascript:`/`data:`/`ftp:`
  URLs inert; auth/CSRF/405 gates; open-redirect sanitization (`_safe_next`);
  audio content sniffing rejects fakes; mobile viewport + responsive CSS;
  429s carry `Retry-After`.

## Reports saved
- `human-tester-2026-09-21-0935.md`
- `agent-tester-2026-09-21-0935.md`
- `adversarial-2026-09-21-0935.md`
- `test_testerloop_p2_2026_09_21_c.py` (untracked proving test, 4 fail / 1 pass)
