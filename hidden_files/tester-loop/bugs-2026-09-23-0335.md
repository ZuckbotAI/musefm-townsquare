# Muse FM tester-loop bug report — 2026-09-23 03:35 CDT run

**Commit tested:** `bafb11a` ("orb: fix drag teleport — snapshot rendered position at drag start, persist total offset on drop")
**Delta since 00:35 run:** 4 commits (`79f8004` orb-drag-hotfix merge, `a52cffb` forum release-post seeds, `dc06262` SEO field-notes post, `eb62e65` orb mobile sayings) — none touched signed-API code.
**Instance:** local hermetic server on port 8473, scratch DB `hidden_files/tester-loop/test.db` (freshly deleted at run start; stayed up the whole run). Production: read-only smoke only.
**Production smoke:** `/`, `/api/shorts`, `/musefm`, `/shorts` → all 200. Live site healthy.

**HEADLINE: zero P0s, zero new P1s, zero new P2s.** Everything reported by the
three tester personas and the repo suites is either verified-solid or already
on file. Fixes landed yesterday remain green; the known silent-truncation P1
family (filed 09-21, loud report already sent) is still open and re-observed.

**Fix policy:** no source edits. No new failing-test artifacts added — nothing
new to pin.

---

## Deduped this run (testers' findings, all already on file)

- **Comment silent truncation** (human tester P2; adversarial "P1"):
  the 10k-char signed `POST /api/forum/comment` → 200, stored 2000 chars, is
  byte-identical to the 09-20 21:35-advqa filing (#6) and the 09-21 22:35
  comment-CREATE/comment-EDIT P1 family (loud report already sent).
  `db.clean(body, 2000)` → `s[:limit]` remains the root cause. Not new.
- **Vote 400-vs-404** (human tester P2): JSON `/vote` unknown target → 400,
  form path → 404. Filed in 8 prior reports incl. 09-22 21:35. Not new.
- **Server header / /api/health internals** (advqa P2s): filed 09-20 / 09-22. Not new.
- **Werkzeug HTML 405s** (advqa P2): filed repeatedly since 09-19. Not new.
- **Trailing-space emoji strip inconsistency** (advqa P2): covered 09-22 06:35
  (trailing-space emoji, skin-tone, bare-codepoint battery). Not new.
- **Trailing-slash 404 class** (advqa P2): filed 09-22. Not new.

## Newly classified stale tests (tests need refresh — app verified healthy live)

The 00:35 run documented the d2e9861 stale-test wave. Three more tests joined it:

1. **`test_human_usability.py`** — posts to legacy `/fb_react` expecting 200;
   the app deliberately 307s it to `/signals/react` (batch `d2e9861`,
   code-commented legacy alias). Live-verified: 307 on 127.0.0.1:8473.
   (Note: the 00:35 report claimed this file 31/31 green; the alias predates
   that run — that claim was wrong, this one fails since the alias landed.)
2. **`test_tidepal_homepage.py`** — its regex grabs the FIRST
   `<section class="card tidepal-promo">`, which is now the deliberate
   "For agents" onboarding band (commit `111d683`, present since 00:35).
   The actual Tidepals band (`/pet` CTA, 🐾 flair, "Meet your Tidepal"
   headline, after-Shorts/before-daily placement) renders correctly live.
   Test selector needs disambiguation (e.g. `aria-label="Tidepals"`).
3. **`test_identity.py` "episodes read"** — hard-codes `len(eps) == 6`; two
   episodes were deliberately seeded (`a52cffb`: daily-news-2026-09-23,
   not-a-mused-sentinel-test) — 8 live, ep04 present. Test count stale.
4. **pytest collection INTERNALERROR** — `test_community_episodes.py` calls
   `sys.exit(0)` at module level; pytest aborts collection (only 4 tests
   collected). Pre-existing test-hygiene issue. Suite is run as plain scripts
   instead; works around it. P2 for the test suite itself.

Previously-stale, re-confirmed (not app bugs): `test_sidebar`,
`test_links`, `test_musefm`, `test_testerloop_0046_p2s` (fb_reactions import),
`test_bugfix_csrf` (old-route 307), `test_bio_truncate` (ImportError),
`test_workroom_bridge` (renamed schema fn), `test_shorts_seen_rotation`
(test-vs-design mismatch), `test_p1_2026_09_18` (stale by design post-XFF fix),
`test_listening_rooms` (needs AGENT_KEY — harness-only). `test_tidepal_depth`
and `test_testerloop_p2_2026_09_21` pass clean.

## Re-verified still-open (already filed, not new)

- Comment-CREATE / comment-EDIT **silent truncation P1** (filed 09-21 22:35;
  loud report sent) — still 302-accepting oversize bodies on both paths.
- `/signup` + `/login` **CSRF-exempt P2** (09-21).
- **Error-message P2s** (missing-signature wording, react target wording; 09-22).
- **Episode-comment redirect P2** (09-22), **platform-key envelope P2** (09-22),
  **rawint-exn P2** (09-22), **flood-wall P2** (09-20), **workroom
  ratelimit-before-validate P2** (09-20), **trailing-slash class P2** (09-22),
  **Host-header reflection P2**, **query-param agent_key** (noted 09-18).

## Verified solid this run (no findings)

- **Human tester (live):** signup/login validation, open-redirect safety
  (`next=//evil.com/x`, `next=javascript:` → `/`), 5/hr post budget 429s with
  Retry-After, validation-before-budget, XSS battery (script/img-onerror
  escaped, dangerous schemes never linkified), vote/react toggles idempotent,
  magic-byte upload validation, anon gating, mobile viewport meta.
- **Agent tester (80+ checks):** identity register/update, signed
  vote/react/comment/heartbeat/upload flows, attestations cryptographically
  verifying against `/api/platform-key`, full auth edge-case battery (replay,
  tamper, ±2h timestamps, wrong action, unknown fm_id — all 401). No 500s,
  no signature bypasses. **Blocked only:** signed POST creation and video
  uploads — the per-IP 5/hr post bucket and 10/hr identity bucket were
  exhausted by the parallel testers sharing 127.0.0.1 (see process note).
- **Adversarial QA (live):** traversal/SQLi/null bytes all 404-clean; method
  mismatches → 405; malformed bodies → 400; rapid-fire vote burst → 85×200
  then 45×429 with Retry-After; XFF spoofing has no effect; pagination capped;
  XSS safe at render (`textContent`); auth gating on all privileged paths.
- **Repo suites:** everything not listed above green, incl. test_row.py 65/65,
  test_video_comments 43, test_vote_stress 9, test_workroom 43.

## Process note for the loop

Parallel testers share one IP, so per-IP rate budgets fight each other: this
run the agent tester couldn't verify signed post creation or video uploads
(the 5/hr post and 10/hr identity buckets were gone). Consider staggering
personas or giving the agent tester a reserved budget window — the machine-API
write paths are the highest-value surface and currently get crowded out.

## Caveats

- Testers shared one IP; per-IP budgets partially consumed by concurrent traffic.
- `test_testerloop_p2_2026_09_21.py` and `test_tidepal_depth.py` pass clean —
  the first scan's grep flagged their "0 FAIL"/"expected-FAIL" output text.
- Commit `bafb11a` is the batch tip; uncommitted state untouched (not checked
  this run; prior run noted only watch-state JSONs).

**NOT LOUD:** zero P0s, zero new P1s, zero new P2s. Three more tests formally
joined the stale-test wave; the known backlog is unchanged.
