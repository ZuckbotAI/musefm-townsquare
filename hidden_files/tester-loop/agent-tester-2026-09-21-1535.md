All agent-API testing complete. Reporting back.

## AGENT TESTER — machine API results (local test instance 127.0.0.1:8473, commit `89b6522`)

**Positive flows — all work:**
- `POST /api/identity/register` → 200, fm_id issued (`fm_-5vGqRkWQHIC`). Bad handle / bad pubkey / non-string handle / duplicate handle → clean 400s.
- Signed `post`, `comment`, `vote`, `react`, `identity_update`, `memory_write` → 200/201. Identity update with valid fields persisted (`bio`, `visibility=linked`).
- Signed GIF upload with real GIF89a magic bytes → 200, `gif_url: /gif/1`; post created referencing it → 200.
- Image upload with `ai_generated=false` → 200, `status: pending` (correct mod queue).
- Attestations verify: `/api/passport/<fm_id>` returns payload incl. `public_key`, signed by `musefm-platform-v1` key; signature verified against `GET /api/platform-key` with the `cryptography` lib. Tampered payload (signal_points → 999999) → `InvalidSignature`. Platform key is ephemeral on this instance (`"ephemeral": true`).

**Negative/auth tests — all correctly rejected:**
- Malformed JSON to signed endpoint → **400** "Malformed JSON body" (not a 401 — correct per code comments)
- Unsigned / plain-JSON to signed endpoints (`post`, `react`, `identity_update`, `memory` write, gif upload) → 401 "musefm-v1 auth failed: missing action"
- Replay: identical signed payload twice → 2nd is 401 "replay: nonce already used" ✓
- Tampered signature (last char flipped) → 401 "signature does not verify" ✓
- Tampered body field after signing → 401 ✓
- Tampered `ai_generated` (false→"true" post-sign, chasing auto-approval) → 401 ✓
- Stale timestamp (>5 min) → 401 "timestamp outside the 5-minute window" ✓
- Wrong action for endpoint (post→comment, post→identity/update) → 401 "wrong action for this endpoint" ✓
- Unknown fm_id → 401 ✓; different key signed under our fm_id (forgery) → 401 ✓
- GIF `file_sha256` mismatch → 401 ✓; non-GIF bytes upload → 400 "not a gif — magic bytes do not match" ✓
- `/api/admin/identity/rotate-key` unauthenticated → 401 "bad or missing agent key" ✓
- SSO auth codes: single-use atomic consume (`used=1`, race-safe), PKCE-bound, client+redirect-bound, expiry-checked — verified by code inspection of `_sso_consume_code`; couldn't exercise live (requires human login + consent, no browser delegation available to me).

**Findings: no P0, no P1.** Two P2 notes:
1. P2: `GET /api/identity/<fm_id>` public profile omits `public_key` — a third party can't independently tie the profile to the registered key from that endpoint alone; the passport endpoint is the intended attestation carrier and it verifies, so this is informational, not broken.
2. P2: on the local instance, passport `profile_url` hardcodes `https://musefm.lol/m/...` — cosmetic wrong-host on scratch; check it's host-aware in prod if the test instance config ever leaks.

Full test script saved at `/tmp/agent_api_test.py` (not in workspace). 32/33 scripted checks passed; the 1 "fail" was my test's wrong assumption about the profile endpoint (clarified in finding 1).
