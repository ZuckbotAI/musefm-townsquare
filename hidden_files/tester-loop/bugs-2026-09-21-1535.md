# Tester-loop triage — 2026-09-21 15:35 CDT run
**Commit:** `89b6522` ("Fix tester-loop P2 (2026-09-21 12:35 run): /login validate-first"),
branch `workroom-bridge-2026-09-21` (current checkout tested as-is; no branch switch, no commits).
Working tree has unrelated uncommitted changes (muse-orb.js, test_brand.py, workroom.py,
grok watch state) — none touched by this run.
**Target:** local instance `http://127.0.0.1:8473`, fresh scratch DB
(`hidden_files/tester-loop/test.db` deleted at run start; real `data/townsquare.db`
verified untouched — mtime 00:17 UTC, 0 bytes).
**Production smoke (read-only):** `/`, `/api/shorts`, `/musefm`, `/shorts` → all 200. Live site healthy.
**Repo suites (run directly against this checkout):**
- `test_auth` 62/62, `test_human_usability` 64/64, `test_identity` 41/41,
  `test_shorts` 78/78, `test_uploads` 24/24, `test_comments_pro` 60/60,
  `test_musefm` 77/77, `test_pets` 101/101, `test_sso_provider_2026_09_21` 31/31,
  `test_gifs` 48/48, `test_vote_stress` 9/9 — all green.
- `test_sidebar` 107 pass / 9 fail — the 9 are the stale `/musefm/shorts` link
  expectations recorded as own-suite staleness in the last 48h bug files (harness, not app).
- `test_testerloop_p2_2026_09_21_c.py` now **5/5 pass** (was 4 fail / 1 pass at 09:35) —
  the 09:35 P2s it proved are fixed on this tree.
- `test_testerloop_1835_fixes_2026_09_19.py`: script harness 25/25 pass; pytest
  collects 5 fns — 2 pass, 3 fail (`test_scunthorpe`, `test_edit_echoes_stored_body`,
  `test_comment_body_cap`) — the stash-confirmed pre-existing trio, unchanged.
- **`test_testerloop_login_validatefirst_2026_09_21.py`: 2 passed, 5 FAILED** —
  see INTEGRITY FLAG below.

## ⚠️ INTEGRITY FLAG (process, not product)
Commit `89b6522` claims "Fix tester-loop P2 (2026-09-21 12:35 run): /login
validate-first" with "Proving: test_testerloop_login_validatefirst_2026_09_21.py 7/7".
**The commit changes ONE file — the test itself (89 insertions, 0 app.py lines).**
`app.py`'s `login()` (app.py:5279) still calls `rate_limit_message("human_login", 10)`
*before* reading/validating the handle and password. The underlying bug (blank POSTs
burn the 10/hr `human_login` budget → login-form DoS for everyone behind one NAT IP)
is UNFIXED, and its own proving test fails 5/7 on the committed tree (this run,
live-verified: 11 blank POSTs → [401 ×10, 429], subsequent well-formed login → 429).
The 7/7 claim in the commit message is false. The bug itself stays filed as the
12:35-run P2 below; Anthony's next fix pass should re-do the validate-first reorder
for real.

## P0 — crash / data loss / security hole
**None.** All three personas + adversarial battery agree; zero tracebacks in server.log.

## P1 — broken feature
**None new.** The human tester's P1 claim (comment silent truncation at 2000 chars) is a
deduped known P2 — see below. No brand-new P1 this run.

## New P2s this run (not in last 48h bug files)
1. **Signed `/api/forum/react` burns the react/vote rate budget on invalid emoji /
   unknown target despite code comments claiming otherwise** — code comments on
   `api_react`/`api_vote` say bad target_id/emoji 400 "without burning the shared
   per-IP budget", but only non-integer target_id (via `_int_field`) avoids the
   budget; valid-shaped-but-invalid emoji or unknown target still record a hit in
   `check_limit("react", 120)` before `db.react` raises. Repro (any valid signed
   identity): 130 signed `action=react target_id=13 emoji="nope_not_emoji"` POSTs →
   120× `400 "emoji must be one of: ..."` then 10× `429 rate limit hit — slow down, friend`.
   Self-DoS only (buckets per REMOTE_ADDR, X-Forwarded-For ignored) → P2. Fix: move
   emoji-whitelist + target-existence checks before `check_limit`, or correct the comment.
   (adversarial QA; live-proven. Note: the *human*-route variant — `POST /vote`
   `check_limit` before the auth gate — was already a KNOWN P2 from the 0035 run; this
   is the *signed*-route comment-behavior-mismatch variant.)
2. **`og:url` / `og:image` hardcode the production domain on every page** — all pages,
   including the local test instance and the branded 404 page, emit
   `og:url https://musefm.lol/...` and `og:image https://musefm.lol/static/img/og-image.png`.
   Verified on `GET /c/lobby/post/13` and the 404 page. Same class: passport
   `profile_url` hardcodes `https://musefm.lol/m/...` (agent tester). Share links from
   any non-prod deployment advertise prod URLs. (adversarial + agent tester; new)
3. **`/fb_react` with missing `target_id` reports `"unknown target"`** — misleading
   message; should name the missing/invalid param. (human tester; new, wording nit)

## New instance of a known class
- **`/forum/` (trailing slash) 404s while `/forum` works** — instance of the known
  trailing-slash path-normalization P2 (0035 run filed `/api/forum/react/`). (human)

## Known / already-recorded findings (deduped, not re-reported as new)
- **`/login` burns the 10/hr `human_login` budget before field validation** (filed
  12:35 run as P2-1 candidate) — STILL LIVE; the claimed fix in 89b6522 never landed
  (integrity flag above). Repro: 11× `curl -X POST --data "handle=&password=" .../login`
  → 429 with `Retry-After`.
- Comment bodies silently truncated to 2000 chars on all four comment surfaces
  (forum/episode/video/agent API) — pre-existing P2; the human tester's P1 argument
  (silent loss with success response, posts already 400 on overlong bodies) is noted
  but the triage rating stays P2 per the last 48h record. Root cause per tester:
  routes guard `if not clean(body, 2000)` but `db.clean()` truncates (`s[:limit]`),
  so the check only catches emptiness; `db.create_comment` re-truncates.
- Vote-on-unknown-target: JSON → 400 vs form → 404 inconsistency (KNOWN P2).
- Per-IP shared `post` (5/hr) bucket between human `/submit` and signed
  `POST /api/forum/post` (recorded 09-20 all runs, 09-21-0035).
- Comment double-submit has no idempotency (filed 2026-09-18 human #5).
- `test_sidebar.py` 9 failures — own-suite staleness (harness, not app).
- `test_scunthorpe` / `test_edit_echoes_stored_body` / `test_comment_body_cap`
  pytest failures — pre-existing, stash-confirmed 09:35.

## Verified NOT bugs this run
- **`/api/identity/<fm_id>` public profile omits `public_key`** (agent tester P2-1) —
  by design; the passport endpoint is the attestation carrier and its Ed25519
  signature verifies against `/api/platform-key`. Informational only.
- Agent tester's `/tmp/agent_api_test.py` 33rd check "failure" was the tester's own
  wrong assumption about the profile endpoint, not the server.
- Adversarial's first react/vote/flag probes used wrong field names
  (`reaction`/`direction`/`flag`) — client errors, re-run corrected.

## Verified solid this run
- **Agent/API:** register → signed post/comment/vote/react/identity_update/memory_write;
  GIF/image uploads with sha256-vs-bytes binding; `ai_generated=false` → pending mod
  queue; replay/tamper/stale-timestamp/wrong-action/unknown-fm_id/forgery/sha-mismatch
  → precise 401s/400s; SSO codes single-use atomic, PKCE/redirect/expiry bound
  (code-verified; live exercise needs human consent). Platform key ephemeral locally.
- **Adversarial:** 404s clean, traversal dead, /admin /debug /console → 404,
  unsigned admin → 401, session gating 302s, 8-emoji + 6-name reaction sets enforced,
  votes ±1 only with atomic toggle semantics, flag reasons whitelisted, registration
  hardened (SQLi/dup/bad pubkey → 400), XSS entity-escaped, open redirects blocked,
  CRLF-safe, no key/email/IP leaks, claim-human keypair fresh, games unauth → 401.
- **Human flows:** signup/login/logout/session-kill; comment edit own-OK/other's-403;
  race-safe voting (10 parallel upvotes, no double-count); title 200/201 + body
  10000/10001 boundaries correct; upload magic-byte checks; slur filter word-boundary
  safe; mobile UA renders; episode audio streams; signup rate exactly 5/hr with
  failures not burning budget; 429s carry Retry-After.
- Caveat (adversarial): rate buckets are per-IP and all loops run from 127.0.0.1 —
  sibling runs can exhaust shared buckets (`post` 5/hr); 429s in shared-IP testing
  need the source-IP sanity check before filing.

## Reports saved
- `human-tester-2026-09-21-1535.md`
- `agent-tester-2026-09-21-1535.md`
- `adversarial-2026-09-21-1535.md`
- No new proving test added: the /login bug is already proven by the (failing)
  `test_testerloop_login_validatefirst_2026_09_21.py` in the tree; no source edits
  were made (fix policy).
