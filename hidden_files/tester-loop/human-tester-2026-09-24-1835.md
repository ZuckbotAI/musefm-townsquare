# Human tester report — 2026-09-24 18:35 CDT run

COMMIT: `6661f91 Pet backend cutover: canonical pet store + village key unification`
(new since the 15:35 run, which was on `961f41f`)

Tester persona: curious, slightly mischievous first-time human. Session: signed up as
`MischiefTester9` (`fm_T1Zb2_i0iTLV`), logged in via cookie jar. All writes to the
scratch DB at `hidden_files/tester-loop/test.db` on `http://127.0.0.1:8473`.
Production untouched (read-only smoke is the coordinator's job). No app source edited,
no commits, no branches switched.

Headline: **0 P0, 0 new P1, 1 new P2.** The 15:35 P1 is confirmed FIXED in this tree.
Everything else I poked behaves; the only drama was environmental (test server died
mid-run again, session invalidation explained the one moment I thought votes were
broken).

---

## Bugs

### P2 — Double-submit creates two identical posts (no dedupe on /submit)

- SEV: P2 (nit/polish; same class as the filed comment-burst P2, but on the
  human web path)
- REPRO: logged in, POST /submit twice with identical title/community/body
  (valid CSRF each time) → 302 to /c/lobby/post/17, then 302 to
  /c/lobby/post/18; two distinct rows in `posts` with identical titles.
- EXPECTED: second rapid duplicate either rejected or idempotently collapsed
  (accidental double-click is the classic trigger).
- ACTUAL: two posts created, ~1s apart. Sibling: adversarial tester's P2-3
  covers the signed-API comment-burst variant; this is the web-form post
  twin — new facet, new filing.

## FIXED since 15:35 (verify, don't re-report)

- **15:35 P1 "long `q` → 500" is FIXED.** `GET /api/forum/posts?q=<60000 A's>` →
  **400** `{"error":"search query too long (max 2000 characters)","ok":false}`,
  and the web-UI twin `GET /c/lobby?q=<60000 A's>` → **400** "search query too long
  (max 2000 characters)". Length cap verified on both paths.

## What worked (no bugs found)

- **Signup/login/logout**: all flows correct (same as 15:35 — duplicate handle,
  bad email, short password, mismatched password, wrong-password login all
  produce the right 400/401/302s; logout kills the session).
- **Stored XSS, post title**: `<script>alert("XSS-TITLE")</script>` in title →
  escaped everywhere (`<h1>`, `<title>`, og/twitter meta). No raw `<script>`.
- **Stored XSS, comment body**: `<img src=x onerror=alert('XSS-body')>` +
  `<svg onload=alert(2)>` → fully escaped on render. Two raw-looking matches
  (`onerror=alert(`) in the HTML are inside escaped text (`&lt;img ... onerror=alert(&#x27;...)`),
  including the edit-comment textarea prefill — not exploitable.
- **10k-char comment body** → 400. (10k post body untested this run: shared
  per-IP post rate budget was burned by the parallel testers early on — my
  second post 429'd; the budget freed later and I posted /c/lobby/post/16.)
- **Votes**: 12 rapid +1s then flips — semantics CORRECT once I had a valid
  session: same-value → toggle off (row deleted), flip 1→-1 → row updated,
  single row per (target_type, target_id, handle). Final sequence verified:
  `-1 → [('MischiefTester9', -1)]`, `1 → [..., 1]`, `1 again → []` (toggle off),
  `-1 → [..., -1]`.
- **Reactions**: param name is `reaction` (not `signal`); invalid values → 400
  "reaction must be one of: lit, idea, kind, fire, build"; toggle add/remove →
  302s (JSON semantics verified by the 15:35 run).
- **Photo upload**: 16×16 test PNG → 302 `?pending=1` (human-review queue).
  Evil payloads rejected: `evil.svg` (image/svg+xml) → 400 "not a recognized
  image (png, jpeg, gif, webp)"; text file renamed .png → same 400. Content
  sniffing works.
- **Shorts**: `/api/shorts` → 200, empty items (seed present, total 0). No crash.
- **Episodes**: `/episodes` 200, `/episodes/ep04` 200, `/audio/ep04.mp3` → 200
  `audio/mpeg`.
- **Linkification (post 16)**: `javascript:alert(1)` and `data:text/html,...`
  NOT linkified; `https://example.com/page?a=b&c=d` linkified with
  `target="_blank" rel="noopener nofollow"`. Malformed `https://[::1/foo` left
  as plain text. Safe.
- **Pet backend cutover (the new commit) — happy path**: `POST /pet/adopt`
  (species=brine, name=Splashy) → 302 and created rows in BOTH `tidepals` and
  `row_pet_adoptions`, both keyed by `fm_id` (`fm_T1Zb2_i0iTLV`). The two stores
  are consistent — unification holds on the write path.
- **Pet name validation**: name `<img src=x onerror=alert("petxss")> Splashy`
  → rejected with flash "name must be 2–24 chars (letters, numbers, spaces,
  _ -) and stay classy". No row created. Species=bogus_species → flash
  "unknown species (choose: brine, bloop, ...)" — clear feedback, no row.
- **Vote param edge cases**: `target_id=abc` / `value=xyz` / `value=1.5` /
  `value=true` → clean 400s ("bad target_id/value: must be an integer", no raw
  exceptions); nonexistent post id and empty target_id → 404 "unknown target";
  empty `value` → defaults to 1 (302) per the documented default.
- **Post community validation**: `community=no-such-community` → 400 "unknown
  community" (before the rate bucket — text validation ordering holds).

## Observations (not bugs)

1. **Test server died mid-run AGAIN (~23:47 UTC).** Port 8473 went 000 with no
   traceback (server.log just ends). This is the second consecutive run with a
   mid-run death. Rebooted per procedure → 200. Cause unknown; my preceding
   traffic was a single /vote POST. Flagging for the coordinator in case the
   werkzeug dev server is unstable under the parallel tester bursts (server.log
   shows 3 "Running on" lines = 3 boot cycles this run).
2. **Session secret is per-boot**: after the restart, my cookie jar session was
   invalid, so `/vote` silently 302'd to `/login?next=/` and dropped votes
   (302 either way — the ONLY tell is the Location header). This briefly looked
   like "votes not recording" but is purely environmental. Not an app bug; just
   noting so no one files a phantom vote bug. Re-login + fresh CSRF (per-session
   CSRF tokens rotate on login) restored everything.
3. **Shared scratch instance**: the 5/hr per-IP post budget was burned by the
   parallel testers before I finished — my emoji-flood post attempt 429'd.
   Budget freed later in the run. Not a bug, just noting contention on the
   shared scratch DB.

## Dedup notes

- Read bugs-2026-09-24-1535.md and human-tester-2026-09-24-1535.md first. The
  long-`q` P1 is fixed (verified above, not re-reported). The gif_url rate-budget
  P2, missing-signature P2, comment-burst P2, and text/plain-as-JSON P2 are all
  filed already — I did not re-test those paths.
- Nothing in the last 48h of bug files covers vote-flip or pet-cutover
  behavior; both check out here, so no new filings.

## Caveats

- One mischief item not exercised: 10k-char POST body (rate budget was burned
  by parallel testers at the time; the 15:35 run already verified 12k → 400).
- Server left RUNNING on :8473 (I rebooted it; it's the shared run instance —
  the coordinator kills it at run end).
