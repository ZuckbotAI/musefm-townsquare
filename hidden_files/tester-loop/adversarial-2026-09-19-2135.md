# Adversarial tester — musefm-tester-loop 2026-09-19 21:35

**Commit:** `6adf330` · **Instance:** local `http://127.0.0.1:8473`, scratch DB
`hidden_files/tester-loop/test.db` · **Persona:** hostile form probing (curl)

## Findings

- **Double-submit creates duplicate comments — RE-CONFIRMED (known P2).**
  10 rapid identical `POST /post/10/comment` → 10 identical comment rows
  (DB ids 5–14, handle `advtester2`, all `created_at` 2026-09-20 02:41:47 UTC).
  No idempotency token / dedupe window. First reported 2026-09-19 03:35,
  re-confirmed 06:35. Not new.
- **100KB comment body stored as exactly 2000 chars** (DB id 17,
  `length(body)=2000`). Known P2 (silent truncation, reported 03:35) —
  re-confirmed, not new. Note: the signed API route now also caps at 2000
  (`app.py:2123`), so the 18:35 "no length cap on comment bodies" P2 is
  **fixed** in this commit.
- **CSRF: no bypass found.** Authenticated `POST /post/8/comment` without
  `csrf_token` → **403 "bad form token — reload and try again"** (verified live
  2026-09-20 05:0x UTC). Enforcement present in code on `/submit`,
  `/post/<pid>/comment`, `/vote`, `/episodes/<slug>/comment`,
  `/photos/upload` — the routes flagged in earlier reports. The commit's
  "CSRF form-token holes" fix is real and working.
- Signature/tamper probes on the human session cookie: Flask session is
  signed; forged cookies rejected (login required). No session fixation
  observed.

## Nothing new filed from this persona.

## Late handoff addendum (full tester report arrived post-write)
- Double-submit repro used **valid CSRF tokens** — no bypass; the 302s were
  successful tokened creations (the known P2 manifesting).
- New from this handoff, filed in main report: **P2-3** malformed JSON →
  misleading 403 on `/fb_react`/`/vote` JSON paths; **P2-4** no security
  headers on any response.
- Independently verified solid: vote toggle race-safe (11 concurrent
  same-user upvotes → score 1), FB reaction validation (400s), method-misuse
  405s, clean 404s, no reflected XSS in `?q=`/`?x=`, path traversal blocked,
  workroom authZ (unauth create/claim/done → 401).
