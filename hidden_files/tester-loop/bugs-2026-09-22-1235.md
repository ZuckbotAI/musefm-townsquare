# Muse FM tester loop — 2026-09-22 12:35 CDT run

**Commit tested:** `3f8d6ba` ("Tidepals page: add 'rework coming soon' banner at top of /tidepals")
**Working tree:** dirty — pre-existing uncommitted mods to `agent_guide.txt`, `app.py`, `db.py`, `grok_bot_watch.py`, `hidden_files/grok_watch_state.json` (not made by this run; no source changed by this run).
**Instance:** LOCAL ONLY — http://127.0.0.1:8473, scratch DB `hidden_files/tester-loop/test.db` (deleted + recreated at run start).
**Production smoke (read-only):** `/`, `/api/shorts`, `/musefm`, `/shorts` → all 200 ✓.

## Verdict

**No P0. No brand-new P1.** Zero new findings this run — the two headline items from the
human tester both dedupe against already-open P1s (re-verified LIVE below, still awaiting fix).

## NEW findings this run

None. The human tester's two "NEW" items dedupe as follows:

1. *"NEW P1: login session cookie Secure on plain-HTTP local instance"* → **ALREADY KNOWN P1
   (filed 2026-09-20).** `app.py:361`: `SESSION_COOKIE_SECURE = not app.debug`; the loop boots
   with debug off, so the session cookie carries `Secure` over plain HTTP and spec-compliant
   clients (urllib cookiejar, real browsers) never send it back. Re-demonstrated this run via
   source read (server log: "Debug mode: off"; `app.debug` False → flag True). Fix exists as
   held-branch commit `6b46823` ("Secure cookie gated on request scheme") — **still NOT merged
   to master**. Re-verified LIVE, awaiting fix.
2. *"NEW P2: client-supplied X-Forwarded-For rotates the per-IP rate-limit bucket on direct
   connections"* → **ALREADY KNOWN P1 (filed 1535 09-21, LIVE per 1835 note, awaiting fix).**
   `ProxyFix(x_for=1)` at `app.py:118` trusts the client-supplied header when the app is hit
   directly; `app.py:112-116` comments claim the header is "untrusted and ignored" — true only
   behind Render's edge, false for direct-origin access. Re-demonstrated this run by the human
   tester (429 `Retry-After: 3251` on `human_login`, then 302 success with `X-Forwarded-For:
   9.9.9.77`). Re-verified LIVE, awaiting fix. (Tester also flags the shared-bucket harness
   pain: parallel testers burn each other's 10/hr login budget — consider a harness-exempt
   header for the loop.)

## Repo suites (run by lead against this checkout)

- `test_sidebar.py` (script harness): **118 passed, 0 failed**.
- `test_identity.py`: 40 passed, **1 failed** — "100th registrant still pioneer": **known-stale test**
  (filed in 09:35 report; `db.py:487` `PIONEER_COUNT = 25`, test still expects 100). Source value
  dates to the original commit; the test was never updated when the threshold changed.
- `test_gifs.py`: 48/48 · `test_shorts.py`: 78/78 · `test_uploads.py`: 24/24 ·
  `test_auth.py`: 62/62 · `test_comments_pro.py`: 60/60 · `test_vote_stress.py`: 9/9.
- Note: the whole suite is script-style (`check()` harness) — `pytest` collects **0 tests** from
  these files; run them as `python3 <file>`. (Repeated note from prior runs; not an app bug.)

## Agent tester (machine API) — 0 new findings

~30 signed-flow cases all correct: register (`fm_hXJx71aiuGOl`), duplicate-handle 400,
signed post/comment/vote, GIF/PNG/MP4 uploads (magic-byte + sha256 validation), platform
attestation envelopes verify against the published platform key (`musefm-platform-v1`,
`ephemeral: true`). Rejections: tampered sig → 401, replayed nonce → 401, stale ts → 401,
short nonce → 401, malformed JSON → 400, off-whitelist `gif_url` → 400. Spot-verified by lead
(platform-key envelope, malformed-JSON 400, unauth `trustline/status` 401, garbage-path 404).

## Adversarial QA (lead re-ran after agent's report text failed to deliver)

- Open redirect: closed (`_safe_next` rejects absolute/scheme-relative/backslash URLs — verified live).
- Reflected XSS via `/login?next=`: escaped (`&#34;&gt;&lt;script&gt;`) — verified live.
- Garbage paths → 404; unauth non-public hits → 401/405; wrong method → 405; huge query string → 404.
- `limit` params: capped everywhere (uncapped parse in `api_events` is clamped by
  `poll_events` to 200 — defense-in-depth holds; `/api/shorts` page-based with `min(...,50)`).
- 20× rapid requests: no 429 on public paths, no degradation; `/../..` normalizes to `/`.

## Human tester clean confirmations (this run)

Signup/login/logout round-trip, wrong-password → 401, XSS battery fully escaped in
titles/comments, linkify sane (`javascript:`/`data:` not linkified), body/title/comment length
boundaries enforced (10000/200/2000), vote toggle + range validation, reaction allowlist,
upload mod-gate ("pending mod review"), `/watch` + audio range requests, mobile viewport meta +
19 `@media` queries, branded 404s, Tidepals rework banner present, zero tracebacks in server.log.

## Already-known, re-observed (not re-reported)

- Episode-comment redirect P2 (09-22 09:35), platform-key envelope P2 (09-22 06:35),
  comment silent-truncation P1 (09-21 22:35 — did NOT reproduce on the 2000-char comment path
  this run; left as filed), `/signup`+`/login` CSRF-exempt P2, trailing-slash class P2,
  comment double-submit P2, missing-signature "bad signature encoding" P2.

## Notes for triage

- Two P1s are LIVE and awaiting fix: Secure-cookie-on-local (fix held in branch `6b46823`,
  unmerged) and XFF rate-limit bypass (1535 P1). Both re-proven this run.
- Both subagent "final report" payloads failed to deliver through the normal handoff channel
  this run (only the meta "already delivered" text arrived); the human-tester report was
  recovered from its session log. Adversarial findings were independently re-proven by the lead.
- No new failing-test files added this run (fix policy allows; no new bugs to pin).
