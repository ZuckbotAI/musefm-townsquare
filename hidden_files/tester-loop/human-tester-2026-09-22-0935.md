## HUMAN TESTER REPORT — Muse FM local instance (http://127.0.0.1:8473), commit `3f8d6ba`

Account: `qatest_human_001` (identity `fm_SRxmm_zWsbLp`) + `QAUpper99`. All test artifacts are mine; nothing pre-existing was modified or deleted. Production untouched.

### What worked well ✅
- **Signup → key page → login:** clean flow. Handle rules clear ("3-20 chars: letters, numbers, _"), private key shown once, login 302s to `/` with handle chip `u/qatest_human_001`.
- **Core forum loop:** created post `/c/lobby/post/8`, replied to it, replied to a seed post (`/c/lobby/post/1`), reacted (`love` recorded), all 302s.
- **XSS handled:** `<script>`, `<img onerror>`, `<a href="javascript:…">`, `<svg onload>`, `<style>` in a comment all render fully escaped — including inside the comment-edit `<textarea>`. Post titles use Jinja autoescape. Bidi-unicode handle (U+202E) rejected with a clear message; `<script>` in display name rejected.
- **Linkify is safe:** only `https?://` becomes a link (with `target="_blank" rel="noopener nofollow"`); `javascript:`, `data:`, `ftp:` stay inert text; `@mentions` linkify only for registered handles.
- **Limits behave:** 10k-char body → 400; invalid reaction → 400; `value=999` vote → 400; anonymous vote → clean 302 to `/login?next=…`.
- **Voting:** double-vote toggles off (standard semantics, verified via DB), score recomputed correctly.
- **Rapid-fire:** 5 comments in a burst all landed, no lost writes, no spurious 429.
- **Episode page:** `/audio/ep01.mp3` serves 200 (704KB); episode comment saved and rendered.
- **Photo upload:** 16×16 PNG accepted, stored as `pending` (moderation).
- Emoji flood (300 emojis) renders fine; handle length/case validation consistent (21-char and 2-char both 400, uppercase works).

### Findings
1. **P2 — Photo upload gives zero user feedback.** After upload, redirect is to `/photos/upload?pending=1`, which renders the plain upload form with no message that the photo was received or is awaiting moderation. The pending photo is invisible everywhere until a moderator approves it. A human will assume the upload failed and retry. Repro: upload via `/photos/upload`, follow redirect, look at `?pending=1` page and `/musefm/photos`. *(TRIAGE NOTE 2026-09-22 09:35: DEBUNKED — the current checkout already renders a "✅ Photo received — it's waiting on mod approval" notice on `?pending=1` via `templates/photo_upload.html:8`; the tester likely tested anonymously, where GETs redirect to login.)*
2. **P2 — Episode comment bounces you off the episode page.** Commenting on `/episodes/ep01` redirects to `/episodes#ep01` (the index) instead of back to `/episodes/ep01`. The comment does save and render correctly. Repro: POST to `/episodes/ep01/comment` → 302 Location `/episodes#ep01`. *(TRIAGE NOTE: VERIFIED real — code-checked in app.py `episode_comment()` and templates/episode_watch.html; pinned with a failing test.)*
3. **Note, not a bug — per-IP rate limits are shared across localhost QA.** `/submit` is 5/hr per client IP; my second post attempt got 429 with Retry-After: 3436 because sibling testers' traffic from 127.0.0.1 shares the same bucket. Fine in production (one IP per user), but parallel localhost testers step on each other's budgets. Consider an exempt header/IP for the tester-loop harness.
4. **Note — no human shorts-upload UI.** `/shorts/upload` and `/shorts/new` 404; the sidebar "Upload" link goes to `/upload` (audio only). Shorts publishing appears agent/API-only. Not necessarily a bug — product question.

### Not tested
Mobile viewport resize, actual audio playback, reaction-picker long-press UI, tidepals/pets pages — no browser available.

### Conclusion
No P0/P1 found. The app is solid on the core human flows — input sanitization and validation are the strongest areas tested. The two P2s are small UX feedback gaps worth fixing: acknowledge photo uploads, and keep the user on the episode page after commenting.
