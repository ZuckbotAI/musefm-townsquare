# Muse FM tester-loop triage — 2026-09-21 18:35 CDT run

Tested commit: `395c80b` "Fix tester-loop P2s (2026-09-21 15:35 run)" (verified via `git log --oneline -1`).
Instance: local only, `python3 app.py --port 8473 --db hidden_files/tester-loop/test.db`
(fresh scratch DB; `data/townsquare.db` untouched). Production smoke: read-only GETs on
https://musefm-townsquare.onrender.com → `/`, `/api/shorts`, `/musefm`, `/shorts` all **200**.

Fix policy honored: no app source edits. No new test files added this run (no new bug to prove).

## P0 — crash / data loss / security hole
**None.** Adversarial battery (404s, traversal incl. %2e%2e/double-encoding, malformed JSON,
unauth hits on all write/upload/admin paths, PUT/DELETE/PATCH → 405, 40MB → 413, bursts)
found zero tracebacks, zero stack-trace leaks, zero auth bypasses. Zero tracebacks in server.log.

## New P1
**None.** Every finding this run dedupes against the last 48h of bug files.

## Known findings re-observed (deduped, not re-reported)
- **P1 (filed 2026-09-20): `SESSION_COOKIE_SECURE` emitted on plain-HTTP local instance.**
  Still present on master (`app.py:357`: `SESSION_COOKIE_SECURE = not app.debug`; loop boots
  debug=False). Human tester re-demonstrated: strict HTTP clients (Python urllib cookiejar)
  drop the Secure cookie over `http://127.0.0.1:8473`, so session flows are untestable via the
  prescribed harness. Fix exists as held-branch commit `6b46823` ("Secure cookie gated on
  request scheme") — NOT merged to master.
- **P2 (filed 12:35 run): `/login` burns the 10/hr `human_login` budget before field validation,
  and successful logins also burn budget.** Human tester re-demonstrated: after ~10 mixed
  POSTs (~5 min), a *valid-credential* login → **429, `Retry-After: 3323`**. The same lockout
  blocked the rest of the human tester's flow (post/comment/vote/react/upload/shorts/episodes/
  mobile/adversarial inputs untested this run — coverage gap, not a bug).
- **P2 (pre-existing, noted 15:35 run): oversized prose silently truncated, never rejected.**
  Adversarial re-demonstrated: 10k-char post body → 200, stored cut to 2,000 by `db.clean()`;
  same for 5k-char comments. No signal data was lost. Longstanding `clean()` design; flag
  reasons hard-400 by contrast. Triage rating stays P2.

## Verified-fixed this run (closing evidence)
- `395c80b` P2-1: 140× invalid-emoji reacts → 140× 400, zero 429s (`api_react`/`api_fb_react`
  validate before touching the rate budget). Adversarial-probed, hermetic fresh IPs.
- `395c80b` P2-2: `og:url`/`og:image` now request-derived on homepage, share pages, 404 page —
  no more hardcoded `https://musefm.lol`.

## Suite results (repo's own tests, run standalone as designed, TOWNSQUARE_DB scratch)
- `test_sidebar.py`: 107 pass / **9 fail — all the `/musefm/shorts` sidebar link, which commit
  `7caa312` *deliberately* dropped** ("Sidebar: drop duplicate FM Shorts link; station clips
  still linked from /musefm hub" — hub links verified live on the test instance). **Stale test
  expectations, not a product regression.** Test file needs updating to match 7caa312.
  (Also noted: these check()-style files are NOT pytest-safe — under pytest `setup()` never
  runs and check() failures are invisible; standalone + TOWNSQUARE_DB is the correct invocation,
  per app.py's own docstring. `test_testerloop_1835_fixes_2026_09_19.py` went 25/25 standalone.)
- `test_testerloop_1835_fixes_2026_09_19.py`: 25/25 pass.
- `test_testerloop_p2_2026_09_21{,_b,_c,_1535}.py`, `test_testerloop_login_validatefirst_2026_09_21.py`,
  `test_brand.py`, `test_sso_provider_2026_09_21.py` (31), `test_pet_onboarding.py` (49): all pass.
- pytest batch (`test_pets`, `test_shop_proxy`, `test_row`, `test_sso_provider`, `test_pet_onboarding`): 65 pass.

## Persona results
- **Agent tester:** 22/22 — identity register, signed post/comment/vote, signed image upload,
  attestation re-verification client-side; all failure modes (bad sig, replay, stale timestamp,
  wrong action, tampered credential) correctly rejected. No bugs. Caveat: client reused
  identity.py helpers, slightly weakening independence.
- **Human tester:** partial — signup/login/CSRF verified; blocked mid-run by the known
  `human_login` 429 lockout (see above). Re-demonstrated the two known findings; no new bugs.
- **Adversarial QA:** full battery clean; one new-but-duplicate P2 (silent truncation, already
  filed). Both `395c80b` fixes verified.

## Bottom line
No P0, no new P1, no new P2. All findings already on file. Staying silent per reporting policy.
