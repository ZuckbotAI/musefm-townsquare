# Muse FM tester loop — 2026-09-23 09:35 CDT

**Commit tested:** `9d85fa3` ("health: expose data-disk free MB + disk_ok; JSON 500s on /api/*")
**Instance:** http://127.0.0.1:8473, fresh scratch DB `hidden_files/tester-loop/test.db` (deleted at run start)
**HEADLINE: zero P0s, zero new P1s.** Hardening continues to hold: signed flows, auth gates, upload validation, replay protection all verified live. Four NEW P2s this run; one known P1 re-verified still-open.

## Production smoke (read-only, this run)
`GET https://musefm-townsquare.onrender.com/` + `/api/shorts` + `/musefm` + `/shorts` → all 200. Live site healthy.

## New findings

### [P2, NEW] `/api/passport/<fm_id>` lacks the standard `ok` envelope
- **Found by:** agent tester (machine API, ed25519 identities)
- **Repro:** register identity via `POST /api/identity/register` → `GET /api/passport/<fm_id>` → 200, body is bare `{"key_id":"musefm-platform-v1","payload":{...},"signature":"..."}` — no `"ok": true`.
- **Expected:** the envelope `/api/docs` section 2 ("Conventions") promises for ALL successful responses: `{"ok":true,...}`. Every other `/api/*` endpoint wraps; machine clients keying on `ok` will treat a valid passport as malformed.
- **Note:** same envelope class as the 2026-09-22 platform-key P2 (still on file), but a different endpoint — this one is new. Proving test added: `test_testerloop_passport_envelope_2026_09_23.py` (fails only on the envelope check; signature chain itself verifies fine against `/api/platform-key`).
- The passport content itself is sound: signature cryptographically verifies under canonical JSON.

### [P2, NEW] `/musefm/shorts?limit=99999999` silently ignores `limit`
- **Found by:** adversarial QA
- **Repro:** `GET /musefm/shorts?limit=99999999` → 200, returns the same 74KB page (code pins `limit=20`). No error, no clamping notice.
- **Expected:** either honor/clamp the param with a visible signal, or don't accept it. Silent ignoring confuses API consumers.

### [P2, NEW] `/musefm/shorts` shows no active nav state
- **Found by:** repo suite (`test_sidebar.py` check "fm shorts highlights FM Shorts")
- **Repro:** visit `/musefm/shorts` — no sidebar link carries `active`. The Shorts `sb_link` activates on `request.path.startswith('/shorts')` or `/watch`, but not on `/musefm/shorts`.
- **Expected:** an active highlight matching the page you're on. Tiny UX nit.

### [P2, NEW, low confidence] one form vote silently dropped
- **Found by:** human tester (handle `qa_human1`, form POSTs)
- **Repro:** 13 sequential `POST /vote` on post 1 (`+1,+1,−1,−1×10`, all 302). Toggle semantics were independently verified correct this run (upsert + toggle-off, one row per handle), so 13 effective votes must end with exactly one `−1` row — the `votes` table was empty afterward. Exactly one of the 13 didn't take effect. Not a 429 (all printed 302), not a 500. NOT reproduced in controlled retests (JSON + form, posts 1 and 11 all toggle correctly).
- **Suspected:** shared-IP rate bucket interaction with the parallel adversarial tester, not an app bug. Flagged for the record only; not worth a code change without a repro.

## Re-verified still-open
- **Comment silent truncation P1 (filed 2026-09-21 22:35) — STILL OPEN.** Human tester re-verified: 10001-char `POST /post/<pid>/comment` → 302, stored truncated to exactly 2000 chars, no user-facing warning. `/submit` 400s on overlong bodies instead — inconsistent UX. The truncation is deliberate per `test_testerloop_comment_edit_truncate_2026_09_21.py`, but the missing warning is the open half of the P1.

## Process notes (not app bugs)
- `test_sidebar.py` has 11 failures: 10 expect `/arena` and `/pro` sidebar section links (intentionally removed — Arena retired 2026-09-21; Pro folded under Family) and 1 is the `/musefm/shorts` active-state nit above. Test expectations are stale w.r.t. the branding change.
- `test_identity.py` check "episodes read" expects exactly 6 episodes; the checkout now seeds 8 (new daily-news episodes). Stale hardcode in the test, not a regression.
- Human tester note: 400s don't burn the signup bucket ✓; the login `Secure` cookie flag breaks plain-HTTP curl session reuse (manual header passing needed) — test-harness friction, not a bug. `/submit` 429 during this run was the shared-IP `post` bucket consumed by the parallel adversarial tester.
- Proving test added this run (tests only, no source changes): `test_testerloop_passport_envelope_2026_09_23.py` — 6/7, fails only on the passport envelope P2.

## Persona results summary
- **Human:** no P0, no confirmed P1. Signup/login/vote/react/comment-edit/episode-comments/photo-upload (magic-byte ✓)/pet flow/escape of `<script>`/`<img onerror>` payloads all clean.
- **Agent:** no P0, no P1. Replay (identical payload + recomputed-fresh-sig-on-old-nonce) → 401; wrong-key/tampered/malformed/stale-timestamp sigs → 401; evil GIF host → 400 whitelist; 4MB+ image → 413; truncated MP4 → 400; `file_sha256` mismatch → 401; signed attestation roundtrip end-to-end ✓.
- **Adversarial:** no P0, no P1. 404s branded; `/mod/*` 302→login; destructive endpoints 401; malformed JSON → 400; 100KB header → 431; path traversal blocked; `TRACE` → 405; `/login?next=//evil.com` safe (`_safe_next`). Server healthy after every probe.

**NOT LOUD.** Zero P0s, zero new P1s. Four new P2s (two genuine nits, one trivial UX, one unreproduced flake) + one known-P1 re-verification. Quiet per policy.
