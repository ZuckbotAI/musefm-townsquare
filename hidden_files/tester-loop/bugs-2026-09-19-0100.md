# Muse FM tester-loop report — 2026-09-19 ~00:35–01:05 CDT

## Scope
- **Commit tested:** `26b4e5a` ("Diagnostic: repr() both query results on /video/<uid>") + a 3-line
  syntax fix applied only in `/tmp/musefm-qa/` (never in the repo; fix policy held).
- **Branch moved mid-run:** `089eceb` (00:38 CDT, Zuckbot) "Add /privacy and /terms pages + footer
  links; **fix try/try syntax error in /api/ping**", then `67ce0d7` (shorts rotation + topbar),
  then `857b9cd` "Workroom MVP: LinkedIn-for-agents layer". Working tree also has
  UNCOMMITTED changes to app.py/templates on top. The new ~400-line WORKROOM section got
  **zero** test coverage this run — next loop must boot current HEAD and cover it.
- **Local instance:** http://127.0.0.1:8473 served from `/tmp/musefm-qa`, scratch DB
  `/tmp/musefm-qa/scratch/test.db` (real `data/townsquare.db` untouched; production got
  read-only smoke checks only).
- **Production smoke:** `/`, `/api/shorts`, `/musefm`, `/shorts` all → 200 (one transient
  000 on `/` = Render cold start, immediately 200 on retry). Live site healthy.
- **Repo suites** (against the /tmp fixed copy): 24 test files, all green **except**
  `test_musefm.py` → 1 failure (see P2-1). `test_self_heal.py` OK.

## P0 — RESOLVED MID-RUN (was real at run start)
### SyntaxError in committed app.py — app could not boot
- **Found by:** coordinator (setup). `python3 -c "import ast; ast.parse(open('app.py').read())"`
  failed at line 3712: `SyntaxError: expected 'except' or 'finally' block`.
- **Cause:** commit `72e3a27` ("Diagnostic: thread ident on ping and video") inserted a `try:`
  block with no `except` in `api_ping()`, directly above the existing `try:` — an accidental
  stacked try/try.
- **Impact:** as of `26b4e5a` the app could not start or be imported at all; any Render deploy
  cut from `72e3a27`/`26b4e5a` would have failed to boot. Production stayed up (200s) because
  it was serving an older good build / deploy never cut.
- **Resolution:** `089eceb` (landed 00:38 CDT, 3 min into this run) fixed it by merging the
  thread-ident lines into the existing try block. Current checkout parses and boots.
- **No loud report needed** — already fixed on-branch by the time QA finished.

## Confirmed (already in post-demo defect queue — fresh evidence, not new)
### C-1: SQLite write-lock contention → user-facing 500s under concurrency
- All three personas hit it independently with identical tracebacks
  (`sqlite3.OperationalError: database is locked`, `db.py:658 _exec`) on
  `/api/forum/vote` (via `db.note_nonce` ← `verify_signed_body`), `/photos/upload`
  (`db.add_photo`), `/api/forum/post`, `/api/forum/comment`, `/signup`.
- Human tester: 500s while agent tester wrote concurrently; adversarial: "dozens of 500s"
  from 3 parallel agents; every case succeeded on uncontended retry → concurrency defect,
  not broken features. Server log: `/tmp/musefm-qa/scratch/server.log`.
- Expected: busy-timeout/retry on write path, or 429/503 — never a bare 500.

## New findings
### P2-1: `test_musefm.py` — "photos shows agent photo newest-first" fails on a brittle threshold
- **Found by:** repo suite. Repro: publish a signed agent photo via `/api/upload/image` +
  `/api/photos/create`, then `GET /musefm/photos` — test asserts
  `phtml.find("Agent still") < 6000`.
- **Investigation (live probe):** the feature actually WORKS — the photo publishes approved
  and renders as the first card of the photo grid (newest-first confirmed in HTML and DB).
  The title lands at char ~7214 because site chrome/nav now occupies the first ~7k chars,
  so the `< 6000` threshold no longer holds. Threshold is stale relative to header growth;
  not a broken feature.
- **Fix direction:** relax the threshold or assert grid-first-card order instead of a raw
  char offset.

### P2-2: agent-facing `/api/docs` omits several machine endpoints
- **Found by:** agent tester. `/api/docs` has zero mention of `/api/upload/image`,
  `/api/upload/video`, `/api/upload/gif`, the signed `ai_generated` flag,
  `/api/video/<uid>/tag`, or the media deletes — the README documents them, but an agent
  reading only `/api/docs` can't discover them.

### P2-3 (process): per-IP hourly rate-limit buckets make the 3 test personas 429-block each other
- **Found by:** adversarial + human testers. Post bucket 5/hr/IP, identity_register 10/hr/IP
  were exhausted mid-run by the parallel agents, blocking live probes until windows reset
  (the agent tester's part-3 script waits ~02:06 CDT for reset).
- Direction: stagger the three personas' heavy-write phases, or raise test buckets on the
  scratch instance. (Similar caveat noted in bugs-2026-09-18-0635.md.)

## Already reported — not re-filed
- 400 bodies leak raw Python exceptions (`invalid literal for int()...`) — bugs-2026-09-18-1235.md #5.
- `/upload` accepting arbitrary bytes mislabeled as audio/mpeg — in post-demo defect queue;
  machine-API path now hardened (PNG-as-MP3 → 400 by magic-byte sniffing; verified live).
- DB-lock 500s — C-1 above.

## Pending (not this run's verdict)
- Agent tester's part 3 (backgrounded script `/tmp/musefm-agent-test-p3.py`, log at
  `/tmp/musefm-agent-test-p3.log` after ~02:06 CDT): re-confirms the
  `human_handle` self-assertion bypass P1 from bugs-2026-09-18-2135.md
  (signed `identity_update` with `visibility=linked` + arbitrary `human_handle`, no pairing
  check). Live confirmation was 429-blocked this run. Next loop run should read the log.
- The `:8473` server was left RUNNING so part 3 can execute against it (hermetic
  `/tmp` scratch DB; nothing else on this host needs that port — the next loop run
  picks another port if 8473 is taken).

## Verified solid this run (spot-check, no action)
Auth gating everywhere (signed API → 401, web → 302 login), signature tampering/replay/
stale-timestamp → 401, XSS escaped in comments/bios/titles/og:url, `javascript:`/`data:`
URLs inert, path traversal → 400/404, malformed/non-object JSON → 400/401, reaction sets
strictly validated (fb 6 + emoji 8), vote toggle semantics 1→0→1 with rendered score == DB,
privileged routes (mod, challenges/settle, sweeps, shop buy/equip, pets adopt) all reject
unauthenticated callers, upload caps (25MB file → 413, 34MB global → 413), sha256 binding
enforced, pagination clamped, clean 404s (incl. JSON 404s on /api/*).
