## ADVERSARIAL QA REPORT — musefm-tester-loop run (2026-09-21 ~18:38 CDT)

**Commit tested:** `395c80b` ("Fix tester-loop P2s (2026-09-21 15:35 run): react/fb_react validate-before-counting, request-derived OG tags, fb_react_web missing-target_id message")
**Method:** hermetic Flask TestClient probes (fresh scratch DB + fresh REMOTE_ADDR per batch — no rate-bucket cross-talk with sibling loops). Zero tracebacks in live `server.log`. Production untouched.

### Verdict: no P0, no P1. One P2 nit. Both 15:35 P2 fixes verified working.

### FIX VERIFICATION
- **P2-1 (validate-before-counting) — FIXED.** 140x `POST /api/forum/react` with invalid emoji → 140x `400 {"error": "emoji must be one of: ..."}`, zero 429s. Pre-checks in `api_fb_react` likewise 400 without touching the budget. Code comments and behavior now agree.
- **P2-2 (og tags) — FIXED.** `og:url`/`og:image` are request-derived (`http://localhost/...` in hermetic client) on homepage, share pages, and the 404 page. No `https://musefm.lol` hardcode remains in rendered HTML.

### P2-1 (new) — oversized prose silently truncated, never rejected
Signed-API `POST /api/forum/post` with a 10,000-char body → 200; `POST /api/forum/comment` with 5,000 chars → 200. `db.clean(s, 2000)` caps at 2000 chars and returns truthy, so the API stores a silently truncated version with a success response. The human web form presumably caps via `maxlength`; the agent API gets no signal it lost data. (Flag's reason, by contrast, hard-rejects at 400.) Behavior is consistent across post/comment routes and longstanding `clean()` design — informational consistency nit, not breakage.

### Verified solid (do not file)
- **404s:** `/nope`, `/api/definitelynotreal` (JSON `{"error":"not found"}`), `/shorts/999999`, `/post/abc`, `/c/lobby/post/999999999`, `/watch/abc`, `/musefm/photos/abc`, `/passport/notarealid`, `/api/episodes/no-such-slug`, `/admin`, `/debug`, `/console`, 3k-char path → clean branded 404s, no traceback/stack-trace/path leak.
- **Traversal:** `/uploads/../../app.py`, `%2e%2e` variants, `//etc/passwd`, `%252e%252e` double-encoding, `/photo-file/…`, `/video/…` → all 404. `/audio/../../../../etc/passwd` → 400 "nope" (rejected).
- **Malformed JSON:** truncated, arrays, strings, numbers, `null` (→ "Malformed JSON body"), `text/plain`, UTF-16 BOM → all clean 400s. Wrong types (int title, list body, null title) → 400. Extra unknown fields (incl. `"admin": true`) ignored, 200.
- **Missing params:** no title → 400; no value on vote → defaults to +1 (documented); no emoji / no target on react → 400; no reason on flag → 200 (defaults, intended); comment on nonexistent post → 400 "unknown post"; bad parent_id → 400; `javascript:` image_url → 400.
- **Unauth:** all signed write endpoints (post/vote/react/comment/flag, tide-toss, feed-frenzy, admin rotate-key, challenges/settle, uploads image/video/audio) → 401; web session routes (`/settings`, `/notifications`, `/pet/feed`, `/workroom/create`, `/settings/unlink`) → 302 to login. No bypass.
- **Bursts:** 30x `GET /api/shorts` → 30x 200 in 0.05s, no 500s. 5x identical upvotes → exactly 1 vote row in DB, toggle on/off semantics intact (no double-counting).
- **Vote value:** 2/-2/0 → 400; `"1"` coerced to 1 (200, documented `_int_field` behavior); 1.5/true → 400.
- **Reaction picker:** invalid name/empty/20k-char/unicode/whitespace/null/int/list/dict/bool emoji → all 400; nonexistent/null target → 400; float target → 400 "must be an integer". Repeat same-emoji → idempotent 200.
- **fb_react:** case/space normalized (`"LOVE"` → toggles `love`), toggle add→remove works, unknown reaction/target_type/target → 400, missing target_type defaults to `post`.
- **Methods:** PUT/DELETE/PATCH/HEAD on `/api/forum/post` → 405. 12MB body → 401 (auth before parse); 40MB body → 413 (34MB `MAX_CONTENT_LENGTH` cap enforced).
- **Share links:** valid `/c/lobby/post/<id>` → 200 with proper og tags; `?x=junk` query ignored; `/junk-suffix`, `/s/abc`, `/p/abc` → 404.
- **Query abuse:** `?page=-1`, `?page=abc`, `?limit=1000000`, `?limit=-5`, `?x=`+2000 chars on `/api/shorts` → all 200 with sane defaults.
- **Registration:** dup handle → 400.

### Caveat
Earlier comment probes using `text=`/`target_id=` failed with "comment body required" — my client error (correct fields are `body=` + `post_id=`), re-run correctly. No server bug.

All test artifacts (identities advqa1836–advqa1836h, posts) live only in hermetic scratch DBs under /tmp/advqa-1836*, never the repo test DB or production.
