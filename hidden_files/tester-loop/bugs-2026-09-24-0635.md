# Tester loop triage — 2026-09-24 06:35 CDT

- Commit at run start: `961f41f` — "Add /faq page (Getting Started, Account, Trust, Troubleshooting); expand /facts to 8 Q&As"
- Local instance: http://127.0.0.1:8473 (port free at boot), scratch DB `hidden_files/tester-loop/test.db` (deleted at run start; hermetic)
- Real local DB: untouched. Production: READ-ONLY smoke — `/`, `/api/shorts`, `/musefm`, `/shorts` all 200. Live site healthy.
- Three subagent reports: human tester (handle `qa_mischief`), agent API tester (29/29 signed-flow checks), adversarial QA (~110+ probes).

**Verdict: quiet run.** No P0s. No brand-new P1s. Two new P2s below.
Per reporting policy: no chat surfacing beyond the daily-log one-liner.

## NEW P2s (not in last 48h of bug files)

1. **P2 — Share dialog says "Share this episode" on forum thread pages.** [human tester; coordinator-verified live]
   Repro: `GET /c/lobby/post/15` (200) — the `#share-sheet` card heading reads `<h3>Share this episode</h3>`.
   Expected: thread-appropriate copy ("Share this thread") or a generic label, since the sheet is a global base-template partial shown on every page.
   Cause: `templates/base.html:249` hardcodes the episode copy.
   Evidence: `grep "Share this episode" templates/` → only base.html; thread-page HTML contains the episode copy verbatim.

2. **P2 — Missing `nonce` reports "bad nonce" (no missing-vs-malformed distinction).** [agent tester; coordinator-verified in code]
   `verify_signed_body` (identity.py) got the missing-vs-bad distinction for `timestamp` on 2026-09-21 ("missing timestamp" vs "bad timestamp"), but the nonce check still does `if not valid_nonce(nonce): raise IdentityError("bad nonce (need 128-bit base64url)")` — a missing nonce and a malformed nonce produce the identical string. Rejection behavior is correct (401 either way); this is error-message precision for agent client debugging, same class as the already-fixed timestamp P2.

## Re-observed (previously filed, still present — new instances this run, NOT new bugs)

1. **Error-envelope inconsistency — two new path variants.**
   Filed: "Error-page inconsistency (405/400/404)" (09-23 21:35 dedupe list).
   - `GET /api//forum/vote` (doubled slash) → **405 raw Werkzeug HTML** (`<title>405 Method Not Allowed</title>`), never reaches the 404 handler's JSON-405 branch. Verified live.
   - `GET /api` (bare) → **404 HTML** — the API-404 branch checks `startswith("/api/")`, so the exact `/api` path falls through to the HTML 404 template. Verified live (`Content-Type: text/html`).

2. **Rate budget burned before auth/validation outcome — new instance: unauthenticated 401s burn the per-IP `post` bucket.**
   Filed 2026-09-21 15:35 (react budget), re-observed 2026-09-24 03:35 with the 120-invalid-react repro.
   This run: adversarial sent ~40 unauthenticated POSTs to `/api/forum/post` → all 401, then subsequent requests → `429 {"error":"rate limit hit — slow down, friend"}` with `Retry-After: 3433` (~57 min). 5 garbage requests can lock out posting for a whole NAT'd IP block for an hour. Code comment says deliberate (protects the one-time nonce); trade-off noted, not a vuln — same class as the react case.

## Deduped (already on file, NOT new)

- Silent limit clamping (`/api/forum/posts?limit=-5/-1/0/abc` → 200, 1 post) — filed 2026-09-23 15:35 P2 #2.
- `Server: Werkzeug/3.1.8 Python/3.12.3` version banner — filed (09-23 12:35 P2 #5).
- All by-design-failing proving tests (`test_testerloop_1535_p2s`, `1835_fixes`, `1835_flood_wall`, `error_messages`, `passport_envelope`, `platformkey_envelope`, `ratelimit_before_validate`, `rawint_exn`, `logout_csrf`, `2135_p2s`, etc.) — each proves an already-filed P2. Still failing as designed.

## Repo suites (111 test_*.py, serial as scripts + pytest collection)

All failures triaged — every one is a stale test after an intentional, Anthony-approved change, or a known cascade. No new failures:

- **Signup-without-email cascade** (commit 454ba91 made email mandatory, 2026-09-23): `test_testerloop_1835_fixes_2026_09_19`, `test_testerloop_flag_messages_2026_09_19`, plus the previously-triaged set (`test_auth`, `test_sidebar`, `test_human_usability`, `test_identity`, `test_kind_tags`, `test_pet_onboarding`, `test_pet_presence`, `test_p1_csrf_human_handle_2026_09_18`, `test_workroom_pilot`, `test_workroom_private`, `test_openmic`, `test_signals`, `test_notifications`, `test_asks`, `test_ai_images`, `test_comments_pro`, `test_gifs`, `test_linking`, `test_mod_approval`, `test_mod_notifications`, `test_sso_provider`, `test_shorts`, `test_video`, `test_video_comments`, `test_uploads`, `test_upload_audio_mimetype_2026_09_18`). Underlying features verified healthy this run: scunthorpe filter (`has_banned("snigger")`=False, standalone slur still blocked), comment 2000-char cap (now via `db.loud_limit`, raises on >2000), signup WITH email → 200.
- **`/row` now serves village.html (3D canvas)** — `test_row.py` 7 fails (ROW_STATE/csrf-meta expectations) are stale: the legacy 2D `row.html` is intentionally retired ("the 3D canvas is the launch vehicle"). Row APIs verified healthy live: `/api/row/feed` → 200, `/api/row/standing` + `/api/row/player` → 401 logged-out (auth-gated as designed), `/row` → 200. The `/row/avatar` redirect fails are stale after the intentional move of the avatar customizer to agent profiles.
- **One-word "MuseFM" rebrand + orb favicon** (Anthony's commits 75a6a46, 86e3684): `test_brand.py` 4 fails — all expect `"Muse FM"` with a space or the old SVG favicon; app now uses `aria-label="MuseFM"` (no space) and `<link rel="icon" type="image/png" href="/static/img/orb-mark.png">`. Stale, not bugs.
- **Dead/stale**: `test_musefm` (`import fb_reactions` — module removed, commit d2e9861), `test_workroom_bridge` (`ensure_workroom_events_schema` renamed), `test_shorts_seen_rotation` (test-vs-design), `test_links` (/arena 404 + /pro 301 — retired/consolidated), `test_tidepal_homepage` (regex grabs first band).
- `test_community_episodes.py`, `test_shorts_seen_rotation.py`: module-level `sys.exit` — pytest can't collect; run as scripts (known).

## Persona reports (scratch DB only)

- **Human:** signup/login gates exact (dup handle 400, bad password 401, 5/hr signup 429; 400s don't burn budget), post/comment/vote/react/upload journeys all green, XSS escaped everywhere (script/img-onerror in titles/bodies, `javascript:`/`data:` URLs not linkified), vote idempotent (20 rapid same-value votes → 1 row), 25 rapid reaction toggles → single row, image upload magic-byte solid, Range requests 206 with correct Content-Range, traversal → 400, mobile viewport + ~15 breakpoints OK. 1 new P2 (share-sheet copy above).
- **Agent:** 29/29 signed-flow checks pass — register, signed post/comment/vote, GIF/image/video uploads with `file_sha256` binding (truncated video → 400 structural moov check), identity_update, platform-key/passport/signal-credential attestations verify cryptographically (tampered payload fails). 15 negatives all correct 401s (nonce reuse → "replay: nonce already used", wrong key, tampered, bad b64, missing/short nonce, stale/future timestamp, unknown fm_id, missing action, action mismatch, unsigned, sha256 mismatch). 1 P2 nit (missing-vs-malformed nonce wording above).
- **Adversarial:** ~110 probes, zero 500s. 404s clean (HTML pages / JSON `/api/*`), no stack-trace leakage, traversal/SQLi/null-byte all 404/400, malformed JSON → uniform 400 (`"Malformed JSON body"`; duplicate keys → 400), open redirects safe (`_safe_next` unit-validated against 13 hostile inputs), admin/mod paths gated or 404-disabled, security headers present (`nosniff`, `SAMEORIGIN`, strict referrer; `no-store` on `/api/*`; no CSP — deliberate, documented). Findings above.

## Caveats

- The adversarial tester's rate-limit probing burned the shared per-IP `post` (5/hr) bucket on this scratch instance — unrelated to the real DB (scratch DB + in-process buckets only).
- Two pytest modules (`test_community_episodes`, `test_shorts_seen_rotation`) call `sys.exit` at import; pytest can't collect them — ran as scripts per prior-run convention.
- Bug reports live in `hidden_files/tester-loop/` (uncommitted) — the branch is Anthony's to review and change.
