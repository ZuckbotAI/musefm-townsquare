# Tester-loop triage — 2026-09-21 06:35 CDT run
**Commit:** `62e646b` ("Fix tester-loop P2s (2026-09-21 00:35 run)")
**Target:** local instance `http://127.0.0.1:8473`, fresh scratch DB
(`hidden_files/tester-loop/test.db` deleted at run start; real `data/townsquare.db` untouched)
**Production smoke (read-only):** `/`, `/api/shorts`, `/musefm`, `/shorts` → all 200
**Repo suites:** 73 `test_*.py` files — 69 pass. 4 failing files are byte-identical
to the 03:35 run and all recorded in the last 48h's bug files:
- `test_sidebar.py` 107 pass / 9 FAIL — expects the deliberately removed
  `/musefm/shorts` sidebar link (removed in commit `7caa312`, "drop duplicate
  FM Shorts link"); stale test, not an app defect.
- `test_testerloop_1835_flood_wall_2026_09_20.py` 12 pass / 4 FAIL — flood-wall
  collapse fix lives in held branch `466dfe6`, not merged; fails as expected.
- `test_testerloop_ratelimit_before_validate_2026_09_20.py` 3 pass / 2 FAIL —
  known since prior runs.
- `test_tidepal_homepage.py` 7 pass / 4 FAIL — expects pre-redesign homepage;
  known since prior runs.

## P0 — crash / data loss / security hole
**None.** All three personas + adversarial battery agree.

## P1 — broken feature
**None.**

## New P2s this run (not in last 48h bug files)
All six proven by new test file `test_testerloop_p2_2026_09_21_b.py`
(tests only — 0 pass / 6 fail; no source changes):

1. **No `Cache-Control` on API responses** — `/api/ping` returns none; dynamic
   JSON could be cached by intermediaries. Expect `Cache-Control: no-store`
   on `/api/*`. (adversarial QA)
2. **`GET /api/agents/<unknown>/activity` → 200 echoing the raw id**
   (`{"fm_id":"fm_no_such_muse_xyz","items":[],"ok":true}`) while
   `/api/identity/<unknown>`, `/api/pond/<unknown>`, `/api/rewards/<unknown>`
   all 404. Standardize to 404. (adversarial QA)
3. **Inconsistent path normalization** — `GET /api/forum/react/` (trailing
   slash) → 404, `GET /api//forum/react` (double slash) → 405. Should agree.
   (adversarial QA)
4. **Display-name charset rejects apostrophes** — "Bob O'Brien" → 400
   ("display name: 1-40 chars — letters, numbers, spaces, _ . -").
   Secure but hostile to real human names; consider allowing `'`.
   (human tester)
5. **Misleading error wording** — signed body missing `timestamp` reports
   `"bad timestamp"` (identity.py:183 maps missing→None→int() TypeError→
   "bad timestamp"); `"missing timestamp"` would be clearer. (adversarial QA)
6. **Duplicate JSON keys silently last-win** — registered `advqa_probe4`-style
   handle from the second `"handle"` key. Standard `json` behavior; noting
   only, not a defect. (adversarial QA)

## Verified NOT bugs this run
- **"No cap on post body length"** (human tester): wrong — `MAX_BODY = 10000`
  (db.py:72) is enforced on both the human `/submit` form route (app.py:1270)
  and the signed API (app.py:2151). The tester's 10,000-char body was exactly
  at the cap, hence accepted. Correct behavior.
- **Repeated identical votes toggle the vote off** (human tester): reddit-style
  toggle by design; 10 parallel `value=1` votes alternated cleanly ending at
  score 0 with no race corruption. Already recorded in many prior runs.
- **`**bold**` renders literally** (human tester): post bodies are escaped
  plaintext by design (XSS prevention); no markdown rendering exists.
- **405s return Werkzeug HTML** (adversarial QA): already reported
  (09-19-0935/1535/1835, 09-21-0035).
- **Shared post rate bucket (5/hr) exhausted by parallel testers** (agent +
  adversarial QA): already reported extensively (09-20 all runs, 09-21-0035);
  blocked forged-media-URL and duet-length negative tests this run. Code
  inspection confirms `db.create_post` validates image/gif/video URLs through
  the `valid_*_url` whitelist → `ValueError` → 400, and `validate_duration_secs`
  + `detect_video` EBML/Segment scans cover the WebM path. Recommend a
  hermetic toggle/idempotency run when the bucket clears.

## Verified solid this run
- **Agent/API:** identity register → signed post/comment/vote happy paths;
  uploads (gif/image/video) with sha256-vs-bytes binding; ai_generated=false
  → pending + anon 404 gate holds; duet_of valid→200 / bogus→404; all crypto
  negatives (unsigned, tampered, replayed nonce, wrong action, future
  timestamp, wrong sha256, flipped ai_generated) → precise 401s; memory
  write→201 / signed read→200 / unsigned read→401. Ed25519 attestations on
  `/api/signal/credential`, `/api/passport`, `/api/agents/<fm_id>/activity`
  verify against `/api/platform-key`; tampered/wrong-key payloads fail.
- **Adversarial:** 120 probes — malformed/truncated JSON, array bodies, type
  confusion, reaction allowlists (react 8 / fb_react 6, case-sensitive),
  path attacks (`/static/../app.py`, `%2e%2e`, `%00`, unicode, 4000-char),
  5000-deep nested JSON, 10MB/40MB bodies, share-link degradation — every
  input got a precise 4xx, nothing crashed, nothing leaked. Nonce consumed
  before target lookup (replay → 401 after 400 unknown target — correct
  ordering). X-Forwarded-For rotation bypass confirmed fixed in code.
- **Human flows:** signup validation, nested comments, vote range, reaction
  picker, fake-image rejects (text-.gif, EXE-.png → 400 "not a recognized
  image"), real PNG → moderation queue, CSRF on all forms, anon gates 401,
  GET /logout → 405 POST-only, session killed on POST logout, audio/shorts
  playback, mobile markup.

## Reports saved
- `human-tester-2026-09-21-0635.md`
- `agent-tester-2026-09-21-0635.md`
- `adversarial-2026-09-21-0635.md`
- `suite-run-2026-09-21-0635.txt`
- `test_testerloop_p2_2026_09_21_b.py` (untracked proving test, 0/6 green)
