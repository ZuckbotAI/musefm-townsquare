# Adversarial QA — 2026-09-21 03:35 run (commit 5a38b43, local http://127.0.0.1:8473, fresh scratch DB)

Test identities created this run (scratch DB only): `advqa_sig_a` (fm_L9otWGhMEq0P),
`advqa_sig_b` (fm_jQk9a1LshBIo); web handles `advqa_one`, `advqa_two`.
Keys: hidden_files/tester-loop/advqa_priv_{a,b}.txt (test-only).

## Findings

### P2 — legacy /api/forum/react has no removal path (NEW)
- `db.react` (db.py:2352) is `INSERT OR IGNORE` — add-only since the original
  commit (verified via git log -L). No unreact/delete endpoint exists for the
  custom-emoji `reactions` table (only cascade-delete in videos.py when a video
  is deleted). Once an agent/human adds a custom emoji via
  POST /api/forum/react, it is permanent — a mis-react can't be undone.
- Verified live: A 👍 then A 👍 again on same post → count stays correct but
  there is no way to remove; two identities (A+B) both 👍 → 2, correct
  per-reactor isolation, but neither can ever remove theirs.
- fb_reactions.py docstring calls this system "legacy" and describes it as
  "one of each per reactor" — so this may be accepted legacy behavior. If
  intentional, document it; the FB-six picker (toggle: added/switched/removed)
  is the only removable path.
- Repro: register identity, POST /api/forum/react signed {target_id, target_type: post, emoji: 🔥} twice → 200 both, no removal API exists.

### Coverage gap (not a bug): web session+CSRF paths partially blocked
- human_login rate limiter (10/hr/IP) tripped by this run's own login attempts
  (wrong-pw/SQLi/empty-cred probes + a second test login) → subsequent logins
  429. Web-authenticated probes for /vote, /fb_react, /submit edge cases and
  the 20-concurrent web race test could not run this round. The sibling human
  tester covers browser-authenticated flows; signed-agent equivalents all ran.

## Verified clean (no findings)
1. 404 handling: nonsense paths → clean branded 404 page, no stack leaks;
   wrong methods → 405 with Allow header (TRACE → 405 too); sqlite_int
   converter rejects junk → 404.
2. Malformed bodies: invalid JSON → 400 "Malformed JSON body"; JSON array/null
   → 400/401 correctly; wrong content-type → 401 (auth-first); 2MB body
   unauth → 401 in 0.02s; 1MB body to /vote anon → 401 in 0.01s (no body-read
   hang); null bytes in JSON → 400; 5000-deep nested JSON → 400 (no
   RecursionError); duplicate JSON keys → signature check catches (401).
3. Missing/edge params (signed): target_id list/dict/alpha → 400
   "bad target_id: must be an integer"; target_type list → 400; emoji
   dict/script/9000-char flood → 400 allowlist; value alpha/1.5/2/0/giant →
   400, value -1 → 200; post with no fields → 400 "title required"; 200k-char
   target_type → 400 (no hang).
4. Unauthenticated non-public paths: 36 endpoints probed — all 401, 403, 302
   to /login, or 404. No 200 leaks. Notables: /api/admin/identity/rotate-key,
   /api/challenges/settle, /api/reengagement/sweep, /api/pets/sweep,
   /api/memory/wipe, /api/webhooks, /api/upload/image, /api/video|audio delete,
   /api/forum/{react,vote,post}, /api/workroom/create, /api/swarm/freeze,
   /api/shop/buy_proxy, /api/memory, /api/notifications, /api/link_muse (400
   "code required" — shape validation, no leak), /api/agent-memory/*,
   /mod/*, /settings, /notifications, /workroom/invites. Non-mod session user
   → /mod/flags 302 (couldn't fully verify 403 vs redirect due to login block).
   /api/platform-key 200 is BY DESIGN (published Trustline verify key).
5. Race: 20 concurrent signed POST /api/forum/vote → all 200, final toggle
   state consistent (score 1 after odd toggles). 20 concurrent signed
   /api/forum/react (valid emoji) → all 200, total consistent (add-only
   idempotent). No double-counting, no corruption.
6. Reaction edges: invalid emoji codes → 400 allowlist (8 emojis); react on
   pid 0/-5/99999999 → 400 "unknown target"; pid "abc" → 400.
7. Share links: /s/99999999, /p/99999999, /s/abc, /p/abc, /m/doesnotexist99,
   /video/99999999, /watch/99999999, /episodes/no-such-slug,
   /c/no-such-community, /agent/nosuchhandle → all 404.
8. Open redirects: _safe_next (app.py:715) verified by read + live: rejects
   absolute, //host, backslashes, control chars; login POST uses it; login.html
   `{{ next }}` autoescaped. /login?next=evil… renders login page (200), never
   redirects out. No open redirect found.
9. AuthN hygiene: wrong pw / SQLi handles / empty creds → 401 (one path;
   failed logins render 200 login page with error — normal); tampered
   signature → 401; wrong action → 401; assertion (GET /api/assert-identity,
   requires login) cannot be used for writes (401).
10. IDOR: identity B deleting/editing A's memory entry → 404 (owner-scoped);
    signed DELETE /api/video/99999999 → 404; /api/video/1/delete as non-owner
    → 403 "only the uploading identity may delete its video"; memory wipe
    requires {"confirm": "WIPE MY MEMORY"}.
11. Security headers present: nosniff, SAMEORIGIN, strict-origin-when-cross-origin.
    OPTIONS → 200 with no Access-Control-Allow-Origin wildcard.

## Suggested for bugs-2026-09-21-0335.md
- (P2) legacy /api/forum/react: no un-react path — reactions permanent.
- Coverage note: web session+CSRF race/edge probes deferred (login limiter).
- No P0, no new P1 → per loop policy, quiet in chat.
