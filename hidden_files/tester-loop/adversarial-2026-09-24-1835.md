# Adversarial QA report — Muse FM tester loop, 2026-09-24 18:35 run

- COMMIT: `6661f91` ("Pet backend cutover: canonical pet store + village key unification")
- Target: http://127.0.0.1:8473 (scratch DB hidden_files/tester-loop/test.db; own test identity fm_MvUAAWypek-L / qa_adv1835; own posts/comments/reactions/flags only)
- Production: untouched.
- Server stayed healthy the whole run (no mid-run death this time).

## Summary
0 new P0/P1. 1 new P2 nit. **The 15:35 P1 (60k-char `q` → 500) is FIXED at this commit** — strike it from the dedupe list. Everything else in the adversarial battery passed clean.

---

## FIXED since 15:35 (strike from dedupe list)

### 15:35 P1 — `GET /api/forum/posts?q=<60000 chars>` → HTTP 500 — FIXED
- Verified live at this commit: `GET /api/forum/posts?q=<60000 x "A">` → **400** `{"error":"search query too long (max 2000 characters)","ok":false}`. Threshold edge: 2000 chars → 200, 2001 → 400.
- Web-UI twin `/c/lobby?q=<60k>` → **400** `search query too long (max 2000 characters)` (plain-text body, page still renders 200 for the shell — fine).
- Code: `app.py:654-658` now has `SEARCH_Q_MAX = 2000` with the comment "P1 2026-09-24: max length for user search queries (forum ?q=, agent directory ?q=). sqlite's default LIKE pattern limit is 50000 bytes — anything beyond that 500s the request."
- Sibling endpoints unaffected: `/api/shorts?q=<60k>` → 200 (no cap, no 500 — different search path).
- Repro command: `curl -s --noproxy '*' -o /dev/null -w '%{http_code}' "http://127.0.0.1:8473/api/forum/posts?q=$(python3 -c 'print("A"*60000)')"` → `400`.

---

## P2 — nits/polish (new this run)

### P2-1: Reaction allowlist rejects skin-tone modifiers (👍🏽 → 400)
- SEV: P2 (UX friction, not breakage — the 400 message is clear).
- REPRO: signed `POST /api/forum/react` with `{"target_type":"post","target_id":12,"emoji":"👍🏽"}` → **400** `{"error":"emoji must be one of: 🔥 ❤️ 👍 😂 🎙️ 👏 💡 🚀"}`. Same for other modified variants (🇺🇸, 👨‍👩‍👧‍👦, 🧑‍💻, 👍👍, bare ZWJ, 500-char string, empty string — all 400 with the allowlist message; base ❤️ with VS16 → 200, allowed).
- EXPECTED vs ACTUAL: expected either acceptance/normalization of the modifier variant or rejection with a hint — phone keyboards emit skin-toned emoji by default, so a human tapping 👍 from their phone gets a 400. Actual: clean 400 with the allowed set, so a user can retry with the base form — hence P2, not P1.
- Caveat: the allowlist is explicitly enumerated, so this may be intended design (keeps counting/dedup simple). Flagging for Anthony's review rather than asserting it's wrong.

---

## PASSED (verified clean this run)

- **404 handling:** `/no-such-page-zz99`, `/a/b/c/d/e/deep/path`, `/%2e%2e/%2e%2e/etc/passwd`, double-encoded `/%252e%252e/...`, null-byte path `/no%00such`, `/api/..%2f..%2f..`, `/api/nope-xyz` → clean 404s (HTML for UI, `{"error":"not found"}` for /api). No traversal, no source disclosure.
- **Share-link formats:** `/c/lobby/post/12` → 200; `/c/lobby/post/{abc,0,-1,99999999,12/../../../x}` → 404; `/watch/{99999999,abc,0}` → 404; `/p/{not-a-post,12,}` → 404; `/api/episodes/nope` → 404 "unknown episode"; `/api/identity/fm_..%2f..%2fetc` → 404.
- **Malformed bodies:** `not json{{` → 400 "Malformed JSON body"; `[1,2,3]` → 400 "JSON body must be an object"; 300-deep nested JSON (unsigned) → 401 auth rejection before any field parsing.
- **Unauthenticated/unsigned hits:** `/api/forum/post` with `{"action":"post","title":"x"}` → 401 "missing fm_id"; react with unsigned JSON body → 401; `POST /api/admin/triage/run`, `GET /api/admin/triage/status` → 401 "bad or missing agent key".
- **Signature auth:** wrong action for endpoint → 401 "wrong action for this endpoint (got 'post')"; tampered field after signing → 401 "signature does not verify"; missing signature/nonce/timestamp → distinct 401s; replayed signed body → 401 "replay: nonce already used"; +10h/−10h timestamps → 401 "timestamp outside the 5-minute window".
- **Validation-before-budget on signed post route:** `POST /api/forum/post` validates community/title/body/banned-content (400s) before `check_limit("post", 5)` — confirmed in code (app.py:2635-2657).
- **Oversized:** 60k-char comment body → 400 "comment body too long (max 2000 characters)"; 2000-char q → 200, 2001 → 400.
- **Injection:** `?sort=1;DROP TABLE posts;--`, `?limit=10 UNION SELECT 1` → 200 with params safely ignored; `q='`, `q=%%%` (LIKE wildcards) → 200 normal results; `q` with embedded newline → 200.
- **Reactions (signed):** comment target → 200; bad target_type → 400 "target_type must be post or comment"; unknown post → 400 "unknown target"; 30x concurrent identical reacts → all 200, idempotent final count, no budget blowout.
- **Votes (signed):** `value` "abc"/1.5/0 → 400s; `target_id=2**70` → 400 "unknown target"; first vote → 200, replayed body → 401 (replay guard).
- **Flags (signed, action=flag_post):** nonexistent target → 400 "unknown target"; own post → 200 flag_id 1; bad reason → 400 with allowed-reason list; 100-emoji reason → 400.
- **Methods/content-types:** GET on POST-only react/comment/vote → 405; `OPTIONS` → 200 (Flask default); `Content-Type: application/json; charset=utf-8` → 200; `HEAD /` → 200.
- **Null bytes:** signed comment `hello\x00world` → 200, stored with the null byte stripped (safe; consistent with 15:35).

## Observations (not bugs)

1. **Shared per-IP post budget across parallel testers:** my `POST /api/forum/post` probes (missing title, 500-char title) returned 429 because the parallel human tester had burned the shared per-IP 5/hr "post" bucket. The decorator's `_would_limit()` peek returns 429 without consuming my nonce (confirmed live — later signed requests worked fine). Expected shared-bucket behavior, not a bug — but it means adversarial runs that overlap with human-tester bursts will see 429s on post creation.
2. **`limit` clamping is silent** (`limit=-1`, `limit=999999999` → 200 with clamped results) — same class as the previously-filed "silent limit clamping" P2; not re-filed.
3. The scratch DB contains seeded demo content ("The humanoids clocked in. Figure AI's Helix 2.5...") — posts from earlier runs/seed data, not mine. Left untouched.
4. My test artifacts (identity qa_adv1835, post id 12, comments id 3-4, reacts, flag id 1) live only in the scratch DB.

## Repro commands (exact)

```bash
# fixed P1
curl -s --noproxy '*' -o /dev/null -w '%{http_code}\n' "http://127.0.0.1:8473/api/forum/posts?q=$(python3 -c 'print("A"*60000)')"   # 400

# skin-tone reaction rejection (P2-1) — signed body via identity.signed_body:
python3 - << 'EOF'
import json, sys, urllib.request
sys.path.insert(0, "/home/hatch/workspace/musefm-townsquare")
from identity import signed_body
PRIV = "2oXLc8k2Bi0eYIphoGBZyhZIuonmBiqarWmCrMhDxKM"; FM = "fm_MvUAAWypek-L"
b = signed_body(PRIV, "react", FM, target_type="post", target_id=12, emoji="👍🏽")
r = urllib.request.Request("http://127.0.0.1:8473/api/forum/react", data=json.dumps(b).encode(),
                           headers={"Content-Type": "application/json"})
try: urllib.request.urlopen(r)
except urllib.error.HTTPError as e: print(e.code, e.read().decode()[:120])  # 400 allowlist message
EOF

# traversal / 404s
for p in "/%2e%2e/%2e%2e/etc/passwd" "/api/..%2f..%2f.." "/c/lobby/post/abc" "/watch/0" "/p/12"; do
  curl -s --noproxy '*' -o /dev/null -w "%{http_code} $p\n" "http://127.0.0.1:8473$p"; done   # all 404
```
