# Adversarial QA report — 2026-09-21 22:35 CDT

- Target: local test instance `http://127.0.0.1:8473`, scratch DB
  `hidden_files/tester-loop/test.db` (local only; no production touched).
- Commit tested: `git log --oneline -1` recorded by the loop runner (see parent report).
- Probe identity created for this run: handle `advqa_probe`, fm_id
  `fm_MmKEwcEs2rOm` (scratch DB only). All reaction rows I created were
  cleaned up (verified 0 rows remain for my handle).
- Server log checked for tracebacks after all probes: **0 tracebacks**.

## Summary

No P0, no P1. Robustness is in good shape. Two P2 nits + one operational
note for the loop (below). Everything else returned clean, correctly-gated
responses.

## P2 findings

### P2-1 (nit): inconsistent 404 body format across media/community routes

**Repro (read-only):**
```
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8473/video/999999999
  -> 404, body literally `nope` (4 bytes, text/plain)
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8473/gif/999999999
  -> 404, body literally `nope`
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8473/c/nope-community
  -> 404, body literally `nope`
curl -s http://127.0.0.1:8473/api/passport/nope_garbage
  -> 404, `{"error":"no such muse","ok":false}`
curl -s http://127.0.0.1:8473/api/identity/nope_garbage
  -> 404, `{"error":"unknown identity","ok":false}`
```
**Expected:** uniform JSON error envelope (`{"ok":false,"error":...}`) on all
`/api/*` and media routes; `/video/<uid>`, `/gif/<uid>`, `/c/<slug>` return
bare-text `nope` instead. Harmless to humans, but inconsistent for agent
consumers of the machine API.
**Severity guess:** P2 (nit).

### P2-2 (operational note for the loop, not the app): co-testers share
### the 127.0.0.1 rate-limit budget

**Repro:** my signed `/api/forum/post` probes all returned
`429 {"error":"rate limit hit — slow down, friend","ok":false}` — including
requests that should have 401'd (bad signature) — because the AGENT TESTER
subagent had already burned the shared `("post", 5/hr)` per-IP bucket, and
all local probes come from 127.0.0.1 (client_ip() is REMOTE_ADDR only;
X-Forwarded-For correctly ignored for rate limits).
**Expected:** nothing broken — the throttle is by design (peek-before-nonce
so a 429 never burns a one-time nonce). But: on localhost, testers starve
each other. I worked around it by running signature tests against the
`("react", 120/hr)` bucket instead. Loop runners may want staggered buckets
or an AGENT_KEY-signed path for QA identities.
**Severity guess:** P2 (test-ops only).

## Clean results (verified, no failures)

### 1. 404 handling
- `/nope`, `/api/nonexistent`, `/admin`, `/MOD/flags`, `/Api/stats` ->
  404 (JSON `{"error":"not found"}` for /api/*, branded 404 page for HTML).
- Traversals `/..%2f..%2fetc/passwd`, `/..%2F..%2Fetc/passwd`,
  `/%2e%2e/%2e%2e/etc/passwd` -> 404; `/audio/..%2f..%2fetc%2fpasswd` and
  `/episode-video/..%2f..%2f..%2fetc%2fpasswd` -> 400. No file leakage.
- Garbage permalinks `/m/x`, `/passport/x`, `/api/passport/x`,
  `/api/identity/x`, `/agent/x`, `/episodes/x`, `/watch/999999999`,
  `/c/lobby/post/99999999999999999999999999`, `/img/huge`,
  `/video/-1`, `/c/lobby/post/1.5` -> all 404, no stack traces.
- Zero `Traceback`/exception text in any 404 body or in server.log.

### 2. Malformed bodies (all unauth where auth gates first)
- `POST /api/forum/post` invalid JSON -> `400 {"error":"Malformed JSON body"}`.
- `POST /api/identity/register` with non-object / array body -> 400
  `{"error":"JSON body must be an object"}`.
- Duplicate JSON keys (`{"handle":"z","handle":"zz"}`) -> 400
  `{"error":"duplicate key 'handle' in JSON body"}`.
- Register validation: bad handle, 1-char handle, reserved handle
  (`admin`), bad Ed25519 key, `javascript:alert(1)` avatar_url -> all clean 400s.
- Wrong/missing Content-Type on signed endpoints -> 401 (auth gates first),
  as designed.

### 3. Unauthenticated hits on non-public paths
- `GET /mod/flags`, `/mod/uploads`, `/settings`, `/notifications`,
  `POST /mod/flags/1/resolve`, `/mod/uploads/gif/1/approve`,
  `/settings/link-code` -> 302 to login (correct).
- `GET /api/openmic/queue` -> 403; `/api/openmic/mine`, `/api/openmic/1/approve`,
  `/api/notifications`, `/api/memory`, `/api/memory/export`, `/api/memory/wipe`,
  `/api/pets/status`, `/api/shop/balance`, `/api/rewards/invite-code`,
  `/api/webhooks`, `/api/challenges/settle` -> 401 with clear error strings.
- `POST /api/admin/identity/rotate-key`, `/api/agent-memory/store`,
  `/api/swarm/freeze` -> 401 (`bad or missing agent key` / `pilot auth required`).
- `PATCH /api/agent-memory/update`, `DELETE /api/agent-memory/forget` ->
  401. `PUT /vote`, `DELETE /api/forum/react`, `PATCH /submit`,
  `TRACE /` -> 405. `OPTIONS /vote` -> 200 with `Allow: OPTIONS, POST`.
- Public-by-design reads: `GET /api/pond`, `/api/workroom/tasks`,
  `/api/swarm/projects` -> 200 `{"ok":true,...}` with empty lists on the
  fresh DB (no sensitive fields exposed).

### 4. musefm-v1 signature auth edge cases (signed as advqa_probe, react bucket)
- Tampered signature -> `401 "musefm-v1 auth failed: signature does not verify"`.
- Stale (-10 min) and future (+10 min) timestamps -> 401 timestamp-window.
- Unknown fm_id -> 401 unknown fm_id.
- Nonce first use -> 400 unknown target (auth passed, validation hit);
  nonce REUSE -> `401 "replay: nonce already used"`.
- Wrong action name (`vote` sent to `/api/forum/react`) ->
  `401 "wrong action for this endpoint (got 'vote')"`.
- Extra unknown field -> 400 unknown target (does not break signature verification).
- 200 KB oversized bodies -> 400/401 cleanly, no 500s.

### 5. Rate/stress: 20 rapid identical requests
- 20 rapid `fb_react` ("like") taps on post 4: all 200 in 0.75 s, actions
  alternated exactly `added/removed/added/removed...`, ending `removed`
  with `total: 0`. No double-counts, no 500s, no lockups.
- 20 rapid `api_react` (emoji 👍) on post 4: all 200 in 0.35 s, final
  counts `{"👍": 1}` — INSERT OR IGNORE idempotent, no double-count.
  (Cleaned up my row afterwards; 0 rows remain for my handle.)
- Reaction validation edge cases (all signed): bogus emoji, empty emoji,
  bad `target_type`, `target_id` as string / -1 / 0, unknown target ->
  clean 400s (`emoji must be one of: ...`, `unknown target`, etc.), and
  validation runs BEFORE the rate bucket is recorded (no budget burn).

### 6. Share-link / permalink formats
- No dedicated `/share*` routes exist in app.py (share UI is client-side
  share-sheet only). Garbage ids on `/m/<fm_id>`, `/passport/<fm_id>`,
  `/watch/<uid>`, `/gif/<uid>`, `/video/<uid>` -> 404, clean.

### 7. Query-param abuse on public GETs
- `/api/forum/posts?limit=abc|-1|999999999`, `?sort=<script>`,
  `?community=../..` -> 200 with sane clamping (bad community -> empty
  posts array). `/api/shorts?limit=abc` -> 200. No errors.
- Host header `evil.com` / `X-Forwarded-Host: evil.com` -> 200, no
  poisoning visible (links in output stay same-origin for relative paths;
  og:url reflects request host on the 404 template, which is standard
  behavior, not a vulnerability).

## Notes / caveats
- Reaction-picker edge cases were exercised with real musefm-v1 signatures
  against the `react` (120/hr) bucket on validation-only paths
  (target_id=999999 -> "unknown target", no state change); the `post`
  bucket was unavailable due to the shared-IP starvation noted above.
- I did not test human-session (cookie) flows — no browser here; the HUMAN
  TESTER covers those.
- The probe identity `advqa_probe` remains registered in the scratch DB;
  its reaction rows were deleted. The scratch DB is deleted at the start
  of every loop run anyway.
- Server stayed healthy throughout: HTTP 200 on `/` at start and end,
  0 tracebacks in the log.
