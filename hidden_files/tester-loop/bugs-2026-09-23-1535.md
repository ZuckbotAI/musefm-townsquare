# Tester loop triage — 2026-09-23 15:35 CDT

- Commit at run start: `52b82e3` — "mod queue overhaul: bulk select/actions, flag category filters, AJAX no-reload, pagination"
- HEAD moved mid-run to `7b65c41` — "Floating orb dock: fixed overlay on every page, draggable, position persists" (someone else committed while the run was in flight; `git status` shows uncommitted `M app.py`, `M grok_bot_watch.py` — live editing in progress, left untouched). The test server (booted 15:35) ran `52b82e3` code; subagent findings below are against that.
- Local instance: http://127.0.0.1:8473, scratch DB `hidden_files/tester-loop/test.db` (deleted at run start; hermetic)
- Real local DB: untouched (only the known stray instance on port 8479, pid 3105, still points at `townsquare.db` — not mine, left alone)
- Production: READ-ONLY smoke — `/`, `/api/shorts`, `/musefm`, `/shorts` all 200. Live site healthy.
- Three subagent reports: human tester (~15:35 run, handle `qatesthuman`), agent API tester (signed flows), adversarial QA.

**Verdict: quiet run.** No P0s. No brand-new P1s. Two new P2s below.
Per reporting policy: no chat surfacing beyond the daily-log one-liner.

## New P2s (not in last 48h of bug files)

1. **P2 — Bare POST /signals/react 302s to /login?next=/ (loses path) instead of 401.** [adversarial]
   `curl -X POST /signals/react` (no body, no Content-Type) → `302 /login?next=/`.
   JSON-body variant → 401 like every other JSON API (`/api/forum/vote`, `/api/memory/export`,
   `/api/collab`, `/api/trustline/link` all 401). The same endpoint changes its denial shape
   based on Content-Type, and the redirect drops the original path (`next=/` not
   `next=/signals/react`). Machine client gets a redirect instead of a machine-readable denial.
   Proving test added: `test_testerloop_1535_p2s_2026_09_23.py` (tests only, no source edits).
   Confirmed on both live server and Flask test client.
2. **P2 — `limit` query params silently accept garbage, no clamp.** [adversarial]
   `GET /api/forum/posts?limit=-1`, `?limit=0`, `?limit=abc`, `?limit=999999999` — all 200
   (limit=-1/0 → 1 post, 999999999 → uncapped full list). Expected: 400 on invalid or clamp
   to a sane range. On a large production dataset an uncapped limit is a cheap amplification
   vector. Same file proves it (4 checks, all failing).

## New-but-stale test failure (not an app bug)

- `test_gifs.py` "index shows gif thumbnail" now fails. Root cause: commit `d5d2c4b`
  ("Forum feed: Reddit-style inline image/video previews + feed cleanup", 2026-09-23 12:47 CDT)
  intentionally replaced the old 72px `post-gif-thumb` with the new `_post_media.html` partial
  (`class="post-gif"` full-width inline preview). Verified the GIF still renders on the index —
  the test expectation is stale after the intentional UI upgrade. Same class of staleness as
  the `/arena`+`/pro` sidebar failures.

## Deduped (previously reported, NOT new)

- **P2 known — `Server: Werkzeug/3.1.8 Python/3.12.3` version banner** (1235 P2 #5).
- **Known friction — login `Secure` cookie breaks plain-HTTP session reuse** on the test rig
  (human tester's P1; previously documented as harness friction, 0935 — production is HTTPS,
  `SESSION_COOKIE_SECURE = not app.debug` and the loop boots with debug off).
- **P1 KNOWN — comment bodies silently truncated at 2000 chars** instead of 400 (human tester);
  already reported LOUDLY 2026-09-21 22:35 with proving test `test_testerloop_comment_edit_truncate_2026_09_21.py`.
- **Previously-stale suite failures, re-confirmed (not app bugs):** `test_sidebar` (11, arena/pro
  removed by design), `test_links` (arena/pro/routes retired), `test_musefm` (`fb_reactions` import),
  `test_bugfix_csrf` (old-route 307), `test_bio_truncate` (ImportError), `test_workroom_bridge`
  (renamed schema fn), `test_shorts_seen_rotation` (test-vs-design), `test_p1_2026_09_18`
  (stale by design post-XFF fix), `test_listening_rooms` (needs AGENT_KEY — harness-only),
  `test_testerloop_0046_p2s`, plus by-design-failing proving tests
  (`comment_edit_truncate`, `json_notype_400`, `passport_envelope`, `platformkey_envelope`,
  `ratelimit_before_validate`, `rawint_exn`, `bio_truncate`, `post_body_limit`, `newline_preserve`,
  `logincsrf`, `multipart_bool`, `error_messages`, `episode_comment_redirect`, `flag_messages`,
  `1835_fixes`, `1835_flood_wall`).
- `test_identity.py` "episodes read": stale hardcode (expects 6 episodes, checkout seeds 8) — noted 0935.

## Verified clean (highlights)

- Agent signed flows: register, signed post/comment/vote, identity update, signed multipart
  image/gif/video uploads (magic-byte + sha256 binding), passport + signal-credential
  attestations verifying against the platform key; negative battery (tampered body, replay
  nonce, stale timestamp, wrong action, unknown fm_id, unsigned) all correctly 401.
- Human surface: stored XSS escaped everywhere; `javascript:` hrefs neutralized; votes/reactions
  toggle by design; uploads validate magic bytes and reject fakes; validation messages correct;
  429 budgets + Retry-After working; media/episodes/shorts all 200/206 with range support.
- Adversarial: clean branded 404s, traversal/null-byte blocked, malformed JSON → 400 (no 500s),
  wrong methods → 405, zero tracebacks in server log.
- **Stability note:** the local test server stayed up for the entire run — the recurring
  "silent death" phenomenon (reported 09-20 and 09-23 00:35/12:35) did NOT recur this run.

## Environment anomalies (flagged, not acted on)

- Stray instance on port 8479 (`--db townsquare.db`, pid 3105, started 2026-09-23 17:44 UTC)
  still running against the REAL local DB. Flagged in the 12:35 run too. Left untouched.
- Sibling testers share 127.0.0.1 rate buckets: human tester contended the post 5/hr and login
  10/hr budgets with sibling traffic; several post attempts 429'd from combined volume. Consider
  per-tester identity-scoped budgets or staggered starts (noted by human tester too).
- This run's local server (8473) was shut down at the end of the run to leave the port free.

## New failing test file (tests only — no source edits per policy)

- `test_testerloop_1535_p2s_2026_09_23.py` — proves both new P2s above. 6/8 fail on `52b82e3`
  (2 controls pass).
