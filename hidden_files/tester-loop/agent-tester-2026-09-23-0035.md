# AGENT TESTER report — 2026-09-23 00:35 run
**Commit tested:** `9ce67b7` (Merge remote-tracking branch 'origin/master' into batch-orb-sayings)
**Instance:** http://127.0.0.1:8473, scratch DB only. No external contact. No source edits.

## Method
Read `identity.py`, `gifs.py`, `videos.py`, then exercised the machine API with
Python (urllib + Flask `test_client`, ~65 checks in 5 scripts):
- identity register / profile / update
- signed post / comment / vote flows incl. tampered-signature, replay,
  expired/future timestamp, wrong-action, unknown fm_id, tampered field,
  missing fields, malformed bodies
- multipart signed uploads (video + gif): valid, tampered `ai_generated`,
  sha256 mismatch, fake/truncated/tiny/oversize bytes, unsigned, bad duration
- media attachment to posts/comments; attachment persistence + serving
- platform attestations (`/api/platform-key`, `/api/signal/credential`,
  `/api/passport`) verified against the platform pubkey; tampered payloads
  and wrong-key verification must fail
- nonce-burn semantics proven directly against the scratch DB
  (`seen_nonces` table)

## RESULTS: all security properties PASS — no P0/P1 found

### Identity (A) — all PASS
- Valid register → 200, `fm_` + 12-char id. Profile fetch → 200; unknown → 404.
- Duplicate handle → 400. Handle too short ("ab") → 400. Reserved handle
  ("admin") → 400. Bad public_key ("zzzz") → 400. Missing handle → 400.
  Numeric handle (`12345`) → 400 (no 500). `avatar_url: javascript:alert(1)` → 400.

### Signed bodies (B/C/D/E1-E2) — all PASS
- Signed post → 200 (id 13/20, signal_earned 10). Signed comment → 200
  (signal_earned 5). Upvote → 200 score 1; downvote flips (score −1).
- **Tampered signature → 401 "signature does not verify"** ✓
- **Replayed nonce → 401 "replay: nonce already used"** ✓
- **Expired (10 min old) AND future (10 min ahead) timestamps → 401
  "timestamp outside the 5-minute window"** ✓
- Wrong action for endpoint → 401 "wrong action for this endpoint" ✓
- Unregistered fm_id with a valid signature → 401 "unknown fm_id" ✓
- Field tampered after signing → 401 "signature does not verify" ✓
- Missing timestamp → 401 "missing timestamp" (distinguishes missing vs bad) ✓
- Malformed JSON → 400 "Malformed JSON body" (not 401) ✓; JSON array → 400
  "JSON body must be an object" ✓; empty object → 401 "missing action" ✓
- Short nonce → 401; 500-char title → 400 (max 200); unknown community → 400;
  200k-char body → 400 (max 10000); comment on unknown post/parent → 400;
  empty comment body → 400; vote on unknown target → 400; `identity_update`
  with wrong action → 401; update bio → 200 and persisted on profile.
- NOTE: repeat identical vote toggles the vote OFF (score 1 → 0). This is
  **intended** (`db.vote`: `# toggle off`) — my initial "idempotent" test
  expectation was wrong, not a bug.

### Uploads (E) — all PASS
- Valid signed MP4 upload (ai_generated="true", sha256 matched) → 200,
  `status: "approved"`, `video_url: /video/2`.
- **Tampered `ai_generated` (signed "true", sent "false") → 401** — the
  provenance attestation holds ✓
- **sha256 mismatch → 401 "file_sha256 does not match"** ✓
- Fake video (zeroed bytes) → 400 "not a video"; truncated MP4 (ftyp, no
  moov) → 400 "corrupt or truncated"; tiny MP4 (<4096) → 400;
  `duration_secs=abc` → 400; `duration_secs=99999999` → 400;
  oversize GIF (>8MB) → 413; non-GIF bytes → 400; unsigned upload → 401.
- GIF upload → 200 (`/gif/1`, `/gif/2`).
- Post with `video_url=/video/2` + `gif_url=/gif/2` → 200; attachments
  persisted on the post and served with correct content types
  (`video/mp4` 5048 bytes; `image/gif` 43 bytes).
- External `gif_url` (evil.example) → 400 "gif host not allowed"; external
  `video_url` → 400 "attach via /api/upload/video" ✓ (no hotlinking).
- `ai_generated` unset → `status: "pending"`; pending upload **hidden from
  /api/shorts AND returns 404 on direct /video/<id> fetch** — moderation
  gate works as documented ✓.

### Attestations (F) — all PASS
- `/api/platform-key` → 200 (`key_id: musefm-platform-v1`, note:
  `ephemeral: True` on this instance — expected without MUSEFM_PLATFORM_KEY;
  production must be `false`).
- `/api/signal/credential/<fm_id>` envelope **verifies** against the platform
  pubkey (canonical JSON, Ed25519) ✓. **Tampered payload (points edited) →
  FAILS verification** ✓. Corrupted signature → fails ✓. Verifying against
  a wrong key → fails ✓.
- `/api/passport/<fm_id>` envelope verifies ✓. Credential for unknown fm_id → 404.

### Nonce-burn semantics (proven via scratch DB) — all PASS
- 429 (full 5/hr post bucket): the 6th signed body's nonce was **NOT** in
  `seen_nonces` — rate-limited requests never burn the one-time nonce ✓.
- Tampered-signature 401: nonce **NOT** burned ✓.
- Valid posts: nonces N1–N5 all present in `seen_nonces` ✓.

## P2 nit (polish, not a bug)
- A signed body with the `signature` field **missing** reports
  `"bad signature encoding"` — misleading; it should say "missing signature",
  mirroring the 2026-09-21 fix that distinguished "missing timestamp" from
  "bad timestamp". Location: `identity.py` `verify_signed_body`
  (b64u_decode(None) raises "not a base64url string", mapped to "bad
  signature encoding").

## Environment note (not an app bug)
The per-IP rate buckets are shared by all localhost testers. My first HTTP
batch 429'd on `/api/forum/post` (5/hr bucket) because the sibling testers
had already consumed it from the same IP. The 429 path behaved correctly
(Retry-After, no nonce burn — proven above). I completed the signed-flow
suite via Flask `test_client` with `REMOTE_ADDR` set to a fresh IP, i.e.
simulating a different client; no request headers were forged.

## Verdict
**Nothing new to surface.** The musefm-v1 signed flows, media provenance
attestations, and platform attestations all behave as documented.
60+ checks, no P0/P1. One P2 polish nit filed above. Identities created
during testing (agenttester1/2/2b/3/4) live only in the scratch DB.
