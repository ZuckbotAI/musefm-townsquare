# HUMAN TESTER report — Muse FM tester loop, 2026-09-24 09:35 CDT run

- **Target:** http://127.0.0.1:8473 (local scratch instance, DB `hidden_files/tester-loop/test.db`)
- **Commit tested:** `961f41f` ("Add /faq page (Getting Started, Account, Trust, Troubleshooting); expand /facts to 8 Q&As") — same commit as the 03:35 and 06:35 runs.
- **Tester persona:** curious, slightly mischievous first-time human
- **Method:** ~90 HTTP checks via `requests` with a manual cookie jar (SESSION_COOKIE_SECURE over HTTP — requests' cookie policy won't send Secure cookies back; prior runs hit the same quirk).
- **Test identities:** `mischiefht475` (posts /c/lobby/post/11), `mischiefht765` (posts /c/lobby/post/13), `mischiefht424`, `dbgXXXX` — all in this run's scratch DB only.

## Verdict: NO P0, NO P1, NO new P2s. One filed P2 confirmed FIXED (not reproducible).

## New bugs

None.

## Fixed P2 (previously filed, no longer reproduces — drop from dedupe list)

1. **"Share dialog says 'Share this episode' on forum thread pages" (filed 2026-09-24 06:35).**
   Re-check on this run: `GET /c/lobby/post/13` (200) — the `#share-sheet` heading is now `<h3 id="share-title">Share</h3>` (generic), and `static/js/app.js:63` sets context copy at open time: `kind === 'episode' ? 'Share this episode' : kind === 'thread' ? 'Share this thread' : 'Share'`. `grep "Share this episode" templates/` → zero hits. Same commit `961f41f` as the 06:35 run — the 06:35 observation is not reproducible on the current tree. Resolved.

## Verified working (all intended behavior)

- **Core human journey:** signup (200 success page with one-time Ed25519 private key) → dup handle 400 ("taken") → login 302 → `/api/csrf-token` 200 → `/submit` 302 to `/c/lobby/post/13` → comment 302 → thread renders comment.
- **Vote semantics (`POST /vote`):** JSON vote 200 with `my_vote`/`score`; rapid repeat same-value votes all 200 (toggle-idempotent, single row); `value=2` → 400; `value=0` → 400 ("value must be 1 or -1" — unvote is repeat-same-value toggle-off, verified in db.py); `target_id=-5` → 400 "unknown target"; `target_id=abc` → 400; bogus `target_type` → 400. Anonymous JSON `/vote` → 401.
- **React (`POST /signals/react`):** `fire` add → 200 (counts), repeat → 200 removed (toggle). (Bogus/emoji reaction-name 400s blocked by burned login bucket this run — verified 03:35 on same commit.)
- **XSS (all via comments on /c/lobby/post/13 — post bucket was burned, see env notes):** 12 probes, all stored and rendered: `<script>alert()</script>`, `<ScRiPt>` mixed-case, `<img src=x onerror=...>`, `<svg onload=...>`, `[evil](javascript:alert())`, bare `javascript:alert(1)`, `data:text/html,<script>`, `file:///etc/passwd`, `<b>/<i>` tags, SQLi string, 300-emoji flood. Thread HTML contains **zero** raw `<script>alert`, zero `href="javascript:`, zero `href="data:`, zero `href="file:`, zero live `<svg onload`, and the two `onerror=` occurrences are escaped text (`&lt;img src=x onerror=...` inside the comment body div and a prefilled reply textarea — no live attribute). Legit `https://example.com` → linkified with `target="_blank" rel="noopener nofollow"`. Escaped payloads visible as text (`&lt;script&gt;`, `&lt;img`, `&lt;b&gt;bold&lt;/b&gt;`).
- **Upload validation:** real PNG → 302 `?pending=1`; fake PNG (text bytes), SVG-with-script, 30MB PNG → 400; traversal filename `../../evil.png` → 400, no crash.
- **Auth gates:** anonymous `/submit`, `/photos/upload`, comment POST, episode comment → 302 `/login?next=…`; anonymous JSON `/vote` → 401 "sign in to vote"; missing CSRF on authed comment → 403; `?next=http://evil.example/x` on login → 302 to `/` (open redirect closed).
- **ID/route edge cases:** `/c/lobby/post/-1`, `/c/lobby/post/abc`, `/c/lobby/post/99999999` → 404; `POST /post/99999999/comment` → 404; `GET /no-such-route` → 404 HTML; `GET /api/no-such-endpoint` → 404 JSON; `PUT`/`DELETE /post/13` → 404; no `/search` route exists (SQLi probes against it were invalid — 404 correct).
- **Length boundaries:** 10001-char body → 400; 201-char title → 400; whitespace title → 400; 500-emoji comment accepted, rendered, no crash.
- **Anonymous page checks:** `/faq`, `/facts`, `/musefm`, `/shorts` → 200; `/welcome`, `/submit`, `/photos/upload` → 302 login (intended); `/logout` GET → 405 (logout is POST-only — intended; the filed logout-CSRF P2 is about the POST path, still present per 03:35).
- **Media:** episode page 200, `/audio/*.mp3` HEAD 200 `audio/mpeg`, `Range: bytes=0-99` → 206 with correct `Content-Range: bytes 0-99/<len>`.
- **Rapid fire:** 15 rapid `GET /` → all 200. Mobile iPhone UA → 200 with `name="viewport"` meta.

## Harness artifacts / disproven leads (NOT app bugs — do not file)

1. **"XSS post rejected"** — my post attempts 429'd because the shared per-IP `post` bucket (5/hr) was burned mid-run by the parallel adversarial tester (~40 unauthenticated `/api/forum/post` attempts, same class as the 06:35 observation). Covered XSS via the comment path instead; same-markdown sanitizer for posts was verified on this commit at 03:35.
2. **"`value=0` unvote 400"** — intended; `db.vote` requires 1 or -1, unvote = repeat same value (toggle-off in db.py:1164). My expectation was wrong.
3. **`GET /logout` 405** — intended; logout route is POST-only.
4. **"Search SQLi 404"** — no `/search` route exists; invalid probe.
5. **First-pass signup 400s** — the confirm field is `password_confirm`, not `confirm`; the dup-handle "PASS" in my first pass was a password-mismatch 400, not a dup rejection. Re-ran correctly: dup handle → 400 "taken".
6. **Agent-signed endpoints (`/api/forum/vote`, `/api/signals/react`) 401 "missing action"** — those are the agent signed-API paths; the human session paths are `POST /vote` and `POST /signals/react`. Harness routing error, not app error.

## Environment notes (for the loop coordinator, not app bugs)

1. **Shared per-IP rate buckets burned each other mid-run.** This run: `post` (5/hr) 429'd my XSS-post probes (~40 unauthenticated `/api/forum/post` hits from the parallel adversarial run — same bucket); `human_login` (10/hr) 429'd my later logins, blocking nested-comment/bad-parent/bogus-reaction re-verification via HTTP (all verified 03:35 on this commit, and nested comments render fine); `vote` (120/hr) 429'd an anonymous comment-vote attempt — sibling interference, not this tester's usage. Standing recommendation from prior runs stands: per-tester ports/DBs or XFF separation.
2. **Signup bucket (5/hr) burned by my own usage** — 5 successful signups this run; later signups 429'd.
3. **Scratch DB residue:** identities `mischiefht475`, `mischiefht765`, `mischiefht424`, `dbgXXXX`; posts pids 11, 13 (+1 more submit); ~15 XSS-probe comments, votes, reactions, 1 pending photo upload — scratch DB only.
4. **Working tree is dirty** (`git status` shows modified app.py, db.py, identity.py, onboard.py, pets.py, row.py, grok_bot_watch.py + state jsons) — other workers' uncommitted changes; I changed nothing and committed nothing.

## What I did not cover

- Nested-comment parent_id edge cases and bogus/emoji reaction names via HTTP (login bucket burned; both verified 03:35 on this commit).
- Post-title/body XSS via HTTP (post bucket burned; comment-path XSS verified, post-path verified 03:35 on this commit).
- `/submit` multipart image-attach, signed agent flows, listening rooms, pets, workrooms, bounties, shop — agent lane / out of scope.
- True mobile layout — HTTP-level only.

## Bottom line

No P0s, no P1s, no new P2s on `961f41f`. The one notable outcome: the 06:35 "Share this episode" P2 no longer reproduces — base template renders generic "Share" and JS sets context copy. XSS escaping, upload validation, auth gates, CSRF, vote/react semantics, length boundaries, and 404 handling all held under mischievous probing. Per the loop's reporting rule (silent on P2-and-below), this report is filed to `hidden_files/tester-loop/` only — no chat surfacing beyond the routine log one-liner.
