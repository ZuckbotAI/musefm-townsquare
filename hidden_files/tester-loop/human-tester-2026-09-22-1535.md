# Human tester report — Muse FM local instance (http://127.0.0.1:8473)

Commit tested: `891fab7` ("P1 XFF rate-limit bypass fix: gate ProxyFix on RENDER_GIT_COMMIT")
Account: `qa_mischief` (identity `fm_XK_9QtXBO3U5`, display "QA Mischief"). All artifacts mine
(post #8, comments #5/#11/#14/#15, photo #3). Production untouched. Scratch DB only.

## What worked well
- Signup → one-time-key page → login → session cookie → handle chip. Clean.
- Post create (302 → /c/lobby/post/8), comment, nested comment (parent_id), vote, react — all 200/302.
- Logout is POST-only + CSRF'd, clears session; /submit then 302s to /login?next=/submit.
- Input handling is strong: title/body/comment HTML-escaped on pages; linkify only linkifies
  `https?://` (target=_blank rel="noopener nofollow"); `javascript:`/`data:`/`ftp:` stay inert;
  @mentions linkify only for registered handles.
- Validation everywhere: dup handle 400, bad-char handle (`qa<script>mischief`) 400,
  wrong password 401, 10k-char post body → 400 "too long", 2000-char comment → 302
  (stored at full 2000 chars — no silent truncation), 2001-char comment → 400,
  fake PNG → 400 "not a recognized image", bad parent_id → 400 "unknown parent comment",
  invalid reaction → 400 allowlist message, anon /api/forum/comment → 401.
- Votes: same-value re-vote toggles off (by design); score recomputed from votes table.
  20 rapid parallel votes on one comment → all 302, zero residue rows, no spurious 429
  (BEGIN IMMEDIATE atomicity holds under concurrency).
- Photo upload: magic-byte checked, traversal filename (`../../evil-traversal.png`) ignored —
  stored as generated `photos/photo-3.png`, status=pending (mod gate). No stray files.
- Media: /watch/1 200 with <video> player, /video/1 206 on Range, /audio/*.mp3 206 on Range,
  bad episode slug 404, /api/shorts + /api/episodes JSON sane.
- Mobile: viewport meta present, 19 @media queries in style.css.

## NEW findings
1. **P2 — /api/forum/post/<id> returns the title/body RAW (unescaped) in JSON.**
   Post #8 title `QA hello world <script>alert(1)</script>` renders escaped (`&lt;script&gt;`)
   in HTML pages, but the JSON API returns it verbatim. Any consumer that innerHTML-injects
   API fields gets stored XSS. Either escape at the API boundary or document that consumers
   MUST escape. Repro: `curl http://127.0.0.1:8473/api/forum/post/8` → `"title":"QA hello world
   <script>alert(1)</script>"`.

## FIXED since last run (verified live)
2. **Episode-comment redirect P2 (filed 09-22 09:35) is FIXED.** POST
   `/episodes/ep01/comment` and `/episodes/agents-humans-future/comment` now 302 back to
   their own episode page (template passes hidden `next=/episodes/<slug>`, `_safe_next`'d).
   Old behavior (`/episodes#ep01`) no longer reproduces.
3. **XFF rate-limit bypass P1 (filed 09-21 15:35) is FIXED in this commit.** `app.py:123`:
   `ProxyFix` now runs only when `RENDER_GIT_COMMIT` is set; locally `client_ip()` reads
   REMOTE_ADDR only. Code-verified; not re-exploited.

## Still open (re-observed, not re-reported)
- **Secure-cookie-on-plain-HTTP P1: STILL LIVE.** Login Set-Cookie carries
  `Secure; HttpOnly; SameSite=Lax` over plain HTTP (`app.py:365`,
  `SESSION_COOKIE_SECURE = not app.debug`, debug off). Spec-compliant clients drop it;
  curl works only because it ignores Secure. (Held-branch fix `6b46823` still unmerged.)
- Shared-localhost rate-limit pain: /submit is 5/hr per client IP; my second post attempt
  got 429 because sibling tester subagents share the 127.0.0.1 bucket (prior runs noted
  this; consider a harness-exempt header).

## Notes
- Comment #15 (2000 z's) verified at full length in DB — the 09-21 silent-truncation P1
  does not reproduce on this path.
- No tracebacks observed in server.log during this run's traffic.
