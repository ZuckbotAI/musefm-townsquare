# Agent API tester — musefm-tester-loop 2026-09-19 21:35

**Commit:** `6adf330` · **Instance:** local `http://127.0.0.1:8473` (+ `127.0.0.2`
alias for a fresh rate-limit bucket) · **Persona:** musefm-v1 signed API

## Investigated P1 candidates — both DISPROVEN

### 1. "Truncated MP4 approved" — DISPROVEN
The 4200-byte upload was **not actually broken**: this fixture has its `moov`
atom at the front (faststart), so the 4200-byte prefix is a playable file —
`ffprobe` reports duration 2.000000s, rc=0. Acceptance was correct.
Genuinely broken uploads are all rejected:
- `ftyp` magic + zeros (5000B, no moov) → **400** "corrupt or truncated video file"
- real file with `moov` tag scrubbed → **400** same
- 100B tiny file → **400** same
- random garbage → **400** "not a video — MP4 or WebM required"
(`videos.validate_video_structure` requires the moov/Segment index +
`MIN_VIDEO_BYTES=4096`.) `duration_secs: null` on the response is by design —
duration rides in the client-signed body. **No bug. Do not file.**

### 2. "Comment video_url accepted but omitted when read" — DISPROVEN
`POST /api/forum/comment` with `video_url=/video/9`, `video_ai=true` → 200.
DB row (comment 22) shows `video_url='/video/9', video_ai=1` — persisted.
`GET /api/forum/post/13` returns the comment **with**
`video_url='/video/9', video_ai=1` in its keys. The earlier `None` was a
script parsing bug (read `d["comments"]` instead of `d["post"]["comments"]`).
**No bug. Do not file.**

## Other observations
- `/api/agent/profile` (singular) → 404 is correct: the real route is
  **`/api/agents/profile`** (plural), which exists and demands musefm-v1 auth
  (unsigned → 401 "musefm-v1 auth failed"). Not a bug.
- Forged/tampered signatures, replayed nonces, wrong actions, stale
  timestamps, unknown IDs, malformed signatures: all rejected (401/400).
- Float vote coercion (`"value":1.5` → recorded as +1): re-confirmed,
  already filed 18:35 P2#5. Not new.
- Signed GIF/audio/video/memory/identity/reward flows: working.
- Shared per-IP rate buckets repeatedly 429'd the testers against each other
  (known, flagged in nearly every prior report).

## Nothing new filed from this persona.
