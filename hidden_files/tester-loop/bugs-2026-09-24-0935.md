# Tester loop triage — 2026-09-24 09:35 CDT

- Commit at run start: `961f41f` — "Add /faq page (Getting Started, Account, Trust, Troubleshooting); expand /facts to 8 Q&As"
- Local instance: http://127.0.0.1:8473 (port free at boot), scratch DB `hidden_files/tester-loop/test.db` (deleted at run start; hermetic)
- Real local DB: untouched. Production: READ-ONLY smoke — `/`, `/api/shorts`, `/musefm`, `/shorts` all 200. Live site healthy.
- Three subagent reports: human tester (`qa_mischief` lane), agent API tester (51/51 signed-flow checks), adversarial QA.
- Proving test added (tests only, no source changes): `test_testerloop_p2s_2026_09_24.py` — both new P2s fail as designed.

**Verdict: quiet run.** No P0s. No brand-new P1s. Two new P2s below + one new P3 note.
Per reporting policy: no chat surfacing beyond the daily-log one-liner.

## NEW P2s (not in last 48h of bug files)

1. **P2 — POST /api/memory returns 201; every other signed write returns 200.** [agent tester; coordinator-verified in code; proving test fails as designed]
   Repro: signed body `{"action":"memory_write", ...}` → `POST /api/memory` → `201 {"ok": true, "entry": {...}}`.
   Expected: 200, matching post/comment/vote/upload/identity_update/heartbeat (signed family) — app.py:2717 returns 201 while the other signed write routes return 200. Functionally fine; API-consistent clients asserting 200 will misfire.
   Code: `api_memory_create` → `return jsonify({"ok": True, "entry": entry}), 201`.

2. **P2 — Upload `file_sha256` mismatch returns 401; it's a content error, should be 400.** [agent tester; coordinator-verified in code; proving test fails as designed]
   Repro: signed `upload` envelope where `file_sha256` doesn't match the uploaded bytes → `401 {"error": "file_sha256 does not match the uploaded bytes"}`.
   Expected: 400 (like "not a gif" and other client-content errors). 401 masks real auth debugging.
   Code: app.py:6572 (gif), 6839 (image); video shares the same line pattern.

## NEW P3 (note, not a bug)

- **P3 — `/api/latest.json?channel=...` silently ignores `channel`.** [adversarial; coordinator-verified]
  The documented param is `community` (docs.html: `/api/latest.json?community=&limit=`); unknown `community` → 404, which works. `channel` was never a musefm param (it's the musebook API's name — carried over from the other platform). Silent-ignore of unknown params is standard here. Not filed as a bug; kept as a cross-platform-muse footgun note: a muse ported from musebook filtering by `channel` gets the unfiltered feed with no error.

## FIXED since 06:35 (strike from dedupe list)

- **06:35 P2 #1 "Share dialog says 'Share this episode' on thread pages" — FIXED in this tree.**
  Verified live this run: `GET /c/lobby/post/15` serves `<h3 id="share-title">Share</h3>` (generic), and `static/js/app.js:63` sets context-appropriate copy ("Share this episode" / "Share this thread" / "Share"). [human tester; coordinator re-verified live]

## Re-observed (previously filed, still present — new instances this run, NOT new bugs)

1. **`_int_field` null coercion — new instance: explicit `value=null` on `/api/forum/vote` records an upvote.** [adversarial; coordinator-verified in code]
   Filed: 2026-09-23 21:35 P2 ("Omitted vote `value` silently records an upvote" — missing field). This run: explicit `null` → also 200 with `{"score":1}`.
   Root cause identical (`_int_field`: `if v is None or v == "": return default`). New detail this run: the docstring at app.py:524 explicitly promises "nulls ... are a clean 400" — the code contradicts its own docstring. Null-vs-missing distinction needed.

2. **Silent limit clamping — `limit=0` → 1 post on `/api/forum/posts`.** [adversarial]
   Filed: 2026-09-23 15:35 P2 #2 (`limit=-5/-1/0/abc` → 200, 1 post). Same behavior, same line (clamp `min(100, max(1, int(limit)))`). Not new.

3. **Rate budget burns before auth/validation — new detail: signed-route 429 masks 401s.** [agent tester P3]
   Filed class: 2026-09-21 15:35 (react budget), re-observed 09-24 03:35 (120-invalid-react) and 06:35 (40 unauthenticated posts → hour-long 429). This run's new detail: on an exhausted shared-IP bucket, wrong-action/tampered/empty signed bodies to `/api/forum/post` return 429 instead of their 401s — a debugger footgun, since 429 on a signed endpoint says nothing about auth. By design (a 429 never burns the one-time nonce); not a bypass.

## Deduped (already on file, unchanged)

- `Server: Werkzeug/3.1.8 Python/3.12.3` version banner — filed 09-23 12:35 P2 #5.
- All by-design-failing proving tests (`test_testerloop_1535_p2s`, `1835_fixes`, `1835_flood_wall`, `error_messages`, `passport_envelope`, `platformkey_envelope`, `ratelimit_before_validate`, `rawint_exn`, `logout_csrf`, `2135_p2s`, etc.) — each proves an already-filed P2. Still failing as designed.

## Repo suites (per-file pytest; 09:35 run)

All failures triaged — every one is a stale test after an intentional change, a known cascade, or a collection-blocker. No new failures vs 06:35:

- **Signup-without-email cascade** (commit 454ba91 made email mandatory, 2026-09-23): `test_testerloop_1835_fixes_2026_09_19`, `test_testerloop_flag_messages_2026_09_19`, plus the previously-triaged set. Underlying features verified healthy: scunthorpe filter (`has_banned("snigger")`=False, standalone slur blocked), comment 2000-char cap via `db.loud_limit`, signup WITH email → 200.
- **`/row` serves village.html (3D canvas)** — `test_row.py` 7 fails are stale (legacy 2D `row.html` intentionally retired). Row APIs healthy: `/api/row/feed` → 200, `/api/row/standing` + `/api/row/player` → 401 logged-out (auth-gated as designed), `/row` → 200.
- **"MuseFM" one-word rebrand + orb favicon** (commits 75a6a46, 86e3684): `test_brand.py` fails — stale, expects "Muse FM" with space / old favicon.
- **Dead/stale**: `test_musefm` (`import fb_reactions` — module removed, commit d2e9861), `test_workroom_bridge` (`ensure_workroom_events_schema` renamed), `test_shorts_seen_rotation` (test-vs-design), `test_links` (/arena 404 + /pro 301 — retired), `test_tidepal_homepage` (regex grabs first band).
- `test_community_episodes.py`: module-level `sys.exit` kills pytest collection (script-style; run as script — known).

## Persona reports (scratch DB only)

- **Human:** full signup→login→post→comment→vote→react→upload journey green. 12 XSS comment probes (script tags incl. mixed-case, img-onerror, svg-onload, javascript:/data:/file: URLs) all neutralized; legit links linkified. Upload validation solid (fake PNG, SVG-with-script, 30MB, traversal filename all 400). Anon gates exact (302/401), missing CSRF 403, open redirect closed, length boundaries enforced, 404s clean, mobile UA + viewport OK, episode Range 206 correct. Confirmed intended: `value=0` vote → 400 (unvote = repeat same value, verified in db.py); `GET /logout` → 405 (POST-only); no `/search` route; anon `/submit` 302s don't burn the post bucket. Filed-P2-fixed finding above. Caveat: adversarial tester's bursts burned shared per-IP post/vote buckets mid-run (scratch-only), so some HTTP re-verifies were deferred — XSS covered via comment path instead.
- **Agent:** 51/51 checks. Signed flows green: register (dup handle → 400, bad pubkey → 400), signed post/comment/vote, GIF+image+video uploads via signed multipart (file_sha256 binding, ai_generated attestation; truncated MP4 → 400), memory write + signed-query read, identity_update, rewards heartbeat, invite-code. Platform attestations verify cryptographically (`/api/signal/credential/<fm_id>`, `/api/passport/<fm_id>` against `/api/platform-key` `musefm-platform-v1`). 15 negatives all correct 401s (replay incl. signed GET, ±10-min timestamps, corrupted/wrong-key sigs, action mismatch, tampering, unknown fm_id, short/missing/garbage sigs, fm_id/key swap, unsigned, sha256 mismatch). Findings above.
- **Adversarial:** zero 500s across ~110+ probes. Ed25519 held vs replay/stale timestamps/action swaps/fm_id swaps. Emoji whitelist rejected scripts/SQL/type-confusion. Traversal, `/.env`, `/console` → 404. Admin routes → 401/405. Rate limiters engaged (vote 120/hr → friendly 429 with Retry-After; identity 10/hr blocked QA mid-run). 404s clean (JSON for API, HTML for pages), malformed JSON → 400, odd methods → 405, security headers present. Caveat: vote-bucket burst burned the shared per-IP vote 120/hr bucket (scratch-only, ~1h).

## Caveats

- The tree is dirty (Anthony's ongoing work, branch agent-onboarding-api) — uncommitted, as required; nothing committed or branch-switched this run.
- Bug reports live in `hidden_files/tester-loop/` (uncommitted) — the branch is Anthony's to review and change.
- Agent tester issued one read-only GET `/` to production during a connectivity sanity check before re-reading the constraint; stopped immediately, self-reported, no further prod contact from any tester.
- Test instance on :8473 killed at run end to keep the port free for the next run.
