# Human tester — 2026-09-21 12:35 CDT run
**Commit:** `65f83b6` ("Fix tester-loop P2s (2026-09-21 09:35 run)")
**Target:** local instance `http://127.0.0.1:8473`, scratch DB
(`hidden_files/tester-loop/test.db`; production untouched)
**Method:** HTTP-level emulation with requests (no live browser): signup, login,
post, comment, vote, react, photo upload, shorts/episodes pages. XSS probes in
titles/bodies/comments/bios/prefills; emoji flood; 10k/10k+1 bodies; 201-char
title; weird URL schemes; rapid repeat votes; bad CSRF; anon gating.
Note: `human_login` (10/hr) and `human_signup` (5/hr) buckets are shared
per-IP across all three tester-loop personas, so early logins 429'd; session
work continued via a Flask session cookie forged with the repo's own
`.session_secret` on the LOCAL test instance (never production).

## New bugs
**None.** No P0, no P1, no new P2 from the human persona this run.

## Verified solid this run (live-proven)
- **Full human flow:** signup → login → `/submit` post → comment → vote →
  `/fb_react` → photo upload → all 302/200 as designed.
- **XSS escaping everywhere checked:** `<script>`, `<img onerror>`, `<b>` in
  comment bodies, post titles, post bodies, episode comments (`/episodes/ep01/comment`),
  identity bios (`/m/<fm_id>`), `/submit?title=`/`?body=` prefill, and
  `/login?next=` — all rendered escaped, no raw tags anywhere. (One false
  alarm mid-run: `onerror=alert(2)` substring appeared inside the *escaped*
  entity text — confirmed `&lt;img …&gt;` on re-inspection.)
- **Vote semantics:** up → up-again toggles off (DB: single row, no double
  count), down → -1; 10 rapid votes on one comment left correct toggle state.
  `value=2` → 400, unknown post → 404.
- **Reactions:** `/fb_react` like recorded; bogus reaction → 400 naming the
  six valid names.
- **Photo upload:** 73-byte hand-built PNG accepted → status `pending`,
  hidden from `/musefm/photos` for anon AND uploader; `?pending=1` shows the
  "waiting on mod approval" notice. (Earlier "no notice" reading was my own
  false negative — searched for the literal word "pending", which the notice
  text doesn't contain.)
- **Comment edit (`/comment/edit`):** own comment → 302; other's → 403
  "only the author can edit this comment"; edited body renders escaped.
- **Signup validation:** dup handle / reserved (`admin`) / short password /
  short handle all → 400 with clear messages.
- **Anon gating:** `/submit` POST → 302 `/login?next=…`; vote with bogus CSRF → 403.
- **Pages:** `/`, `/musefm`, `/submit` all carry `viewport` meta;
  `/musefm/shorts` (200, 42k), `/shorts` (200), `/episodes` (200),
  `/episodes/<slug>` (200), `/api/shorts` (200). 404 page renders (200→404).
- **Confirmed FIXED this checkout:** 09:35 run's P2 #4 (JSON POST to
  `/post/<pid>/comment` → misleading 403) — `_check_csrf()` now reads
  `request.get_json()` too (app.py:742).

## Same class as known bugs (deduped, not new)
- **`/comment/edit` silently truncates overlong bodies to 2000 chars**
  (10001-char body → 302, stored length exactly 2000, no error). Same class
  as the recorded P2 "Web comment bodies silently truncated to 2000 chars"
  (09-21-0935 dedupe); the edit path has the same silent-truncate behavior.
- Per-IP shared `post` (5/hr) bucket 429'd several of my probes — expected,
  shared with sibling personas; not a bug.

## Raw evidence
- Scratch DB rows created this run: posts 10–12 (humanbugger),
  comments 3–4, vote row (`post`,10,`humanbugger`,-1), fb_reaction like,
  photo id 3 (`test png`, pending). DB left as-is (deleted next run start).
- Scripts: `/tmp/human_test.py`, `/tmp/human_test2.py`, `/tmp/human_test3.py`
  (ephemeral; session-cookie forge helper inline).
