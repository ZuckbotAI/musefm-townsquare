# Tester loop triage — 2026-09-23 21:35 CDT

**Commit tested:** `db9313d` ("Agent Onboarding life systems: memory, attachment, nudges, missions (2026-09-23, Anthony)")
**Test instance:** local, http://127.0.0.1:8473, scratch DB deleted at run start
**Production smoke (read-only):** `/`, `/api/shorts`, `/musefm`, `/shorts` → all 200 ✓

**⚠️ MID-RUN INSTANCE DEATH + TREE SKEW:** the coordinator's 21:36 CDT instance died ~21:47 CDT (externally killed — no traceback in server.log; process simply gone). At the same minute an unknown sibling process booted `app.py --port 8472` (see integrity flag below). Between ~21:47–21:49 the working tree's app.py carried a **SyntaxError at line 3712** from live edits by another track; fixed by ~21:50. The human tester observed the outage, investigated, and rebooted a fresh instance on 8473 at 21:49 (same scratch-DB path, hermetic fresh DB; its pre-21:47 test residue was lost — scratch, no impact). Findings from all three personas span both instances; the code under test is the same tree modulo the 21:47–49 edit window.

**⚠️ INTEGRITY FLAG (not this run's doing):** a sibling job is running `.venv/bin/python app.py --port 8472` with the DEFAULT `--db` — it has the REAL `~/workspace/musefm-townsquare/townsquare.db` open (verified via /proc fd). That violates this loop's hard boundary (real local DB is scratch-only). Not mine to kill — flagging for the main agent: find the owning job and move it to a scratch DB.

## Verdict: QUIET — no P0s, no new P1s

## Changed this run (verified live by coordinator)

- **Known P1 CLOSED (in-tree, still uncommitted): comment truncation.** The 09-21 P1 (comment CREATE/EDIT silently truncated at 2000 chars) is now a clean rejection: 2001-char web comment → `400 "comment body too long (max 2000 characters)"`, nothing stored. Verified via curl on the running instance (login → CSRF → POST /post/12/comment). The `loud_limit()` fix covers web post-comment, episode comments, and signed-API routes per the tree. Still uncommitted — Anthony's call.

## New findings this run

1. **P2 (nit) — 302 instead of 303 after mutating POSTs.** `POST /submit` → 302 to `/c/lobby/post/<id>`; `POST /photos/upload` → 302 to `?pending=1`. Strict HTTP clients re-POST the redirect (curl lands on a 405 from the thread page); browsers convert to GET so no user impact. 303 would be cleaner. (human tester; new — not in last 48h bug files)

## New failing test file (tests only — no source edits per policy)

- `test_testerloop_2135_p2s_2026_09_23.py` — proves new P2 #1: POST /submit returns 302 (expected 303). Run: 0 passed, 1 failed as designed. Uses `ensure_human_auth_schema` in setup (mirrors app startup; hermetic TestClient DBs don't run the boot migration).

## Deduped (already on file in last 48h, NOT new)

- Self-vote permitted (product call, not a bug — filed).
- Vote/react "unknown target" 400-vs-404 inconsistency (filed 09-22).
- Adversarial P2s re-observed: 405/global-413 → Werkzeug HTML instead of JSON envelope (filed: "Error-page inconsistency (405/400/404)"); 404 HTML-vs-JSON by path type (filed); `/api/openmic/queue` unauthenticated → 403 "mod session required" vs 401 convention (filed as convention nit — `_require_mod()` deliberately 403s any non-mod).
- `/watch/<uid>` (long-form theater) marks the sidebar "Shorts" link active — already filed ("The Shorts sb_link activates on /shorts or /watch, but not /musefm/shorts"). Real nit, still open.
- Fake.svg filename label mismatch, vote-without-value default, double-vote toggle signal, emoji error-string inconsistency, missing-signature wording, per-IP shared rate-limit buckets — all filed 09-23 18:35, still open.

## New stale-test instances (tests need refresh — app verified healthy)

- `test_sidebar.py` (run as script): 106 pass / 19 fail. 8 account-block fails cascade from signup-without-email (400). 10 "section links" fails = expected `/arena`, `/pro`, `/musefm/shorts` sidebar hrefs that no longer exist (Arena retired 09-21; /pro consolidated under Links — intentional). "fm shorts highlights FM Shorts" = stale (no "FM Shorts" sidebar item anymore; /musefm/shorts uses a topbar link).
- `test_brand.py`: 24 pass / 3 fail — aria expectations stale after tonight's brand/wordmark redesign (approved): label is now `aria-label="MuseFM home"` (no space) and the wordmark moved into the `_brand.html` partial. The accessible label still exists; purely test-stale.
- `test_links.py`: 16 pass / 11 fail — all `/arena` and `/pro` (retired/consolidated; /arena 404, /pro 301). Stale.
- `test_orb_snap.py`: 30 pass / 4 fail — greps for pre-redesign `orb-dock.js` strings (`$('#orb-home')`, `FLOAT_SCALE`); the 21:04 "never-cover" rewrite (Anthony's hard requirement) changed the implementation legitimately. Stale.
- `test_musefm.py`: crashes at import — `import fb_reactions` on a module that was removed (commit d2e9861 cleanup). Dead test file.
- `test_p1_2026_09_18.py`: 2 fails — (a) `init_db creates table fb_reactions` (dead module, stale); (b) "different proxy-supplied IP keeps its own bucket" — stale expectation: the 09-21/22 P1 fix intentionally ignores X-Forwarded-For entirely off-Render (ProxyFix only when RENDER_GIT_COMMIT set). Proven intentional by `test_testerloop_xff_direct_2026_09_22.py` → 4/4 pass.
- Signup-without-email cascades (commit 454ba91 made email mandatory): `test_row.py` 4 avatar fails (pre-existing pins), `test_auth.py`, `test_human_usability.py`, `test_identity.py`, `test_kind_tags.py`, `test_pet_onboarding.py`, `test_pet_presence.py`, `test_p1_csrf_human_handle_2026_09_18.py`, `test_workroom_pilot.py`, `test_workroom_private.py`. App verified healthy: signup WITH email → 200 + row created; verification-email failure degrades gracefully locally.
- `test_listening_rooms_2026_09_21.py`: AGENT_KEY env guard — pre-existing harness note.
- `test_community_episodes.py`, `test_shorts_seen_rotation.py`: module-level sys.exit — pytest can't collect; ran as scripts (known).

## Repo suites (coordinator runs, throwaway DBs / TestClient)

- pytest-structured files: all pass except the pre-existing `test_row.py` 4 pins.
- ~105 script-style files run as standalone scripts: all pass except the stale-test instances above. Full per-file log: `hidden_files/tester-loop/suite-results.txt`.

## Persona reports (scratch DB only)

- **Agent tester:** ~50 checks, all green. Signed flows (register/post/vote/video+audio upload with sha256 + ai_generated provenance), envelope re-verification (activity/signal/passport — tampered fails), and 13 attack negatives (replay/nonce/timestamp/wrong-key/action-mismatch/unknown-fm_id/tampered-payload) all correct. Caveat: shared per-IP post bucket (5/hr) got exhausted by the parallel personas — suggests per-persona XFF or separate IPs for hermetic parallel QA in future loops.
- **Adversarial QA:** ~200 probes. 404 taxonomy, malformed JSON → 400s, auth walls (memory/admin/agent-memory/pet/forum all 401), method mismatches → 405, rate-limit behavior (malformed requests don't burn budget, 429 with Retry-After + friendly message), uploads (magic bytes, sha256, 413 caps), limit clamping, traversal blocked, zero stack traces. Three P2s — all already filed (see deduped).
- **Human tester:** full journey green. Signup boundaries exact (script/reserved/emoji/length); stored XSS escaped everywhere (bio, titles, comments); `javascript:` URLs not linkified, https linkified with `target="_blank" rel="noopener nofollow"`; comment validation exact (10k chars → 400 "comment body too long (max 2000 characters)" — the known P1 now rejects, confirming the fix); votes toggle sensibly (double +1 → off, `my_vote:null`); signals widget works (double-signal toggles off, bogus → 400 with allowed-values list); photo upload (valid PNG → mod queue, fake → 400, empty → 400); episode playback 200/206 with traversal blocked; comment rate limit 30/hr with friendly 429; login gives no user enumeration; 404s clean. Mobile: iPhone UA → 200s, viewport meta present (true layout needs a real browser). Found only the 302-vs-303 nit (new P2 above). Also recommends: the "delete test.db at run start" step is racy with 3 testers sharing one DB — coordinate lifecycle or give each tester its own DB/port.

## Coordinator verifications (live, running instance)

- Photo upload flow healthy end-to-end: login → GET form w/ CSRF → POST multipart PNG → 302 `?pending=1`. The human tester's transient 403 "bad form token" was a curl -L/session quirk — not reproducible in a clean session.
- Stored XSS escaped everywhere checked (`<img src=x onerror=alert(1)>` → `&lt;img …&gt;` in profile HTML).
- Linkification scheme-safe; episode playback 200/206 with traversal blocked.

## Caveats

- Bug reports live in hidden_files/tester-loop/ (uncommitted) — the branch is Anthony's to review and change.
- No source edits, no commits, no production writes this run. Scratch DB only.
- The 8473 instance currently running was rebooted by the human tester at 21:49 CDT from the same (fixed) tree; schema verified complete (74 tables).
