# Tester loop — bugs — 2026-09-24 00:35 CDT run

- **Commit tested:** `86e3684` ("Tab favicon is now the orb mark")
- **Target:** LOCAL instance http://127.0.0.1:8473, scratch DB `hidden_files/tester-loop/test.db` (fresh, deleted at run start). Real `data/townsquare.db` untouched. No branch switches, no commits, no source edits.
- **Production smoke (read-only, every run):** `GET /`, `/api/shorts`, `/musefm`, `/shorts` on https://musefm-townsquare.onrender.com — all **200**. Live site up.

## Verdict: NO P0, NO P1. 4 new P2s. Staying silent in chat per reporting rule.

## NEW P2s (all verified live by coordinator or via code + test_client repro)

1. **POST /logout has no CSRF check → cross-site logout works.** Repro: log in → `POST /logout` with empty form body, no `csrf_token` → 302 to `/`; subsequent `GET /submit` → 302 `/login?next=/submit` (session dead). Code: `logout()` (app.py:6120–6122) is `session.clear()` + redirect, never calls `_check_csrf()` (app.py:762), unlike sibling state-changing routes (`/submit`, `/post/<pid>/comment`, `/vote`, `/signals/react`, `/photos/upload` all 403 without valid token). Impact: logout only — no data change, no account takeover. (human tester; code-confirmed by coordinator)
2. **No rate limiting observed on public GET endpoints.** Repro: 40 sequential `GET /api/shorts?limit=5` in 2.2s → all 200; 10/10 rapid `GET /api/shorts?limit=5` → 200s (coordinator re-verified). Signed endpoints have per-action buckets (429 observed on shared-IP `post` bucket); public GETs aren't in the machinery. (adversarial; coordinator-confirmed)
3. **`/api/shorts/cards?page=-1` echoes `"page": -1` in JSON.** Repro confirmed: `{"html":"","next_page":null,"ok":true,"page":-1,"seed":"…","total":0}`. Underlying `videos.shuffled_short_page()` clamps internally — safe; just normalize before echo. Cosmetic. (adversarial; coordinator-confirmed)
4. **`/api/shorts` passes raw `limit` into query fns; the ≤50 clamp lives one layer down** (videos.py:697, videos.py:638). `?limit=999999` returns ≤50 rows — safe today. Code note: a future caller that skips those helpers inherits the unbounded value. (adversarial; behavior-verified)

**Dropped (worker lead disproven):** adversarial P2-1 claimed login-gated HTML pages (`/mod/flags`, `/settings`, `/workroom/invites`, `/mod/uploads`) return 200 with the login page. Coordinator re-check: all four return **302 → /login?next=…**. The worker's client followed redirects. Not a bug.

## Repo suites (~100 test_*.py, throwaway DBs/TestClient, 4-way parallel)

~70 pass; ~30 fail — ALL triaged, none new:
- **Signup-without-email cascade (commit 454ba91 made email mandatory):** `test_auth`, `test_sidebar` (106 pass/19 fail), `test_human_usability`, `test_identity`, `test_kind_tags`, `test_pet_onboarding`, `test_pet_presence`, `test_p1_csrf_human_handle_2026_09_18`, `test_workroom_pilot`, `test_workroom_private`, `test_openmic`, `test_signals`, `test_notifications`, `test_asks`, `test_ai_images`, `test_comments_pro`, `test_gifs`, `test_linking`, `test_mod_approval`, `test_mod_notifications`, `test_sso_provider`, `test_shorts`, `test_video`, `test_video_comments`, `test_uploads`, `test_upload_audio_mimetype_2026_09_18` (fails at signup assert — the underlying PNG-as-audio bug is FIXED; magic-byte sniffing holds). Already filed 09-23 21:35. Signup WITH email → 200 + row; verified healthy.
- **Stale/dead tests (already filed):** `test_musefm` + `test_p1_2026_09_18` (fb_reactions module removed, commit d2e9861); `test_workroom_bridge` (`ensure_workroom_events_schema` renamed — filed 09-23 00:35); `test_shorts_seen_rotation` 12 pass/6 fail (test-vs-design — filed 09-23 00:35); `test_tidepal_homepage` (regex grabs first band — filed 09-23 03:35); `test_links` (/arena 404, /pro 301 — retired/consolidated — stale); `test_p1_2026_09_18` XFF-bucket FAIL (intentional: ProxyFix only on Render — proven by `test_testerloop_xff_direct_2026_09_22` 4/4 pass); `test_listening_rooms` (AGENT_KEY env guard — pre-existing).
- **Harness artifact:** `test_shorts_shuffle` `sqlite3.OperationalError: disk I/O error` under 4-way parallel only — rerun serially: **15/15 pass**. Not a bug.

## Agent tester (signed/API flows): 46/50, no new bugs

Positive flows all green: unsigned identity register (validation 400s exact: duplicate/taken/reserved/bad-pubkey), signed forum post/comment/vote/identity_update, signed GIF upload with `file_sha256` binding (43-byte GIF89a → 200, then attached post → 200), platform-key/signal-credential/passport/activity envelopes all verifying offline with Ed25519, 13 attack negatives (replay/tamper/timestamp/nonce/wrong-key/action-mismatch) all 401 with exact cause. The 4 "fails" are the shared per-IP 5/hr `post` bucket exhausted by the parallel sibling testers on 127.0.0.1 — known loop artifact (noted 09-23), suggests per-tester XFF/ports next loop. Full report: `agent-tester-2026-09-24-0035.md` (+ script `agent-tester-script-2026-09-24-0035.py`).

## Human tester: full journey green, 1 new P2 (logout CSRF above)

Signup/login hardening exact (taken/`<script>`/emoji handles, reserved `admin`, bad emails, wrong-password ≡ nonexistent-handle 401, open-redirect `?next=` closed). Stored XSS escaped everywhere (titles, bodies, comments, photo titles, bios, submit prefill). `javascript:`/`file:`/`data:` URLs never linkified; https linkified with `target="_blank" rel="noopener nofollow"`. Length boundaries enforced (10001-char body → 400, 201-char title → 400, 2001-char comment → 400 "max 2000 characters" — the 09-23 P1 fix present). Vote toggle by design (1,0,1,0…); react allowlist exact; uploads magic-byte solid (fake PNG/SVG-script/empty → 400; 26 MB → 400 "max 25 MB"); anon gates correct (302 to login / 401 JSON); 404s clean, no tracebacks; page params clamp. iPhone UA → 200 + viewport meta. Full report: `human-tester-2026-09-24-0035.md`.

## Adversarial QA: ~110 probes, zero 500s, no P0/P1

404s branded, traversal blocked (400 "nope"), `.git`/`.env`/`/debug` → 404, malformed JSON → 400 (not 401/500), AuthZ gates 401 with precise errors, nonce one-time semantics correct (consumed even when downstream validation 400s), method mismatches → 405, query-param abuse safely clamped, garbage session cookie ignored, `/api/*` 500 handler generic JSON. Leaks re-observed but already filed: `Server: Werkzeug/3.1.8` banner, `/api/health` internals, trailing-slash 404-vs-405 (filed 09-22). No `/s/` share route in this checkout (404s; share links are canonical page URLs). Full report: `adversarial-2026-09-24-0035.md`.

## Environment notes for future loops
- Shared per-IP rate buckets repeatedly polluted the parallel testers (human `login` 429-locked, agent/human `post` 5/hr bucket exhausted). Give each tester its own port/DB or distinct XFF IPs.
- `SESSION_COOKIE_SECURE=True` over plain HTTP drops cookies in `requests` — harness artifact (noted 09-23).
- 4-way parallel suite run caused one `disk I/O error` (test_shorts_shuffle) — rerun suspicious parallel failures serially before filing.
