# AGENT TESTER REPORT — 2026-09-21 06:35 run (commit 62e646b, port 8473)

Verdict: no P0s, no P1s. All core signed flows work; attestations verify end-to-end.

## P0 — none found.

## P1 — none found.
Happy paths (200/201): identity register, identity get/404-unknown, signed post,
signed comment + nested reply, vote + clean double-vote, identity update
(kind_tag=robot; invalid kind_tag → 400 by design), signed multipart
upload gif/image/video (sha256 binding intact; ai_generated=true → approved,
false → pending + anon GET /img/<id> → 404 moderation gate holds; bytes
byte-identical; video served as video/mp4), duet_of valid → 200,
duet_of=999999 → 404 "duet_of: no such video upload".
Negative crypto all 401 as designed: unsigned, tampered, replayed nonce
(24h store), wrong action, future timestamp, wrong sha256, flipped
ai_generated. Memory write → 201 (REST-correct), signed read → 200, unsigned
read → 401. trustline/status → 200 {"linked": false}.
Attestations verify against /api/platform-key (key_id "musefm-platform-v1",
ephemeral in test env): signal/credential, passport, agents/<fm_id>/activity.
Tampered payload / wrong key_id / different key → verify fails.
signal/credential/fm_nope → 404 "no such muse".

## P2 / notes
1. Forged media URL negative tests on posts NOT run live: post rate bucket
   (5/hr, shared across parallel testers) exhausted → 429. Code inspection:
   db.create_post (db.py:805-810) runs image/gif/video fields through
   valid_*_url whitelist → ValueError → 400. Recommend retest when bucket clears.
2. Same contention blocked: long-form duet rejection (>180s → expected 400)
   and WebM upload path; verified by code inspection
   (validate_duration_secs, detect_video EBML + Segment scan). MP4 moov path
   passed live.
3. POST /api/trustline/link with unregistered pid → 400 "no such Trustline
   profile" — intended, but worth a line in agent docs.
4. Ephemeral platform key in dev/test: attestations won't verify after restart
   without MUSEFM_PLATFORM_KEY set — operational caveat.
5. Harness artifacts (not app bugs): round-1 multipart used wrong field name;
   round-1 attestation failures were verify-against-own-key errors.

## Rate-bucket observation for triage
The 5/hr post bucket being shared across all parallel testers on one box
caused 429s that blocked negative post tests. Consider per-tester allowance or
a loop-local rate-limit exemption for the negative-path suite.
