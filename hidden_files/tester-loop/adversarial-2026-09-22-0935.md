**ADVERSARIAL QA REPORT — Muse FM @ 127.0.0.1:8473 (scratch DB, hermetic run)**

Verdict: **no P0, no P1, no P2 findings**. The app held up across every probe. Server never crashed, zero tracebacks in `server.log` across all probes, no stack traces/paths/secrets leaked in any error response.

**What I probed (with exact repro):**

1. **404 handling** — `GET /nope`, `/api/definitely-not-real`, `/static/zzz.js`, `/admin` → all 404, clean branded HTML 404 page, `text/html; charset=utf-8`, no traceback, no path leak.

2. **Malformed JSON** — `POST {bad json` with `Content-Type: application/json` to `/api/forum/react`, `/api/forum/vote`, `/api/forum/post`, `/api/forum/comment` → all 400 `{"error":"Malformed JSON body","ok":false}`. `/vote` → 401 `{"error":"sign in to vote"}`. `/auth/token` with `{}` → 400 naming the missing fields. Wrong content-type (form-encoded to JSON endpoint) → clean 401 auth error, not a parse explosion.

3. **Unauthenticated writes (the dangerous list)** — every one properly gated, all clean 401 JSON with no auth-bypass surface:
   - `POST /api/memory/wipe`, `/api/memory/{1}/edit|delete`, `/api/memory` → 401 (musefm-v1 auth)
   - `POST /api/admin/identity/rotate-key` → 401 (`bad or missing agent key`)
   - `POST /api/swarm/submit` → 401 (pilot auth required)
   - `POST /api/forum/post`, `/api/collab`, `/api/workroom/create`, `/api/agents/profile`, `/api/agents/endorse`, `/api/row/journal` → 401 (musefm-v1)
   - `POST /photos/upload`, `/post/1/comment` → 302 redirect (login), nothing processed
   - `GET /api/memory/export` → 401. Nothing readable without auth that shouldn't be.

4. **Burst** — 20 simultaneous identical `POST /api/forum/react` → twenty 401s, no double-processing possible (auth gate first). 20 simultaneous `GET /api/shorts` → twenty 200s. Server healthy afterwards (`/health` 200).

5. **Reaction-picker edge cases** — blocked at the auth gate for me: `POST /api/forum/react` requires musefm-v1 signed body, so I could not reach emoji validation unauthenticated. Static read of the handler (app.py `api_react`) shows emoji whitelisting (`emoji not in REACT_EMOJIS` → clean 400), target_type validation, and unknown-target checks all *before* rate-limit burn and DB write — but that's code-read, not live-proven. Handing this live-validation to the AGENT TESTER subagent which has signed-auth capability.

6. **Share-link / id formats** — `GET /m/' OR 1=1` (URL-encoded), `/m/1'--`, `/episodes/' OR 1=1`, null byte (`/m/a%00b`), `%0d` in slug → all clean 404, no SQL-error strings, no injection surface visible. Path traversal: `/episode-video/..%2F..%2Fapp.py` and `/audio/..%2F..%2F..%2Fetc%2Fpasswd` → 400 `"nope"` (explicitly blocked in route). Raw `../../` in path → 404. `sqlite_int` converter edge cases (`/api/forum/post/99999999999999999999`, `/0`, `/-5`) → clean 404 JSON, no overflow error. `/photo-file/1`, `/999999999` → 404 `"nope"`, no file disclosure.

7. **Oversized input** — 2MB JSON body to `/api/forum/post` → processed in 0.46s, clean 401 (rejected at auth before payload did anything weird). 8000-char URL → 404, no 414/crash.

**Notes for triage:**
- `/api/platform-key` is public (200, returns the platform's public ed25519 key) — that's by design for signature verification, not a leak.
- `/api/agents` returns 200 with empty list — public read of agent directory; design call, not a bug.
- Initial confusion: two probes returned `code=000` — that was my curl failing on unquoted spaces in raw URLs, not the app. Re-ran URL-encoded; app returned clean 404s.
- Read-only discipline kept: I created nothing on the instance; only GETs and unauthenticated POSTs that were rejected.

**Bottom line for the parent:** Muse FM's input handling and auth gating are solid — no findings worth surfacing. The one untested area (reaction emoji validation live, double-react idempotency, signed-flow double-processing) requires signed auth and belongs to the AGENT TESTER.
