# Tester loop triage — 2026-09-23 18:35 CDT

**Commit tested:** `db9313d` ("Agent Onboarding life systems: memory, attachment, nudges, missions (2026-09-23, Anthony)")
**Test instance:** local, http://127.0.0.1:8473, scratch DB deleted at run start
**Production smoke (read-only):** `/`, `/api/shorts`, `/musefm`, `/shorts` → all 200 ✓

**⚠️ MID-RUN TREE SKEW:** at ~23:47–23:49 (during this run), the working tree gained uncommitted changes — 23 files, 1781 insertions / 103 deletions (app.py, db.py, grok_bot_watch.py, many templates incl. settings.html). The running test server (booted 23:36) does NOT include them. My subagents made zero file writes (verified in their transcripts); the change came from outside this run (Anthony's live work / another job). Notable: the tree now contains a `loud_limit()` fix for the known comment-truncation P1 (web + signed-API + db.py call sites all reject with 400 now) — fix is in-tree but UNCOMMITTED; needs commit + reboot to take effect (commit is Anthony's call; this run never commits).

## Verdict: QUIET — no P0s, no new P1s

## New findings this run (P2)

1. **P2 (nit) — Signed image upload records a mismatched original filename.** Real PNG bytes uploaded as `fake.svg` via `POST /api/upload/image` → 200 accepted; the stored file is correctly normalized (`img-<uid>.png`) but the DB `ai_uploads.filename` column records `fake.svg` while `mime` is `image/png`. Verified live on the test instance. Cosmetic; magic-byte validation itself is solid (text-as-PNG → 400, SVG-with-script → 400). (human tester; verified by coordinator)
2. **P2 — Omitted vote `value` silently records an upvote.** `POST /api/forum/vote` signed envelope with no `value` field → 200, a real upvote, because `value = _int_field(data, "value", 1)` defaults a missing field to 1. Malformed request silently becomes an upvote. Expected: 400 on missing `value`. (adversarial QA; verified by coordinator in code)
3. **P2 — Double-vote toggle gives no signal.** Same-direction re-vote toggles off (intentional, `# toggle off` in db.py), but the response is plain `{"ok":true,"score":0}` — no indication a toggle happened, so a double-click/client retry looks like the vote vanished. Suggest returning `toggled:true` or current-vote state. (adversarial QA)
4. **P3 (nit) — Inconsistent emoji error strings.** `/api/forum/react`: `emoji=0` (non-string) → 400 "bad emoji: must be a string"; `emoji=null`/`""`/missing → 400 "emoji must be one of: …". Cosmetic. (adversarial QA)
5. **P2 (re-observed / fix-not-landed) — Missing signature reports "bad signature encoding".** Signed envelope with no `signature` key → 401 "musefm-v1 auth failed: bad signature encoding" (identity.py has no `if signature is None` guard, unlike the timestamp path). Filed 09-22; claimed fixed 09-22 18:35 — but the fix is NOT in this checkout: `test_testerloop_error_messages_2026_09_22.py` (the proving test) now fails 2/6 (missing-signature wording + react missing-target_id naming). Same for the sibling "react missing target_id should name target_id" fix. (agent tester; verified by coordinator — ran the test file live)
6. **P2 (observation) — Rate-limit buckets are per-IP, shared across identities.** 130 rapid signed reacts → 26×200 then 104×429; the human tester's posts burned the shared 5/hr post bucket for the agent tester. By design, but one noisy neighbor (or all agents behind one NAT IP) can lock out everyone. (adversarial QA)

## New failing test file (tests only — no source edits per policy)

- `test_testerloop_1835_p2s_2026_09_23.py` — proves new P2s #1 (fake.svg filename label, `filename='fake.svg'` vs `mime='image/png'`) and #2 (vote without value → 200, expected 400). Run: 3 passed, 2 failed as designed.

## Deduped (already on file in last 48h, NOT new)

- Known still-open P1: comment CREATE/EDIT silently truncated at 2000 chars (filed 09-21 22:35; loud report already sent). Re-verified present on the RUNNING instance by all three personas (web: 2001-emoji body → 302, stored exactly 2000; signed API: 200000 chars → 200 id, stored 2000) — BUT the working tree now contains an uncommitted `loud_limit()` fix (see tree skew above). Fix pending commit + reboot.
- Reaction 400-vs-404 inconsistency on unknown target (filed 09-22).
- Vote toggle semantics (intentional, documented).
- Self-vote/self-react permitted (product call, not a bug).
- 4 `test_row.py` avatar failures — pre-existing pins (signup now requires email, commit 454ba91 today).
- `test_shorts_seen_rotation` 12/6 fails (test-vs-design, filed 09-23 00:35); `test_workroom_bridge` AttributeError (renamed fn, filed 09-23 00:35); `test_community_episodes` module-level sys.exit.

## New stale-test instances (tests need refresh — app verified healthy)

- `test_workroom_pilot.py` 50 pass / 5 fail (`human signup ok`, `human login redirects`, `session cookie auth`, `human web create`, `bad csrf`) and `test_workroom_private.py` (400-vs-200): all root-caused to signup now requiring email (commit 454ba91, 2026-09-23) — the tests post without an email field. Verified live: signup WITH email → 200, account created, verification email fails gracefully locally.
- `test_sidebar.py`: standalone pytest collects 0 tests (prior runs reported 118/118 — must be a different invocation; flag for next run to reconcile).

## Repo suites (coordinator runs, hermetic TestClient / throwaway DBs)

- 7 pytest-structured files: 78 passed, 4 failed (the pre-existing test_row.py avatar/email-staleness pins).
- ~90 script-style files run as standalone scripts: all pass except the known stale pins above.
- Suite collection caveat: `test_community_episodes.py` and `test_shorts_seen_rotation.py` do module-level `sys.exit()` → pytest cannot collect them; ran as scripts.

## Persona reports (scratch DB only, residue deleted next run)

- **Human:** signup/login/CSRF, post/comment/vote/react/upload, episode comments, shorts, media streaming all solid. Stored XSS fully escaped everywhere (`<script>` → `&lt;script&gt;` in title/h1/meta/body/comments). Linkification scheme-safe (only https linked, `rel="noopener nofollow"`). 20 concurrent votes atomic (`BEGIN IMMEDIATE` holds). PNG under `.svg` filename accepted (P2 #1 above). Mobile-viewport visual check not possible from this session — flagged.
- **Agent:** ~50 checks. Identity register/update, signed writes, attestations (platform-key/passport/activity envelopes all verify, tampered fails) all green. All auth negatives correct 401s (corrupt/wrong-key/bad-b64/stale timestamp/bad nonce/missing timestamp/unknown fm_id/tampered/action-mismatch/replayed nonce + replayed signed GET). Uploads: PNG→200, sha256 mismatch→401, non-image→400, >4MB→413, GIF→200, truncated MP4→400, >32MB→413. Note: post-route attach tests blocked by shared per-IP post bucket (429) — re-run next loop with a fresh bucket.
- **Adversarial:** ~200 probes. 404s branded (HTML) / JSON for /api/*, zero stack traces. Path traversal blocked, malformed JSON → 400 everywhere, unauthenticated admin/mod/agent-memory POSTs → 401, open redirects safe (`_safe_next`), XSS escaped, SQLi inert, 5MB JSON → 401 no hang, `limit=999999` capped, wrong methods → 405.

## Caveats

- Bug reports live in hidden_files/tester-loop/ (uncommitted) — the branch is Anthony's to review and change.
- The running server did not include the 23:47–23:49 tree change; all "running instance" findings above predate it.
