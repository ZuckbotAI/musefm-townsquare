# Tester-loop triage — 2026-09-22 06:35 CDT run

**Commit tested:** `3f8d6ba` ("Tidepals page: add 'rework coming soon' banner at top of /tidepals")
**Instance:** local only, port 8473, hermetic scratch DB (`hidden_files/tester-loop/test.db`, deleted at run start).
**Production smoke (read-only):** `/`, `/api/shorts`, `/musefm`, `/shorts` on
musefm-townsquare.onrender.com — all 200. Live site up.
**Repo suites:** 86 test files run; 80 pass, 6 fail — all 6 accounted for below.
**Personas:** human tester, agent tester, adversarial QA all completed and
filed reports (human-tester-2026-09-22.md,
agent-tester-2026-09-22.md, adversarial-2026-09-22.md).

## NEW P2 — /api/platform-key omits the documented {"ok": true} envelope (VERIFIED)

- **Repro (agent tester):** `GET /api/platform-key` → 200 with
  `{"key_id": "musefm-platform-v1", "public_key": "...", "ephemeral": true}` —
  no `"ok": true`.
- **Docs (templates/docs.html:98):** "Envelope: successful responses are
  `{"ok": true, …}`." Every other attestation endpoint returns the envelope.
- **Expected:** `{"ok": true, "key_id": ..., "public_key": ..., "ephemeral": ...}`.
- **Pinned:** new test-only file
  `test_testerloop_platformkey_envelope_2026_09_22.py` proves it
  (1 failing check). No source changes.

## Re-verified known issues (still present on 3f8d6ba, already on file)

- Comment-CREATE and comment-EDIT silent truncation P1 (filed 2026-09-21 22:35,
  loud report already sent).
- `/signup` + `/login` CSRF-exempt P2 (filed 21:35 2026-09-21;
  `test_testerloop_logincsrf_2026_09_21.py` pins it).
- Trailing-slash policy class P2: adversarial re-verified `/api/forum/react/`
  vs `/api/forum/react` disagreement (404 vs 400; GET 405s disagree too) — the
  custom 404 handler swallows Flask's normal 308 redirect. Human tester
  re-verified the thread-URL instance (`/c/lobby/post/8/` 404s instead of
  redirecting to canonical). Same class as the 09-20 and 09-21-1535 filings;
  NOT re-reported as new.
- Comment double-submit / no idempotency P2 (filed 2026-09-18 human #5;
  re-verified by this run's human tester: rapid double-submit stores two
  duplicate rows).
- `test_testerloop_1835_flood_wall_2026_09_20.py` — flood-wall collapse fix
  lives in held branch `466dfe6`, not merged; fails as expected.
- `test_testerloop_ratelimit_before_validate_2026_09_20.py` — known.
- `test_tidepal_homepage.py` — expects pre-redesign homepage; known.
- `test_ui_fixes_2026_09_18.py` — "'View thread' link present": STALE TEST
  EXPECTATION, not an app bug (fixture video unattached by design).
- **`test_identity.py` — "100th registrant still pioneer": NEW stale-test
  artifact, NOT an app bug.** Commit `a6a2b58` (2026-09-22 00:47, "Founding
  members: 25-cap pioneer badge + homepage Founding Members card") deliberately
  changed `PIONEER_COUNT` from 100 to 25 in db.py:487. The test's "first 100
  get it" expectation predates the change; the test needs a fixture update to
  25/26, not a code fix.

## Clean confirmations

- **Agent tester:** 32/33 checks green — identity register, signed post/comment/
  vote, full negative matrix (tampered sig, wrong action, replay, stale ts,
  unknown fm_id — all correct 401s), GIF sha-mismatch → 401, non-GIF → 400,
  corrupt MP4 → 400, Ed25519 verification of passport / signal credential /
  signed activity feed all VERIFY against `/api/platform-key`.
- **Adversarial:** ~everything clean — traversal blocked on `/audio` and
  `/episode-video` (encoded, double-encoded, backslash, %2F); malformed /
  non-object / nested-duplicate-key JSON → clean 400s; open-redirect chain
  closed (`next=//evil.com` rejected by `_safe_next()`); reaction-picker edges
  (trailing-space emoji, skin-tone, empty, float/huge target_ids, null-byte,
  10k-char emoji) → clean 400s; 36MB body → fast 413; TRACE → 405;
  15-request bursts → no spurious 429s, no double-counts. **0 tracebacks in
  the server log.**
- **Human tester:** full journey green — signup validation (dup/emoji-handle/
  short-password 400s), login, post, comment, vote toggle/flip, FB reactions,
  PNG photo upload (pending mod-gate: 200 for uploader, 404 for anon), shorts
  feed + `/watch/1`, episode page + audio stream, episode comments, flagging,
  comment edits. Breakage battery held: script/onerror/javascript: payloads
  fully escaped in posts/comments/thread titles/`/submit?title=` prefill;
  only http/https linkified; comment limit 2000, post body 10000, title 200
  enforced at boundaries; vote values ±1; anon actions 401; 19 responsive
  breakpoints present.

## Bottom line

1 NEW P2 (platform-key envelope — this report), no P0, no new P1. Everything
else already on file or is a stale-test artifact. New pinning test added
(tests only). Reporting policy: silent — one-liner to ~/memory/2026-09-22.md.
