## AGENT TESTER report — Muse FM machine API (local test instance, 2026-09-22 18:35 CDT run)

**Target:** hermetic in-process Flask TestClient against the CURRENT checkout (scratch /tmp DBs, isolated REMOTE_ADDRs) — the live 127.0.0.1:8473 scratch server was read-only touched never. No production touched.
**Commit tested:** `891fab7` + uncommitted working-tree changes (identity.py, app.py, db.py, videos.py, agent_guide.txt et al — the loop tests the checkout as-is; NOTHING committed by this run).
**Harnesses:** re-ran `agent_api_hermetic.py` (39 checks, 0 findings) + new gap-coverage script (pets, openmic, audio upload, community episodes, memory_write, attestations, /api/agents, /api/docs) + targeted probes.

### Happy paths — all verified working
Register (dup handle 400, bad pubkey 400); signed post/comment/vote/react; the full
negative signature matrix (tamper, replay, wrong key, expired/future ts, missing/empty
fields, unknown fm_id, wrong action) — all correct 401s; GIF/PNG/MP4 uploads with
sha256 binding (wrong sha → 401, bad magic → 400, truncated MP4 → 400); media URLs on
posts (external/`javascript:` → 400); identity_update; heartbeat + rewards read;
pet species/rules/adopt (2nd adopt → 400, bad species/name → 400), pet feed → 200;
memory_write → 201; /api/platform-key, /api/passport, /api/signal/credential,
/api/agents/<fm_id>/activity?signed=1 — credential + passport envelopes independently
verify against the platform key (canonical JSON, sort_keys, Ed25519);
/api/agents?skill=python, /api/docs → 200; audio upload (valid WAV) → 200;
episode publish own audio → 200 live, cross-identity publish → 400
("that audio isn't yours"), overlong title/desc → 400, unknown upload → 400;
openmic own-audio → 200 pending, foreign-audio → 400 (identity-fraud gate works),
2nd pending clip → 400.

### NEW findings

**P1 — unauthenticated junk uploads burn the shared upload budget (new instance of the
2026-09-20 21:35 P1 class).** `api_upload_audio` (app.py:5632) calls
`check_limit("upload", 10)` BEFORE `verify_signed_body`. Repro (hermetic):
3 multipart POSTs to /api/upload/audio with NO signature fields at all →
each 401 `musefm-v1 auth failed: missing fm_id`, yet `_hits[("upload", IP)]`
grew 0 → 3. Ten junk requests/hour lock out every legit signed agent's audio
uploads for an hour — anyone, no identity needed. The 09-20 pin named
/api/workroom/create (P1) and mentioned /api/upload/gif + /api/asks as same-class;
audio is a new instance. Code inspection confirms the identical ordering in
/api/upload/gif (app.py:5908), /api/upload/image (app.py:5943), /api/upload/video
(app.py:6015). Proving test (fails as designed):
hidden_files/tester-loop/test_testerloop_upload_ratelimit_before_validate_2026_09_22.py.

**P2 — agent_guide.txt step 11 lies about the audio form field.** The new
(uncommitted) guide section says the file goes under the "file" field; the route
reads `request.files.get("audio")` only. Following the guide verbatim:
`POST /api/upload/audio` multipart with field "file" → 400
"no file — send the audio under the 'audio' field". Self-correcting error, but the
documented machine flow is wrong on first contact.

**P2 — verified-but-field-invalid media URLs burn the "post" 5/hr budget.**
The route's own comment (app.py ~2217) says "Validate BEFORE counting the rate
budget"; community/title/body were moved before `check_limit("post", 5)`, but
image_url/gif_url/video_url validation happens inside `db.create_post()` AFTER it.
Repro: verified signed post with image_url="https://evil.example.com/x.png" →
400 "bad image url -- attach via /api/upload/image" AND 1 budget hit recorded; only
4 further valid posts accepted in the hour instead of 5. Requires a registered
identity (fairness nit, not unauthenticated DoS).

**P2 (nit/edge) — sub-second audio is permanently openmic-unsubmittable.**
`probe_duration` int-truncates (0.25s WAV → duration_sec=0 stored), and
openmic.submit_clip fail-closes on `duration < 1` with "audio appears empty".
A legit sub-1s clip can never be submitted; stored metadata says 0s.

### Verified FIXED in the working tree (15:35 P2s)
- P2 #4 (missing signature said "bad signature encoding"): now 401 "missing
  signature". Empty-string signature → 401 "signature does not verify" (acceptable).
- P2 #3 (react missing target_id said "unknown target"): now names the param.
`test_testerloop_error_messages_2026_09_22.py` → 6/6 PASS.

### Not bugs (for triage)
- forum/post DOES validate-before-limit for unverified requests: 3 failing (401/400)
  requests first, then 5 valid posts all 200 — sig-failures don't burn the bucket.
- The earlier hermetic "nonce-reuse → 429" was the 5th recorded post hit (two
  verified-but-400 media-URL posts in the harness record hits) — see P2 above.

### Notes for triage
- Test artifacts live only in /tmp hermetic DBs. No source edits, no commits, no
  production writes, no writes to the live 8473 scratch DB.
- New proving test: hidden_files/tester-loop/test_testerloop_upload_ratelimit_before_validate_2026_09_22.py
  (fails 1 check as designed until the ordering is fixed).
