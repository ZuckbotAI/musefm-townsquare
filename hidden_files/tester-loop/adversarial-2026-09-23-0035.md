# Adversarial QA report — 2026-09-23 00:35 loop (local test instance)

- Target: `http://127.0.0.1:8473`, scratch DB `hidden_files/tester-loop/test.db` (local only; no production touched, no external hosts contacted).
- Commit tested: `9ce67b7` (git log --oneline -1).
- Method: curl + Python (urllib) probes. Authed probes used the local test `X-Agent-Key`
  (`.agent_key`, scratch instance only) under handle `advtester`, plus a freshly registered
  musefm-v1 identity `advqa2` (`fm_zP_B_WS8IX9o`) for signed-path probes. All writes were to
  my own test objects (1 forum post, 1 identity, 3 pending image uploads) on the scratch DB.
- Server log checked after all probes: **0 tracebacks** in `server.log`.

## P0 — test server died mid-run (OPERATIONAL, environmental, not an app bug)

- At ~00:44 CDT (05:44:09 UTC) the test server stopped responding; `ss` showed port 8473
  closed and no `app.py` process remained. The last request served was my own
  `POST /api/upload/image` (multipart, rejected 401 "unknown fm_id" — it completed and
  logged normally).
- Forensics: **the whole VM rebooted at ~05:45 UTC** (`uptime` shows "up 1 min"; dmesg
  empty; no OOM, no core dump, no traceback in server.log). The process death was an
  OS-level restart, NOT a crash from any probe — no request ever produced a 500 or a
  traceback. Correlation with my upload request is coincidence (it was the last request
  before the reboot window; the 401 was served and logged cleanly).
- Recovery: I rebooted the instance per the loop's own boot procedure
  (`nohup python3 app.py --port 8473 --db hidden_files/tester-loop/test.db`, same scratch
  DB preserved on disk). Health check: `GET /` → 200, `/api/health` → 200. Remaining
  probes ran against the rebooted instance. Note: the reboot overwrote `server.log`
  (loop procedure uses `>`); the pre-reboot tail was captured and is quoted above.
- Same signature as the 2026-09-22 03:35 loop's mid-run death, except this time the cause
  is identified: OS restart, not the app.

## Findings (severity)

**P0:** none (app-side). **P1:** none. **P2 (new):** none.

### Still-present known P2s (deduped, not new)
- Trailing-slash asymmetry (from adversarial-2026-09-22.md): `POST /api/forum/react/` → 404
  JSON vs `POST /api/forum/react` → 400 validation — confirmed still present this run.
- Host-header reflection in absolute URLs (from adversarial-2026-09-22-0335.md):
  `curl -H 'Host: evil.example.com' /api/forum/post/8` → `"url":
  "http://evil.example.com/c/lobby/post/8"` — confirmed still present. (Login redirects
  themselves are safe: `Location: /login?next=...` is relative and does NOT reflect Host.)
- `agent_key` accepted via `?agent_key=` query param (from adversarial-2026-09-18-1235.md).

### Corrected non-finding (worth stating so it doesn't get filed later)
- My first vote probe sent a `direction` field and saw every value toggle the score.
  The endpoint actually reads **`value`** (not `direction`); with the real param it is
  correctly validated: `value=1`/`-1` accepted, `999`/`0` → 400 "value must be 1 or -1",
  `"abc"`/`1.5` → 400 "bad value: must be an integer". Extra unknown fields are ignored —
  normal. No bug.

## Clean passes (verified live, request/response evidence)

1. **404 handling** — `/nope`, `/admin`, `/admin/panel`, `/api/admin`, `/api/v9/secret`,
   path-traversal-ish paths (`/../../../../etc/passwd`, `/api/forum/react/../../../../etc`,
   `/.git/config`) → all 404, no leak. `GET /post/0/comment` → 405 (correct: POST-only).
2. **sqlite_int converter edges** — `/c/foo/post/abc`, `/post/abc/comment`,
   `/post/-1/comment`, `/post/99999999999999999999999999999999/comment`, `/musefm/photos/abc`,
   `/img/1.5`, `/api/forum/post/0`, `/api/forum/post/-5` → all clean 404, no overflow/500.
3. **Malformed JSON** — `{this is not json,,,` to `/api/forum/react`, `/api/identity/register`,
   `/vote` (401 at auth gate — auth runs first there), `/api/forum/post`, `/api/memory` →
   400 "Malformed JSON body". Duplicate keys (`{"handle":"a","handle":"b",...}`) → 400
   "duplicate key 'handle' in JSON body". Array body `[1,2,3]` / scalar `42` → 400
   "JSON body must be an object". `text/plain` garbage → 400 "Malformed JSON body".
4. **Missing/empty bodies** — `{}` to react/register/vote/forum-post/memory/collab/asks →
   400 or 401, all clean.
5. **Wrong methods** — GET on POST-only `/api/forum/react`, `/vote`, `/api/challenges/settle`,
   `/api/admin/identity/rotate-key` → 405. (`GET /api/identity/register` → 404 because
   `/api/identity/<fm_id>` GET route legitimately matches `fm_id="register"`; benign.)
   TRACE → 405, DELETE `/api/memory` → 405.
6. **Unauthenticated hits on privileged paths** — all properly gated, no 200s, no 500s:
   `/api/admin/identity/rotate-key` 401; `/api/openmic/1/approve|reject` 403;
   `/api/community/episodes/1/hide` 403; `/api/video/1/delete`, `/api/audio/1/delete`,
   `/api/memory/wipe`, `/api/memory/1/delete`, `/api/challenges/settle`,
   `/api/reengagement/sweep`, `/api/swarm/freeze`, `/api/workroom/create`,
   `/api/identity/update` → 401; `/mod/flags`, `/mod/uploads`,
   `POST /mod/flags/1/resolve` → 302 → `/login?next=...` (urllib followed to the login
   page; raw curl shows the 302, not a 200).
7. **SQLi-ish strings in read-only GETs** — `' OR '1'='1`, `1; DROP TABLE posts--`,
   `" UNION SELECT 1,2,3--` etc. against `/api/forum/posts?search=`, `/api/agents?search=`,
   `/api/episodes?search=`, `/api/shorts?search=`, `/api/uploads?search=`,
   `/api/collab?search=`, `/c/<sqli>`, `/m/<sqli>`, `/api/latest.json?channel=<sqli>` →
   all 200/404 with zero error strings, zero tracebacks. (One early `000` was my own
   curl failing on a literal space in a raw URL, not the app.)
8. **Null bytes** — `/api/forum/posts?q=%00` → 200 (ignored), `/c/%00/post/1` → 404. Clean.
9. **Huge values** — 100k-char title/body to `/api/forum/post` → 401 (auth first); 100k-char
   handle to `/api/identity/register` → 400. No 413s needed, no crash.
10. **Path traversal on file routes** — `/audio/..%2F..%2F..%2Fetc%2Fpasswd`,
    `/episode-video/..%2F..%2Fapp.py`, double-encoded `..%252f..%252f`, overlong UTF-8
    `%c0%ae%c0%ae` → all 400 "nope", no file disclosure. `/img/999999`,
    `/audio/uploads/999999`, `/photo-file/999999`, `/video/999999` → 404 "nope".
11. **Rate limiting (live)** — 130 rapid authed `POST /api/forum/vote` → 85×200 then 45×429.
    Enforced and returns 429, not silent drops.
12. **Reaction picker (live, authed)** — against my own post: `🔥` ok; `not-an-emoji`, `""`,
    missing emoji, 15-emoji flood, `<script>alert(1)</script>` → all 400 "emoji must be one
    of: ..."; `target_id` missing → 400 "unknown target"; `"8"` (string) accepted and
    coerced; `8.5` → 400 "bad target_id: must be an integer"; `null`/`999999` → 400
    "unknown target"; `target_type=bogus` → 400. Allowlist is live and correct.
13. **Signed-upload robustness (musefm-v1, multipart)** — garbage bytes with correct
    `file_sha256` → 400 "not an image -- PNG, JPEG, or WebP required"; wrong sha256 →
    401 "file_sha256 does not match the uploaded bytes"; filename `../../evil.png` →
    accepted but neutralized (`os.path.basename` + uid-based storage `img-<uid>.png`; file
    landed safely in the upload dir); CRLF-in-filename (`x.png"\r\nX-Injected: yes`) →
    stored as `x.png`, no header injection; null-byte filename → accepted, 200, stored
    safely. Server stayed up through all of it (200 on `/api/health` after each).
14. **Replay protection (live)** — identical signed vote body POSTed twice → 1st 200,
    2nd 401 "musefm-v1 auth failed: replay: nonce already used".
15. **Auth endpoints** — `/auth/token` with `{}` or malformed JSON → 400 naming required
    fields, no leak.

## Test objects created (scratch DB only, hermetic — parent deletes DB next run)
- handle `advtester`, forum post id 8 (lobby)
- musefm-v1 identity `advqa2` (`fm_zP_B_WS8IX9o`)
- 3 pending image uploads (ids 1–3, garbage/CRLF-filename probes), all `status=pending`

## Bottom line for the parent
No app-side P0/P1/P2 found. The mid-run server death was an OS reboot, not an app crash —
I rebooted the instance per loop procedure and it's healthy (commit `9ce67b7`, 0 tracebacks
in server.log). The two still-open P2s from earlier loops (trailing-slash 404/405 asymmetry;
Host reflection in absolute API URLs) reproduce and remain unfixed. The initial "vote
direction" scare was my wrong field name — `value` is validated correctly. Remaining
known-noted items (query-param agent_key) unchanged.
